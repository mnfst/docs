# Backlog — out-of-scope observations parked for their own topic

One line each: `- <date> · <page> · <what looks wrong> · spotted during <topic> audit`. The owning topic's next audit consumes and clears these.

- 2026-08-07 · self-hosted.mdx · "first account becomes admin" wording vs the /setup screen; "unknown model falls back to routing" vs M302; fresh-install table swaps M100/M101 · spotted during getting-started audit (already raised in issue #49, do not re-raise)
- 2026-08-07 · deploy/aws,render,easypanel,koyeb · instance-mode env var documented in guides but absent from the reference · spotted during getting-started audit (already raised in issue #49, do not re-raise)
- 2026-08-07 · errors/M201.mdx, M202.mdx, M203.mdx · docs tell users to raise rate/concurrency limits by editing source files (code constants presented as configurable) · spotted by check-coherence.mjs, owner: error catalog topic · CONSUMED 2026-08-14 → raised as issue #58 finding 2 (do not re-park)
- 2026-08-10 · reference/api.mdx:6,204,208 · CONSUMED 2026-08-16 → raised as issue #68 finding 2, extended to reference/headers.mdx:47 and reference/glossary.mdx:53 which this line did not list (do not re-park) · same false "424 on fallback exhaustion" concept already raised for llm-gateway.mdx (issue #53 finding 2), echoed here in frontmatter keyword + status table + prose: no backend code path emits 424 (grep clean in packages/backend/src); exhaustion preserves the upstream/primary status and signals via the X-Manifest-Fallback-Exhausted header + body error.code fallback_exhausted (proxy-response-handler.ts:403-411) · spotted during full audit of PR #2703 (gateway contract tests), owner: reference topic
- 2026-08-10 · reference/headers.mdx:17 · CONSUMED 2026-08-16 → raised as issue #68 finding 7; SETTLED: zero inbound readers, the contract test proves a header-less call reaches auth not a 400, so both "Required" and "Forwarded" are false (do not re-park) · claims anthropic-version is "Required for /v1/messages", but code only shows Manifest INJECTING anthropic-version: 2023-06-01 on the OUTBOUND provider request (provider-endpoints.ts:105,114,122); no inbound client-side enforcement found in the Anthropic adapter or auth guard. Verify whether a client omitting the header actually gets rejected, or whether "Required" overstates it · spotted during full audit of PR #2705 (gateway contract tests), owner: reference topic
- 2026-08-12 · reference/environment-variables.mdx · CLEARED 2026-08-16 as written, and re-raised on a different basis as issue #68 finding 6 (do not re-park): the trigger condition never fired (issue #56 is still `raised`, unmerged) and a grep of the whole *.mdx corpus finds zero mentions of either host-mapping var, so nothing is used-but-undefined and the never-halfway invariant HOLDS today; check-coherence.mjs:68-81 machine-enforces that invariant, so retiring this line loses no tripwire. What IS wrong today is the bind-interface row's own advice, which #68 finding 6 raises; its recommended option adds the host-bind row and pre-satisfies the invariant when the #56 fix lands · the compose-level host-mapping vars (the LAN-bind var + host-port var, docker-compose.yml:50) are defined in no docs page; if the self-hosted.mdx LAN fix (issue #56) introduces the LAN-bind var into prose, the reference must gain its row too (never-halfway invariant) · spotted during self-hosting-with-docker audit, owner: reference topic
- 2026-08-10 · reference/api.mdx:194-195 · CONSUMED 2026-08-16 → raised as issue #68 finding 3, bundled with the 429 status row (same false concept) (do not re-park) · error-envelope example shows error.type "limit_exceeded" with error.code 429 (numeric = HTTP status); no wire response produces this — the M200 limit_exceeded case is delivered as an HTTP 200 friendly stub not a 429 error envelope (issue #53 finding 3), and real error codes are string identifiers (auth → manifest_auth, quota → PLAN_LIMIT_REQUESTS), never the numeric status · spotted during full audit of PR #2703, owner: reference topic
- 2026-08-16 · llm-gateway.mdx:52 · the custom-tiers section says a matching header routes "to that tier's model instead of the default" but never says the match also OVERRIDES an explicit model in the request body (proxy.service.ts:903-905,976-978, rationale :957-959); same gap as issue #68 finding 8, on the routing topic's own page — both fixes should use the same sentence · spotted during the reference audit, owner: llm-gateway topic
- 2026-08-16 · self-hosted.mdx:321 · "Everything else is optional: rate limits, provider timeouts, … See Environment variables for the full list" implies a self-hoster can configure gateway rate limits by env var; no env var reaches the gateway (proxy.controller.ts:123 @SkipThrottle(); proxy-rate-limiter.ts:4-9 hardcoded 200/500/10). Same root cause as issue #68 finding 1, on the self-hosting topic's page · spotted during the reference audit, owner: self-hosting-with-docker topic
- 2026-08-16 · errors/M201.mdx:4,21 + errors/M203.mdx:4,19 · the per-minute cap is called "per user" and the concurrency cap "per harness", but both counters key on the tenant (proxy.controller.ts:378-380, proxy-rate-limiter.ts checkLimit/acquireSlot on tenantId) — i.e. per workspace. Worth fixing in the same wave as issue #68 finding 1 so the reference and the error catalog do not contradict each other · spotted by the issue-verifier during the reference audit, owner: error catalog topic
- 2026-08-15 · providers/api-key-providers.mdx · one Meta model carries a training-data caveat inside its own display name (packages/shared/src/providers.ts:55-62, muse-spark-1.2-contributor → "Muse Spark 1.2 Contributor (inputs and outputs may train Meta)"), a user-facing privacy fact stated on no page; display labels can be overwritten by catalog enrichment (model-discovery.service.ts:679-682), so confirm what the picker shows before writing it · spotted during full audit of PRs #2700/#2702/#2717, owner: providers topic
- 2026-08-17 · PLATFORM, not docs · the harness Settings copy tells self-hosted users their logs "stay in your own database", and the same sentence appears in both harness-creation flows (SettingsRecordingSection.tsx:44, AddAgentModal.tsx:258, Welcome.tsx:964); logs never touch Postgres, they are gzipped to a disk volume or S3 (attempt-recording.service.ts:21-44). This contradicts request-logs.mdx:80 "Recordings don't live in Postgres", so a self-hoster reading both gets opposite answers about where their prompts are. Fix belongs in the platform, not here · spotted during the request-logs audit
- 2026-08-17 · reference/environment-variables.mdx:67 · the storage-mode row lists the four accepted values but does not say that any other value, including a typo, silently yields no storage backend and drops every log (request-recording-storage.service.ts:93-98); same silent-failure class as issue #69 finding 3, on the reference page · owner: reference topic · spotted during the request-logs audit
- 2026-08-17 · PLATFORM, not docs · the in-app help screen's "Schedule a Call" button points at a 15-minute booking slug that returns HTTP 404 today (live-checked); the docs navbar CTA (docs.json:91-92, mirrored in scripts/deploy-mode-toggle.js:147) uses a DIFFERENT, 30-minute slug that returns 200. The retired promo offer used the dead slug, so removing the offer looks to have taken the event type with it. Same screen has no navigation entry anywhere in the app (Sidebar and the avatar menu carry no Help item; the route is reachable only by typing the URL). No *.mdx references either, so there is no docs sentence to correct — this is a platform fix · out of the #2720 window · spotted during the full audit of PR #2720
- 2026-08-17 · providers/api-key-providers.mdx:42 · "add the tile and you have a free model to route to" holds only where the instance is wired to the managed credits gateway and the account clears its eligibility list; on a self-hosted instance without that wiring the tile connects to nothing, and the auto-provision runs silently in the background on every provider fetch (managed-free-providers.ts:41-73, managed-free-provider.service.ts:57-118, frontend services/api/providers.ts:119-126). Deployment dimension of CASE-MATRIX · owner: providers topic · spotted during the full audit of PR #2720
- 2026-08-17 · HARNESS FILES, not docs · CLAUDE.md and several decisions.md lines write internal codenames and an internal hostname in clear text, which AUDIENCE.md:61-67 forbids in this public repo (descriptors only, even when the name is public elsewhere). Redacting the working copy does NOT unpublish them: they are already in this repo's public git history, so an effective fix needs a decision from Seb about history, not a silent edit by a cron run. Deliberately NOT fixed autonomously for that reason · owner: harness maintenance, needs Seb · spotted during the full audit of PR #2720
- 2026-08-17 · PLATFORM, not docs · the restored self-hosted email-provider block is blind to the environment: it is driven only by the saved row (Limits.tsx:254-260), so an install with the email env vars set and no row shows the "Configure email provider" tiles as if nothing were wired, and no "configured by environment" state exists in that block (the rules table's "No provider" tag does read the env, Limits.tsx:41-42,59-60, so the page is inconsistent with itself). Fix belongs in the platform · spotted during the full audit of PR #2727
- 2026-08-17 · PLATFORM, not docs · the alert send path reads the saved provider row with no deployment-mode gate (notification-cron.service.ts:110, limit-check.service.ts:121) while the cloud UI is read-only (Limits.tsx:246-252): a cloud tenant carrying a legacy row would have its alerts sent through it with no screen to see, edit or remove it. Data question, may be an empty set · spotted during the full audit of PR #2727
- 2026-08-17 · self-hosted.mdx · a self-hosted install running outside a container and without the instance-mode variable is detected as CLOUD (detect-self-hosted.ts:16-26 falls through to cloud), so its Limits page shows the read-only cloud email panel and the provider form is unreachable; a failed setup-status call has the same effect (setup-status.ts:33-40,54-61). Cleared as a finding this run because every documented install path is containerised or sets the variable, and the same doc sentence offers the env-var path · owner: self-hosting-with-docker topic · spotted during the full audit of PR #2727
- 2026-08-17 · observability.mdx:42 · the Limits dashboard card promises "Threshold rules per harness, plus email provider setup" OUTSIDE the deploy toggle; that setup block is self-hosted only and Cloud gets a read-only recipient panel (Limits.tsx:246-252, CloudEmailInfo.tsx:15-18). Cleared this run because the defect predates PR #2727, which moved the platform toward the sentence, and the cloud branch at :92 already tells cloud users where their alerts go · owner: observability topic · spotted during the full audit of PR #2727
- 2026-08-17 · PLATFORM, not docs · the operator README names the recordings volume with the short prefix while the compose file pins the long one (docker-compose.yml:229-232); any docs page copying the README name would send users to a volume that does not exist. request-logs.mdx:84 currently has it right, so this is a tripwire, not a docs defect · spotted during the request-logs audit
- 2026-08-18 · reference/api.mdx + reference/headers.mdx + llm-gateway.mdx · no page tells Anthropic-SDK users that `x-api-key` is rejected on `/v1` and that the SDK needs `auth_token`/`authToken` plus a base URL WITHOUT the `/v1` suffix; the guard reads `authorization` only (agent-key-auth.guard.ts:106-136), `x-api-key` is on the proxy strip list (request-headers.ts:14) and is a management-API header (api-key.guard.ts:42-47), and the product's own generated snippets do exactly this (framework-snippets.ts:164-166,178-210). The docs claim an Anthropic-compatible endpoint with no page showing a working client config. Deliberately NOT written into the getting-started fix: stating it there alone would create the halfway state the coherence invariant bans · owner: reference topic · spotted during the getting-started audit
- 2026-08-18 · errors/M204.mdx:26 · "wait for the monthly request quota to reset" never says when; the count clears at 00:00 UTC on the 1st (request-quota-window.ts:11-13, plan.service.ts:199-207), and no *.mdx states the moment (grep of the whole corpus for "reset" returns only M204:4,26,27 and M200). Consumed if issue #71 finding 3 ships as option 2 or 3 · owner: error catalog topic · spotted during the getting-started audit
- 2026-08-18 · llm-gateway.mdx:60,78 · the corpus's only cloud request snippets, and both assume the reader already holds a `mnfst_` key and a connected provider; the page never says where a key comes from. This is where cloud readers currently land from the introduction, so it is resolved for most readers if issue #71 finding 2 ships option 1 or 3 · owner: llm-gateway topic · spotted during the getting-started audit
- 2026-08-18 · self-hosted.mdx:126 · "Signing up leaves you with an empty instance" describes the self-hosted first run as a signup, while the platform creates the first admin through the setup screen and lands on the dashboard (Setup.tsx:39-53). Same actor-wording class as the accepted issue #49 finding 2 on that page · owner: self-hosting-with-docker topic · spotted during the getting-started audit
- 2026-08-18 · images/ · hero-light.png, hero-dark.png and checks-passed.png are referenced by no page (grep of *.mdx, docs.json and style.css is empty); the heroes are decorative logo plates, checks-passed is a GitHub check panel. Housekeeping, not a user-facing defect · owner: harness maintenance · spotted during the getting-started audit
- 2026-08-18 · deploy/*.mdx (aws:86, coolify:74, digitalocean:70, easypanel:103, fly:74, gcp:86, heroku:75, railway:57, render:65) · every production-notes section carries a domain-before-OAuth line, verbatim as "Use a custom domain before configuring production OAuth callbacks." on digitalocean:70, heroku:75, railway:57 and render:65, and reworded on aws:86, coolify:74, easypanel:103, fly:74 and gcp:86, and every one of them frames the domain switch as an OAuth concern only (koyeb has no such note and is not an omission: its BETTER_AUTH_URL is templated to the platform domain, koyeb.mdx:60); none says that the auth URL must follow the domain or every login on it fails with "Invalid origin" (trusted origins are BETTER_AUTH_URL alone in production, auth.instance.ts:46-68), and the setup screen cannot be used as a way back in once an admin exists (setup.service.ts:120-138). deploy.mdx:95 carries the rule prospectively; easypanel.mdx:97 has it as a smoke-test item, not an instruction. Deliberately dropped from the PR #2730 full run as too far from the window; resolved for those pages if issue #72 finding 4 ships option 3 · owner: deploy platforms topic · spotted during the full audit of PR #2730
- 2026-08-18 · PLATFORM, not docs · docker/install.sh:395-399 · the fresh-install success banner still tells users to expose the LAN by editing a compose ports line ("127.0.0.1:${PORT:-2099}:${PORT:-2099}") that no longer exists in the shipped file (docker-compose.yml:50 is the templated form) and that the installer upgrade wipes; #2730 fixed the operator README and left the banner. Surfaced in issue #72's platform-side section · spotted during the full audit of PR #2730
- 2026-08-18 · PLATFORM, not docs · the Docker Hub description sync workflow added by #2730 failed its first run with 401 Unauthorized (run 32122977375), so the live Hub page still carries the pre-#2730 LAN instruction; the same run warned the operator README exceeds Docker Hub's 25000-byte limit (26468 bytes), so once the credential is fixed the page ends mid-word and loses its last two sections. Surfaced in issue #72's platform-side section · spotted during the full audit of PR #2730
- 2026-08-18 · PLATFORM, not docs · docker/Dockerfile:97 · EXPOSE 2099 while the image sets no PORT, so the app listens on 3001 (main.ts:212); setting ENV PORT=2099 in the runtime stage would make the docs' docker run commands correct as written and removes the trap behind issue #72 finding 1. Decide this before that finding's docs PR picks an option · spotted during the full audit of PR #2730
- 2026-08-18 · reference/api.mdx:182 · the Streaming section gives the SSE shape for two of the three inference endpoints ("OpenAI-style `data: {...}` chunks for `/v1/chat/completions`, Anthropic event blocks for `/v1/messages`") and says nothing about `/v1/responses`, which streams its own sequence-numbered `response.*` event set on both the native and the converted path, with its own error event shape (proxy-response-handler.ts:548,576,588,749,824; chatgpt-adapter.ts:333,391; proxy.controller.ts:824-836). Out of the PR #2732 line scope (that window covered the endpoints table at :30-37 only) · owner: reference topic · spotted during the full audit of PR #2732
- 2026-08-19 · request-logs.mdx:34 · images/request-logs-drawer.png still shows the retired hyphenated spelling of the repair feature in the request meta row, the attempt badge and the attempt card; every render site now uses the unhyphenated product name (RequestDrawer.tsx:337,693,697), renamed platform-side on 2026-08-06. Same class as issue #80 finding 2 (the autofix.mdx stale screenshots); a re-capture of the drawer covers both pages · owner: request-logs topic · spotted during the autofix audit
- 2026-08-19 · PLATFORM, not docs · the operator README's Autofix privacy section (docker/DOCKER_README.md:447-450) tells self-hosters the failing request body is sent "after known secrets are scrubbed"; the heal path passes the provider-facing body through verbatim, unscrubbed and with no size cap (autofix.service.ts:361-389), and the scrubber is wired only into the separate diagnostic observation path (observation-payload.ts:163,165). autofix.mdx:115-117 in this repo is correct, so the fix belongs in the platform's own README · spotted during the autofix audit
- 2026-08-19 · PLATFORM, not docs · inline base64 image data is not removed from anything sent to the healing service: the redactor runs only on the throwaway copy used to pick a model (proxy.controller.ts:382) and the comment at observation-payload.ts:135-136 describes a behaviour scrubValue (:90-100) does not implement. Healer-side twin of the storage-side claim already raised as issue #69 finding 1 · spotted during the autofix audit
- 2026-08-19 · PLATFORM, not docs · Google and CodeAssist wire bodies reach the healing service labelled with the caller's API mode instead of the wire mode, because the wire-mode helper returns undefined for google while the wire-format helper does not (provider-client.ts:89-94 vs :100-102); the mislabel is codified by a test (proxy.service.spec.ts:476-484). No docs surface, but it can degrade the patches those users get · spotted during the autofix audit
- 2026-08-19 · providers/local-models.mdx:56 · "If the probe succeeds, every loaded model appears for routing" is a false universal: a non-chat filter drops any model whose id matches embed|tts|whisper|dall-e|imagen and similar for every provider (provider-model-fetcher.service.ts:405-406, applied at :1101 and again on the cached list at model-discovery.service.ts:552-553), so a pulled embedding model never appears; discovery additionally drops any model the public catalog marks tool_call:false (model-discovery.service.ts:330) · owner: providers topic · spotted during the full audit of PR #2733
- 2026-08-19 · providers/local-models.mdx:56 · "Manifest probes `http://localhost:<default-port>/v1/models`" names the wrong call for the runtime the page lists first: the Ollama tile calls ${OLLAMA_HOST}/api/tags (provider-model-fetcher.service.ts:949-953, common/constants/ollama.ts:2), and the /v1/models probe is the LM Studio / llama.cpp path. Harm is low (Ollama serves /v1/models too) · owner: providers topic · spotted during the full audit of PR #2733
- 2026-08-19 · providers/local-models.mdx:102 · the comparison table row "**Network egress** | None. Requests never leave the machine." is true of inference but reads as an instance-wide privacy statement on a page whose selling point is that nothing leaves; the instance still makes daily outbound catalog calls (models-dev-sync.service.ts:114,224; provider-param-spec.service.ts:96,123) and the telemetry report · owner: providers topic · spotted during the full audit of PR #2733
- 2026-08-19 · reference/telemetry.mdx · the corpus's only account of what a self-hosted box sends out documents one daily report and gives an opt-out, while the instance also fetches two third-party catalogs on a schedule (models-dev-sync.service.ts:114,224; provider-param-spec.service.ts:96,123), one of which has no opt-out. CONSUMED if issue #81 finding 5 option 2 ships · owner: request-logs topic (owns this page) · spotted during the full audit of PR #2733
- 2026-08-19 · reference/environment-variables.mdx:3 · the page description promises "Every environment variable Manifest reads at startup, grouped by what it controls" while at least two read-at-startup variables have no row (the parameter-catalog disable and URL overrides, provider-param-spec.service.ts:108,123); either soften the universal or state that escape hatches are deliberately out of scope. Note the 2026-08-07 verdict (decisions.md) already settles that reference rows are for variables users must discover, so the description is the half that drifts · owner: reference topic · spotted during the full audit of PR #2733
- 2026-08-19 · IMAGE · providers/subscription-based-providers.mdx after :56, reusable on providers/api-key-providers.mdx:75 · no image anywhere in the corpus shows the connection detail page carrying the **Refresh models** button, though three sentences instruct the user to click it and api-key-providers.mdx:75 gives two different locations for it; the shot needs the "N models" row, the outlined Refresh models button and the "Connected via" line (ConnectionDetail.tsx:1309-1330). PARKED not filed: PR #2733 gives the user no new reason to refresh (the stored fields it changes drive nothing user-facing), so this is outside the window's causal reach, and issue #67 finding 2 (fixed, docs PR #73 merged 2026-08-18) already owns the surrounding sentences · owner: providers topic · spotted during the full audit of PR #2733
- 2026-08-19 · PLATFORM, not docs · a request whose last user message contains the heartbeat keyword is routed to the retired first complexity slot instead of the default tier (proxy.service.ts:918,926-927 → resolve.service.ts:198-243), and a harness configured the modern way has no model there (guided setup writes the default tier only, Welcome.tsx:423-431; the Routing page hides the other tiers, Routing.tsx:138-148,567,755; the legacy auto-assigned column is always null, tier.service.ts:66,166,223), so a fully configured harness answers those pings with the no-providers message (error-codes.ts:34-37). Routing a heartbeat to the default tier when that slot is empty removes the trap behind finding 3 of issue #82 · spotted during the llm-gateway rotation
- 2026-08-19 · errors/M101.mdx · the page's remedy assumes no provider is connected; the heartbeat routing path above makes M101 reachable on a harness that has a working default tier, so its troubleshooting steps dead-end for that reader · owner: error catalog topic · spotted during the llm-gateway rotation
- 2026-08-19 · llm-gateway.mdx:60,78 · RE-PARKED by the 2026-08-19 llm-gateway rotation, not consumed: the key-provenance half of the 2026-08-18 line is being answered by issue #71 finding 2 (still open) on introduction.mdx, and duplicating it here would create two competing fixes for one gap. Re-evaluate at the next rotation once #71 has a verdict · owner: llm gateway topic
- 2026-08-19 · providers/custom-providers.mdx:65 · "Manifest can't infer pricing for unknown models. Custom-provider requests show up in the dashboard with `cost = 0`" is false in the same way observability.mdx:59-61 is (raised this run as issue finding 3): the custom-provider connect form asks for a per-model price pair ("$/M in" / "$/M out", CustomProviderForm.tsx:434-449), the cache stores an entry keyed `custom:<uuid>/<model_name>` whenever either price is filled (model-pricing-cache.service.ts:293-313), and the recorder looks up that exact raw string before canonicalisation (proxy-message-recorder.ts:1154-1164), so a priced custom model gets a real cost. This page documents the very form carrying the fields, so under the never-halfway invariant the two pages must not disagree: schedule it with the observability fix · owner: providers topic · spotted during the observability rotation
- 2026-08-19 · PLATFORM, not docs · the Overview cost chart tooltip asserts "Actual API key costs only. Subscription usage is not included." (GlobalOverview.tsx:692) while per-request subscription costs ARE in the totals: a non-zero cost with auth_type='subscription' is stored (cost-calculator.ts:87-94) and rendered as a real amount with the title "Per-request subscription cost" (message-table-cells.tsx:135-165). Only flat-fee subscriptions store the literal 0 the tooltip describes · spotted during the observability rotation
- 2026-08-19 · PLATFORM, not docs · the request drawer's zero-attempt body reads "Manifest rejected this request before contacting a provider" (RequestDrawer.tsx:413-439), but a Manifest-blocked request always writes an attempt row (proxy-message-recorder.ts:803, asserted by billing-request-limit.e2e-spec.ts:135-171), so that sentence is only ever shown to a request that is still in flight or that the caller cancelled. It tells the user the opposite of what happened · spotted during the observability rotation
- 2026-08-19 · PLATFORM, not docs · a failed threshold-alert send is never retried: both evaluators insert the dedup log row BEFORE attempting the send (notification-cron.service.ts:106,115-124,126-140; limit-check.service.ts:118,126-135), so the cron's own warning "will retry next cron run" (notification-cron.service.ts:141-143) describes a retry that cannot happen, and the Limits page's Triggered count (notification-rules.service.ts:44-55, LimitRuleTable.tsx:109) increments for a crossing that emailed nobody. Raised this run as a docs finding describing today's behaviour; writing the dedup row after a successful send would remove the trap · spotted during the observability rotation
- 2026-08-19 · PLATFORM, not docs · threshold alert emails link to the legacy /agents/<name> path (notification-cron.service.ts:136, limit-check.service.ts:147), which the app only reaches through a permanent redirect (index.tsx:112-129) · spotted during the observability rotation
- 2026-08-19 · PLATFORM, not docs · the limit-rule consumption query sums attempt costs without the negative-cost sanitiser every dashboard surface applies (notification-rules.service.ts:119-122 vs postgres-sql.ts:65-72), so a bad pricing row can push a spend limit the wrong way; the request drawer's request-level cost sum has the same gap (message-details.service.ts:177-179) · spotted during the observability rotation
- 2026-08-19 · PLATFORM, not docs · the cloud email panel prints the LOGGED-IN user's address (CloudEmailInfo.tsx:18, fed by session() at Limits.tsx:250) while the backend resolves the recipient as the tenant's explicit notification address, else the tenant OWNER's account email, else the tenant contact address (notification-log.service.ts:64-83). These coincide on a single-user workspace and diverge for any non-owner member, who is shown an address that will not receive the alert · spotted during the observability rotation
- 2026-08-19 · PLATFORM, not docs · a cloud user whose alert address is stale has no path: the account email is read-only ("Profile information is managed through your authentication provider", Account.tsx:236-253) and the explicit notification address has a backend route (POST /api/v1/notifications/notification-email) with no cloud UI, since Limits.tsx:246-252 gives cloud the read-only card only. observability.mdx:98 tells cloud users to "check it's current in your profile settings", which is possible; changing it is not · spotted during the observability rotation
- 2026-08-19 · reference/api.mdx · the tenant-scoped X-API-Key credential unlocks every analytics endpoint (api-key.guard.ts:42-77) but nothing on main mints one outside the dev seeder (database-seeder.service.ts:155-165,222-238), and the instance-wide env value authenticates without attaching a tenant so every tenant-scoped endpoint then 401s (api-key.guard.ts:79-87). Whether this is deliberate or a regression is undetermined, so it needs Seb before it becomes a finding · owner: reference topic · spotted during the observability rotation
- 2026-08-19 · llm-gateway.mdx:89 · "A model may be missing because its provider is not connected yet, or because the provider exists in your workspace but is not enabled for this harness" reads as an exhaustive troubleshooting list and omits the third cause: Manifest itself removes models the public capability catalog reports as unable to call tools (model-discovery.service.ts:329-330). Pre-existing, not created by the #2734/#2738 window: the filter has run for the providers Manifest prices from that catalog since 2026-03-29 (ca5fdd3f0) and is live today. Fix it in the same wave as issue #81 finding 1 so the two pages do not disagree · owner: llm gateway topic · spotted during the full audit of PRs #2734/#2738
- 2026-08-19 · providers/api-key-providers.mdx:75 · "A connection keeps the model list it discovered when you saved it. Nothing re-checks it on a schedule." is false: the model picker re-runs discovery on mount once per day per harness, unprompted, whenever a non-custom active provider's models_fetched_at is not today (ModelPickerModal.tsx:126-143, wasFetchedToday :74-83). This is why a user sees the model list change without touching anything, so it is the timing half of the #2734/#2738 finding on llm-gateway.mdx and of issue #81 finding 1; that finding's wording deliberately stays silent on timing to avoid making two pages contradict each other. Pre-existing frontend behaviour, untouched by the window · owner: providers topic · spotted during the full audit of PRs #2734/#2738
- 2026-08-19 · providers/api-key-providers.mdx:106-107 · "Alibaba Cloud, AWS Bedrock, and MiniMax let you pick a region when you connect them" is wrong about MiniMax on an API-KEY page: only Qwen and Bedrock carry apiKeyEndpointRegions (frontend providers.ts:119,173), the MiniMax overlay has none, and the API rejects a region for MiniMax unless authType is subscription (provider.controller.ts:124-154). The regional picker the sentence describes for MiniMax lives on the subscription side. Pre-existing, NOT caused by #2736 (Vertex correctly has no region picker, and that clearance stands) · owner: providers topic · spotted during the full audit of PR #2736
- 2026-08-19 · PLATFORM, not docs · the new Vertex provider ships a project-scoped addressing mode that no user can reach: the parser and the endpoint resolver are complete (vertex-deployment.ts:14-56, forward-endpoint-resolver.ts:112,128-131) but the connect form has no field for it (ProviderKeyForm.tsx:353-373), the API rejects a region for this provider with a message that does not even name it (provider.controller.ts:137-154), the region resolver returns null for it (provider.service.ts:647-648), and the formatter has zero production callers. The PR's own description instructs users to set the connection region to `<project>/<location>`, which is unimplementable at HEAD; the docs deliberately do not repeat it · spotted during the full audit of PR #2736
- 2026-08-19 · PLATFORM, not docs · the new Vertex provider has no icon at HEAD (its icon PR is still open), and the two fallbacks disagree: the Usage-based list renders a hash-coloured single letter (ProviderConnectionsPage.tsx:175-202) while the connect modal renders two brand-coloured initials (ProviderApiKeyTab.tsx:95-101, ProviderDetailView.tsx:231-243). Its registry subtitle is never rendered for a non-local provider (the only consumer is the local-server detail view) · spotted during the full audit of PR #2736
- 2026-08-19 · PLATFORM, not docs · the new Vertex provider offers models it cannot address: its catalogue-sourced list includes non-Google families, every one of them is forwarded to the Google publisher path in Google wire format (provider-endpoints.ts:447-459), and the per-model endpoint re-resolution has branches for eight providers and none for this one (provider-client.ts, zero matches). NOT verified live, so no error is asserted anywhere; one request against a real connection settles it · spotted during the full audit of PR #2736
- 2026-08-19 · PLATFORM, not docs · adding the new provider to the capability/pricing id map (models-dev-sync.service.ts:30) also feeds that vendor's catalogue rates into the shared pricing cache keyed by bare model id. Whether this shifts the displayed price of same-named models on OTHER connections was not traced end to end · spotted during the full audit of PR #2736
- 2026-08-19 · PLATFORM, not docs · the message-log writers run off-request with no retry and no queue: the terminal write is fired through a catch-and-warn helper after the response is sent (proxy-response-handler.ts:104-106,930; proxy-message-recorder.ts:1213-1215), so a failed write leaves the row `pending` forever. No scheduled job reconciles stuck rows (the one finalisation query is a one-time historical backfill gated on a completion marker), and the shutdown drain that could protect in-flight writes runs only in production on a real signal (shutdown.service.ts:38-49), so a self-hosted container restart has strictly less protection than a cloud deploy. The USER-visible half is already open as issue #83 finding 5 · spotted during the full audit of PR #2741
- 2026-08-19 · providers/api-key-providers.mdx:63-65 · "Manifest validates the key by fetching the provider's model list. That list is what routing sees until you refresh it. If the key is rejected, double-check the prefix below." is false for EVERY api-key provider, not only the one added this window: a non-2xx from the provider's /models call returns an empty list (provider-model-fetcher.service.ts:1097-1101), the connect handler swallows discovery errors outright (provider.controller.ts:179-183, "Discovery failure is non-fatal"), and an empty list then falls through to the public-catalogue and OpenRouter fallbacks (model-discovery.service.ts:280-297), so a revoked or wrong key connects, shows a full model list and fails only at the first routed request. Verified in the clone during this run; the claim lens and the gaps lens split on whether the new provider makes it a fresh finding, and the code settles it as pre-existing · owner: providers topic · spotted during the full audit of PRs #2736/#2741
- 2026-08-19 · providers/api-key-providers.mdx:52-67 · the "Add a provider" steps never say that a connection belongs to the workspace and is switched on for every harness the workspace already owns the moment it is saved (provider.service.ts:441, called from the new-row path; the reactivation fan-out at :209-216). The concept appears in the corpus only on llm-gateway.mdx:89 and errors/M302.mdx:26,34, phrased from the failure side, so the providers pages read as if a connection belonged to the harness you were on. Pre-existing since 5daeaa9a2 (2026-06-14) and untouched by #2736 · owner: providers topic · spotted during the full audit of PRs #2736/#2741
- 2026-08-19 · providers/api-key-providers.mdx:18 · the "Where to get a key" cell for the Google row points at ai.google.dev while the product's own "Get Google API key" link for that same provider goes to aistudio.google.com/apikey (provider-api-key-urls.ts:13), so the docs and the dashboard send a user to two different Google pages for one key. Pre-existing; it starts to matter now that a second Google-branded API-key entry sits next to it · owner: providers topic · spotted during the full audit of PRs #2736/#2741
- 2026-08-19 · providers/api-key-providers.mdx:83-103 · the key-prefix table records prefixes only, never the minimum lengths the connect form enforces, so a user whose paste is refused with a "too short" message finds nothing on the page: the Google row at :94 says "none / API key" while the form requires 30 characters (shared/providers.ts:253), and the "no fixed prefix" line at :103 covers AWS Bedrock, whose form requires 100 (shared/providers.ts:102). Pre-existing across the whole table · owner: providers topic · spotted during the full audit of PRs #2736/#2741
- 2026-08-19 · images/providers-usage-based.png · the shot (captured 2026-08-18, issue #67 finding 4) shows 23 usage-based cards from Alibaba Cloud to Z.ai with Google and Groq adjacent; PR #2736 inserts a 24th card between exactly those two. NOT raised as an IMAGE finding this run: the caption sells page shape rather than an inventory, and the new card renders as a bare single-letter tile until its icon PR lands, so a re-shoot today freezes a state that changes again. Re-check once that icon PR ships · owner: providers topic · spotted during the full audit of PRs #2736/#2741
- 2026-08-20 · references/backlog.md:70 CONSUMED and references/backlog.md:62 NARROWED · the usage-based screenshot item's stated release condition fired (platform PR #2740 ships the icon: ProviderIcon.tsx:204, public/icons/vertex.svg), so it is raised as the IMAGE finding of issue #92 on providers/api-key-providers.mdx:69-71. The card count is 24 today against the image's 23, insertion between Google and Groq. The icon-fallback disagreement between the two letter-tile renderers survives for any provider that still has no icon, and the registry-subtitle half is untouched: both stay platform items · do not re-park
- 2026-08-20 · providers/local-models.mdx + providers/custom-providers.mdx · a non-streamed Chat Completions answer carrying no output is now converted into a provider failure for every OpenAI-format target, which includes Ollama (provider-endpoints.ts:516-522) and any custom provider declared OpenAI-compatible (buildCustomEndpoint default format at provider-endpoints.ts:585-606; gate at provider-client.ts:422-425); small local models return an empty completion far more often than hosted APIs, and there is no setting to turn the check off. The gateway half is issue #92 finding 1 on llm-gateway.mdx, deliberately not duplicated here: two competing fixes for one behaviour is the trap recorded on 2026-08-19 · owner: providers topic · spotted during the full audit of PRs #2740/#2742/#2743
- 2026-08-20 · providers/subscription-based-providers.mdx:56 · "A model your plan just gained doesn't show up on its own. Click **Refresh models** on the connection to pick it up." is imprecise in the same way api-key-providers.mdx:75 is (backlog line 59): the model picker re-runs discovery on mount, once per day per harness per browser session, for every non-custom active connection whose list was not fetched today (ModelPickerModal.tsx:126-146, wasFetchedToday :74-83), so a newly granted model can appear with nobody pressing anything. Pre-existing; platform PR #2742 (one id added to one curated subscription list) only makes it the first sentence a Z.ai reader hits. Fix both loci together under the never-halfway invariant · owner: providers topic · spotted during the full audit of PRs #2740/#2742/#2743
- 2026-08-20 · autofix.mdx:174 · the repairable-statuses row reads as a free list of provider statuses eligible for a repair, but the parser drops any token outside 400-499 and, when nothing survives, silently reverts to the 400,404,422 default (autofix.service.ts:75-86); a self-hoster who adds a 5xx gets the default set back with no warning. Pre-existing, outside this window · owner: autofix topic · spotted during the full audit of PRs #2740/#2742/#2743
- 2026-08-20 · observability.mdx:27 · DRAFTING CONSTRAINT for whoever applies issue #83 finding 2 (open, same line): since platform PR #2743 there is a failure class where the provider ran the call to completion and will invoice it while Manifest records zero tokens and no cost (provider-client.ts:155-181 records the upstream 200 body against a row the failure builder writes, proxy-message-recorder.ts:301-302), so the replacement wording must not swing to "a failed attempt costs you nothing". The true statement is about what the dashboard counts, not about what the provider bills. Carried in issue #92's cleared block so it is not lost · owner: observability topic · spotted during the full audit of PRs #2740/#2742/#2743
- 2026-08-20 · PLATFORM, not docs · a Chat Completions 200 carrying no output reaches the caller as HTTP 502 attributed to the provider: the envelope sets source 'provider' (proxy-response-handler.ts:305-319) and the generic message "Upstream provider returned bad gateway" (proxy-error-sanitizer.ts:12,191-201), because 5xx bodies stay generic in production and the structured-error parser refuses status >= 500 (:88-92). The distinctive code and its explanatory message reach the dashboard attempt only, never the client, so a caller cannot tell this apart from a real upstream 502 · spotted during the full audit of PRs #2740/#2742/#2743
- 2026-08-20 · PLATFORM, not docs · the empty-completion check discards the upstream's reported usage: the 200 body is recorded verbatim against the attempt (provider-client.ts:168) but the row is written by a failure builder that supplies no tokens and no cost (proxy-message-recorder.ts:301-302; cost_usd written only at :1105 and :1203), so a provider call that ran to completion contributes zero to spend · spotted during the full audit of PRs #2740/#2742/#2743
- 2026-08-20 · PLATFORM, not docs · SCOPE NOTE so nobody re-derives it as a bug: the empty-completion conversion is gated to non-streamed OpenAI-format chat completions (provider-client.ts:422-425), so a well-formed SSE stream carrying no content deltas is still delivered as a 200 (the stream warm-up only checks that first bytes arrive, proxy.service.ts:541-545), and callers on /v1/messages or /v1/responses are never covered even when their request is translated down to chat completions upstream (proxy.controller.ts:205-226) · spotted during the full audit of PRs #2740/#2742/#2743
- 2026-08-20 · PLATFORM, not docs · glm-5.3 was added to the curated Z.ai plan list (platform PR #2742, shared/subscription/configs.ts:222) without ever being confirmed against a live Z.ai request: the PR's own smoke test hit HTTP 429 "Insufficient balance or no resource package", and the id has no other occurrence in the repo. If Z.ai does not serve it, a user who pins it gets a provider error at chat time. Raised as question 1 of issue #92, not as a finding, since no docs page names it · spotted during the full audit of PRs #2740/#2742/#2743
