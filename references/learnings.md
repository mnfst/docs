# Learnings — append-only memory of the docs-sync harness

- 2026-08-06 · harness created; state seeded at mnfst/manifest PR #2691 (last merged before the harness existed).
- 2026-08-06 · run 1 (message recording): the auditor inverted the REQUEST_RECORDING_STORAGE auto rule (claimed filesystem fallback on partial S3; code returns a null backend). The verifier's code re-read killed it before publication. Lesson: auditors must quote the exact decision line, not summarize control flow.
- 2026-08-06 · run 1: the platform-engineer's report propagated the same misreading — verification must re-read code even when the report cites file:line.
- 2026-08-06 · run 1: never name internal tools in the issue itself, even in the "clean" summary (public repo).
- 2026-08-06 · run 1: keep the "Questions for Seb" section OUT of the public issue; deliver it privately.
- 2026-08-06 · run 1 verdicts: findings must be written in PLAIN language — no UI-component jargon (no "drawer", "Frame", CSS/file names in the finding body; code refs stay in the evidence lines only). Seb could not understand the IMAGE findings as written. An IMAGE finding is one simple sentence: where in the doc, what photo to take.
- 2026-08-06 · editorial: when history is convoluted (feature added/removed/re-added), docs say the simple useful thing, not the history. Recorded in AUDIENCE.md.
- 2026-08-06 · terminology: "logs", "request", "attempt"; the platform will rename the /messages URL later.
- 2026-08-06 · full audit (cron, autonomous, no human): 0 PRs merged on mnfst/manifest since baseline #2691 (max merged PR = #2691). Nothing to compare → no findings → no issue (silence is a valid outcome). lastAuditedPr left at 2691; lastRunAt bumped as a run heartbeat.
- 2026-08-07 · full audit (cron, autonomous, no human): still 0 PRs merged since baseline #2691 (origin/main @ 47d35fa, 2026-08-05, contains merge of #2691; max merged PR = #2691). Nothing new → no findings → no issue. lastAuditedPr unchanged; lastRunAt bumped as heartbeat.
- 2026-08-07 · issue format hardened: human-first (one plain sentence per finding, options one line each, ALL evidence collapsed in a details block). Seb could not tell two images were expected of him.
- 2026-08-07 · questions to Seb must be complete and case-specific (state: existing agents vs newly created, cloud vs self-hosted, pre/post release). "Is recording live on Cloud?" was unanswerable as asked.
- 2026-08-07 · Seb's answer on cloud defaults: pre-existing agents have logs OFF; at agent creation the Connect Harness modal pre-checks Autofix and Logs (suggested defaults, like consent checkboxes) — the user confirms. Transition info that only concerns pre-release users is not worth documenting.
- 2026-08-07 · internal tool names are never written anywhere in this public repo (including harness reference files); the engineer derives the list from code at each run.
- 2026-08-07 · full audit (cron, autonomous, no human): first run WITH new PRs since baseline. 4 merged PRs #2693/2694/2696/2697 (#2695 not merged). 10 findings filed as issue #48; lastAuditedPr advanced 2691→2697. Categories: WRONG ×4, MISSING-CASE ×5, INTERNAL-LEAK ×1. Autofix-spelling and removed-Gemini-model leads were dropped by the auditor (docs already clean) — a lead from a PR title is not a finding until the docs actually contradict.
- 2026-08-07 · a single "infra plumbing" PR (#2693, durable recordings across deploy templates) produced 8 of 10 findings across 7 deploy pages + request-logs. Lesson: template/IaC PRs that touch deploy/* fan out widely into the deploy guides; audit every touched deploy target, not just the feature's own page. Each target falls in a different bucket (auto-provision vs mount-volume vs require-external-S3) and each needs its own case check.
- 2026-08-07 · INTERNAL-LEAK finding on a public issue is doubly constrained: the leak is IN the docs, so the finding must point by file:line and describe the tool generically (the verifier grep-gates the draft for the codename/hostname). When the leak is the VARIABLE NAME itself (WINGMAN_CORS_ORIGINS), an interim doc reword cannot fully hide it — the real fix is a platform rename; offer both as options, don't pretend docs alone close it.
- 2026-08-07 · Railway recurs as an "unverified" edge: README claims durable S3 storage, but railway.toml (external template) has none in-repo. Kept as an OPEN QUESTION in decisions.md, out of the public issue (questions-to-Seb stay private). Resolve by confirming the actual Railway template before touching deploy/railway.mdx or request-logs.mdx line 118.
- 2026-08-07 · run 2 (introduction): verifier killed two false evidence statements ("renamed everywhere but here" — 5 leftovers existed; "three times" — it was two). Lesson: universal quantifiers (everywhere, always, all) in evidence must be grep-verified before writing.
- 2026-08-07 · run 2: the human-first issue format worked — findings are one plain sentence + collapsed proofs.
- 2026-08-07 · run 2: issue #49 was first published in FRENCH — forbidden. Everything on GitHub is English, always; French stays in the private conversation. Rule added to ISSUE-FORMAT.md; the verifier must fail any non-English draft.
- 2026-08-07 · issue rewrite #2 after Seb's feedback: options were fake (restating the problem or citing context as if it were a choice) and recommendations repeated the title. New rules in ISSUE-FORMAT: single obvious fix = one "Fix:" line; options must be genuinely different outcomes, one line each; recommendation marked once; issue-writer voice rules apply.
- 2026-08-07 · Seb's idea adopted: every finding carries a visible "What actually happens" block — the user journey in 2-6 user-language steps, derived from code. It lets Seb verify the behavior before judging options and doubles as the spec for the fix. Verifier walks the code to confirm each step.
- 2026-08-07 · Seb challenged finding 3 ("are you sure?"). Proof delivered by LIVE reproduction against Cloud (fake model → M302 verbatim; auto → routed). Rule: when a behavior finding is reproducible with one cheap request, reproduce it and put the transcript in the evidence — reproduction beats code citation.
- 2026-08-07 · finding 5 was unreadable because the auditor hid the instance-mode variable's name, over-applying the internal-names ban. Rule scoped: the ban covers names with no user-facing existence; anything already printed in public docs or product output may be named in findings when needed for readability. Also: when evidence makes one option clearly right, write a single deterministic Fix, not options — ten agents must produce the same issue.
- 2026-08-07 · finding 5 fix over-documented the instance-mode env var: the variable is user-facing only for the value `selfhosted`; `cloud` (and the mode concept itself) is internal. Lesson: documenting a variable means documenting its USER-FACING surface, never the full internal value set.
- 2026-08-07 · finding 5 finally REJECTED by Seb after two fix attempts. Lessons: (a) a Default column shows a value, never a described behavior ("auto-detect" was invented); (b) an escape-hatch variable fully explained where users meet it (guides, error message) needs NO reference row — in-place completeness beats reference completeness. Reference tables are for variables users must discover, not ones the product hands them.
- 2026-08-07 · instance-mode saga, final lesson: I oscillated (documented too much → removed entirely → restored) instead of holding the invariant "never halfway: used in a guide = defined in the reference". The halfway state I created by removing the row while guides kept the variable was exactly the original finding. Rule added to AUDIENCE.md; when Seb expresses doubt, re-derive from invariants, do not flip.
- 2026-08-08 · scoped run (autofix, cron, autonomous, no human): CLEAN, 0 findings, no issue. A dense, current page (every status code, env default, guardrail constant, and data-flow claim correct) is a valid silence — do not manufacture a finding to have output. Ran the full loop anyway: platform-engineer state-of-truth → docs-auditor cross-check → issue-verifier as a FRESH-EYES ADVERSARY tasked to break the zero-findings conclusion. Repurposing the verifier to attack a silence (not just to kill a draft) is the right guard against the scoped audit's worst failure mode, a false all-clear.
- 2026-08-08 · autofix run, two near-findings that are NOT findings, recorded so they are not re-litigated: (a) "keeps its state" for pre-existing agents — the nullable-reset migration (1799000300000) resets explicit-false rows to NULL, so the internal mechanism differs, but the user-visible effect (cloud=on, self-hosted=enable-yourself) holds via resolveEnabled → NULL maps to !isSelfHosted(); user-visible truth is the bar, not the mechanism. (b) Anthropic subscription-exhaustion HTTP 400 is skipped by the heal path (autofix.service.ts:258-278) even though 400 is repairable; this aligns with the page's own concept ("errors where the provider is telling you the request was wrong"), so enumerating it would add provider-specific noise, not fix a contradiction. Also: the observe feed excludes 401/402/403/429; the "When it runs" prose lists "401, 403, 429" as skipped (402 also not healed) — illustrative, not exhaustive, not worth a finding.
- 2026-08-08 · internal names surfaced by the engineer this run (kept out of every published surface, descriptors only): the healing backend's codename, an internal historical-scrape agent, an internal analytics-tier name, and the healer's infra hostname. All absent from the two doc pages (grep-gated). The docs' user-facing descriptor "the healing service" is the correct substitute.
- 2026-08-08 · full-mode run over a 1-PR window (#2701, Bedrock GPT-5 responses-path fix): CLEAN, valid silence. An internal reverse-proxy buildPath change (upstream endpoint chosen server-side from the model id) has NO docs surface — the documented contract is the client-facing /v1/responses endpoint, not the internal Bedrock upstream /openai/v1/responses. Distinguishing the two same-named paths was the crux: reference/api.mdx:33's /v1/responses is where users POST; the PR only changed what Manifest calls downstream. Ran the full loop anyway (truth → auditor → verifier-as-adversary); repurposing the verifier to attack the all-clear is the standing guard for clean runs. Also confirmed the discipline of NOT publishing regex-fixture model codenames (gpt-5.6-sol/terra/luna, gpt-5.99-future) as a supported-model list — they are test inputs, not a catalog.
- 2026-08-09 · scoped run (llm gateway, cron rotation, autonomous, no human): 5 findings on a page that READ as dense/authoritative but carried three hard falsehoods about status codes. Lesson: exact HTTP-status claims are the highest-yield audit target on gateway pages — the doc confidently stated three wrong ones (424 on exhaustion, 429 for M200 blocks, /v1/messages force-routing). All three were falsifiable against a single predicate/constant in code (shouldTriggerFallback `status >= 400`; buildFriendlyResponse `status: 200`; the explicit-route branch running before any apiMode check). Verify every literal status code and endpoint claim against the exact line, never against the page's own internal consistency.
- 2026-08-09 · the "424 fallback exhausted" story was self-reinforcing across the page (frontmatter keyword :6, prose :97, prose :138) — a wrong concept propagates into keywords and multiple sentences. When killing a false concept, grep the whole page (incl. frontmatter) for every echo, or the fix leaves orphans. Filed as ONE finding with all three loci, not three.
- 2026-08-09 · verifier caught a real self-contradiction the auditor+writer both missed: finding said exhaustion keeps "the last provider's" status; code preserves the PRIMARY/original model's status (proxy.service.ts:1258 `status: primaryStatus`), and the finding's own evidence block already said "primary". Lesson: primary-vs-last (and primary-vs-terminal) is a precise distinction on fallback chains; the visible journey step must match the evidence line verbatim. Fresh-eyes re-read of BOTH sides catches drift the drafting agents introduce.
- 2026-08-09 · "up to 5 fallbacks" is NOT gateway-enforced (no numeric cap in proxy code) but the frontend copy (RoutingDefaultTierSection.tsx:164) says "up to 5" — so the doc matches the product the user sees. Lesson: a UI-copy convention is the right bar for a user-facing count, not a backend constant; don't raise a finding when code lacks a cap the UI advertises. (welcome-helpers MAX_FALLBACKS=2 is only the auto-seeded clean-agent default, unrelated.)
- 2026-08-10 · scoped run (observability, cron rotation, autonomous, no human): 4 findings on a dense, mostly-accurate page. Highest-yield target was a UI-existence claim, not a status code: the doc's self-hosted flow "configure a provider on the Limits page" points at a form that is UNMOUNTED dead code (EmailProviderSetup: zero JSX usages; last commit is a revert of #821). Lesson: for any "do X in the UI" instruction, grep the frontend for an actual JSX mount of that component, not just its existence in the tree — a component can exist, be fully built, and never be rendered. Backend endpoints + DB table for the same feature still existed (email_provider_configs, /notifications/email-provider), which is a classic trap: the API path is live but the user has no way to reach it.
- 2026-08-10 · observability run, the "user-visible truth is the bar" rule cut both ways. KILLED near-finding: docs say local/custom models "record cost = 0"; code returns NULL (computeTokenCost) and the drawer hides the Cost row, but aggregates coerce NULL→0, so the user sees $0 in totals — spirit-correct, left alone. This is the same principle as the autofix "keeps its state" call: judge what the user sees, not the mechanism. Do not manufacture a WRONG finding from a mechanism/display mismatch that produces the promised user-visible effect.
- 2026-08-10 · observability run, a genuine choice that looks like a bug: the limit-rule "Action" field advertises "Email you, block further requests, or both" but the UI only offers email-always + an optional block toggle (no silent-block). Raised as low-sev WRONG with an honest leave-as-is option 2, because every combination the doc names exists in some form and only the silent-block corner is unreachable. Severity tracks how many users hit the missing corner.
- 2026-08-10 · verifier caught a real evidence overstatement the auditor+writer let through: the draft said EmailProviderSetup AND EmailProviderModal have "zero JSX usages (grep clean)", but the Modal IS used by the Setup component (EmailProviderSetup.tsx:48). The conclusion (parent never mounted → both unreachable) held, but the literal "grep clean" was false and would have made Seb distrust the finding on his own grep. Lesson: never write "zero usages / grep clean" about a component that has an internal caller; state the reachability chain (parent unmounted ⇒ child unreachable) instead of a blanket zero-usage claim.
- 2026-08-09 · #2639 (forward uncatalogued models to providers) turns a former absolute ("unlisted model → M302") into a conditional (M302 only when no connected route). Raised as low-severity MISSING-CASE with an honest "leave as intentional simplification" option 1, because the harm is success-in-the-user's-favor, not a broken request. A false universal is still a finding, but severity tracks user harm and the option set must include leaving it.
- 2026-08-10 · full-mode run over a 1-PR window (#2703, gateway API contract tests): CLEAN, valid silence. A test/CI-only PR that adds HTTP contract tests changes no product behavior — the promises it codifies (route availability for /v1/chat/completions + /v1/responses, 401 auth_error for unauthenticated non-streaming calls) predate it and are already documented correctly. Lesson: a contract-test PR is a truth SOURCE, not a truth CHANGE; audit whether the codified contract matches the docs, but a match = silence. The one nuance worth verifying was the auth-401 client split: the {type:auth_error,code:manifest_auth} 401 envelope only reaches NON-streaming callers; a stream:true / Accept:text/event-stream request gets a friendly HTTP 200 instead (proxy-exception.filter.ts isChatRenderingClient :38-44), which the contract test sidesteps by posting plain JSON.
- 2026-08-10 · same false concept can live on MULTIPLE pages: the "424 on fallback exhaustion" and "M200 block → 429 envelope" falsehoods raised for llm-gateway.mdx in #53 (findings 2, 3) are echoed on reference/api.mdx (frontmatter :6, envelope example :194-195, table :204, prose :208). Cross-page echo is the page-level analogue of the 2026-08-09 within-page-echo lesson (grep the whole page for every locus of a false concept). Parked in backlog for the reference topic (lastAuditedAt null) rather than raised out-of-scope in a gateway-contract full run — scope is a hard boundary, and the owning topic re-raising a known-concept echo on its own page is not a rejected-finding re-raise (different page, #53 verdicts still pending).
- 2026-08-10 · full-mode run over a 1-PR window (#2705, extends gateway contract tests to /v1/models + /v1/messages + non-empty auth message): CLEAN, valid silence — the SECOND consecutive contract-test-only PR (after #2703). Confirms the #2703 lesson: a contract-test PR is a truth SOURCE, not a truth CHANGE; audit whether the codified contract matches the docs, and a match = silence. New wrinkle this run: the contract now pins the 401 auth envelope on ALL FOUR routes (guard is controller-wide, @Controller('v1') @UseGuards(AgentKeyAuthGuard)), while errors.mdx:21 / errors/M001.mdx name only "/v1/chat/completions". The tempting near-finding (docs under-state the scope) is NOT a finding: a MISSING-CASE requires the platform to DIVERGE on the unstated case, and here the guard behaves identically on all four routes — same UnauthorizedException → same M001-M005 → same 401, endpoint-independent fix. Representative example-naming with no divergent behavior and no misled user is cosmetic, not a defect. Rule: "the doc names one route but the behavior is universal" is only a finding when a route behaves DIFFERENTLY; identical behavior across the unnamed routes = silence.
- 2026-08-11 · scoped run (providers, cron rotation, autonomous, no human): 6 findings across 4 pages (1 MISSING-CASE, 3 WRONG, 2 IMAGE). Flagship was a deployment-gate case, not a status code: local runtimes (Ollama/LM Studio/llama.cpp) are self-hosted-ONLY and blocked on Cloud at three layers (connect/discovery/proxy → CLOUD_LOCAL_PROVIDER_MESSAGE + M303, PR #2535), yet local-models.mdx presents them as universally available and never names the Cloud restriction or its documented workaround (custom provider on a public URL/tunnel — the error message itself states the escape hatch). Lesson: for any provider/feature that is `localOnly`/`isSelfHosted()`-gated, the deployment dimension of CASE-MATRIX is the first thing to check — a page written for the self-hosted happy path silently 404s the Cloud user.
- 2026-08-11 · providers run, highest-yield WRONG targets were exact upstream endpoints and a security predicate, both falsifiable against one constant: (a) GLM tab named only the CN host (open.bigmodel.cn) when the default region is global (api.z.ai) — the sibling MiniMax tab already showed both, so within-page inconsistency was the tell; (b) the custom-provider SSRF note invented "self-hosted instance on the same network" when the code is a flat `allowPrivate: isSelfHosted()` with no topology concept, and cloud-metadata is blocked in BOTH modes. Verify every printed endpoint host and every security-scope clause against the exact region-map / predicate line, never against the doc's own prose.
- 2026-08-11 · providers run, the "user-visible truth is the bar" rule held once more as a KILL: local-models.mdx:98 and custom-providers.mdx:64 say cost "= 0" while the mechanism is NULL (computeTokenCost) — already decided spirit-correct on 2026-08-10 (aggregates coerce NULL→0, user sees $0). Not re-raised. A near-finding that a prior run explicitly settled must be recognized and dropped by the auditor, not re-litigated; the fresh-eyes verifier confirmed it was not resurrected.
- 2026-08-11 · providers run, IMAGE-format discipline enforced by the verifier: the first IMAGE draft bundled TWO screenshots in one finding ("tile grid AND the panel with the key field") and carried a visible rationale sentence. ISSUE-FORMAT: an IMAGE finding is ONE plain sentence (where + what photo) + a details block, no rationale in the visible body, and the bundled panel shot overlapped a separate two-tab-panel IMAGE finding. Fix = split to the single genuinely-missing visual (the Routing tile grid), move the rationale into <details>. Lesson: one image per IMAGE finding; if two candidate shots overlap, keep the unique one and push justification into evidence.
- 2026-08-11 · providers run, catalog-completeness is a real audit axis but usually a CLEAN result: cross-checked all 33 registry providers against the api-key table, subscription table, prefix table, and local-runtime table — no mis-categorization. Two traps that are NOT findings: Command Code carries a key prefix (`user_`) yet is subscription-ONLY (a declared prefix does not imply an API Keys tab), and Ollama Cloud is a SUBSCRIPTION provider, not a local runtime (name collision with Ollama). Confirm auth capability from the subscriptionOnly/localOnly overlay, not from the presence of a keyPrefix.
- 2026-08-10 · #2705 run, backlog discipline held under the fresh-eyes verifier: the verifier surfaced a genuinely NEW out-of-scope observation (reference/headers.mdx:17 "anthropic-version Required for /v1/messages" — code only shows Manifest injecting it OUTBOUND, no inbound client enforcement found). Correct disposition = append one backlog line for the owning topic (reference), NOT raise it in a gateway-contract full run and NOT silently drop it. A clean run's verifier can still produce backlog value without producing a finding; the scope boundary routes it, it doesn't discard it.
- 2026-08-12 · scoped run (self-hosting with docker, cron rotation, autonomous, no human): 1 finding on a dense, mostly-accurate CLI page. Highest-yield target on a self-hosting page is a HOW-TO instruction that references a literal file line, not a status code: the "Exposing on the LAN" step told users to edit a ports string ("127.0.0.1:2099:2099") that the shipped compose no longer contains — the line is env-templated ("${HOST_BIND_ADDRESS:-127.0.0.1}:${HOST_PORT:-${PORT:-2099}}:${PORT:-2099}") and the real knob moved to HOST_BIND_ADDRESS=0.0.0.0 in .env. Lesson: for any doc step that says "find and change line X in a config file the user downloads", grep the CURRENT shipped file for that literal — install/deploy config templates drift and the doc's copy-paste target silently rots.
- 2026-08-12 · self-hosting run, the second-order harm was the real severity driver: a hand-edit to a downloaded compose file is WIPED by the very upgrade path the same page recommends (install.sh --upgrade atomically replaces docker-compose.yml, leaves .env untouched). Lesson: when a page documents BOTH "hand-edit this managed file" and "upgrade with our installer", check whether the installer regenerates that file — if it does, every documented hand-edit is a time-bomb and the fix must move the setting to the persistent surface (.env / override), not just correct the edit target. The upgrade-survival angle turned a "can't find the line" annoyance into a "silently loses LAN access" data-of-behavior loss.
- 2026-08-12 · self-hosting run, version-threshold discipline: the tempting near-finding "the recordings-volume upgrade note is stale" was KILLED by checking release TAGS, not the feature commit. The commit that added the volume (PR #2565) carried package.json 6.17.1, but the change only appeared in the released tag manifest@6.18.0 (git show manifest@6.17.1:docker-compose.yml had 0 hits; 6.18.0 had them) — so the doc's "before v6.18.0" threshold was correct. Lesson: to verify an "as of vX.Y.Z" claim, diff the shipped artifact at the ADJACENT release tags, never the feature commit's in-tree package.json (which lags the changeset-driven version bump). Same tag-check confirmed HOST_BIND_ADDRESS is genuinely a 6.18.0 addition, framing the LAN finding as current drift.
- 2026-08-12 · self-hosting run, verifier caught a visible-block leak the auditor+writer let stand: the "What actually happens" steps quoted the raw compose template string and named docker-compose.yml/install.sh/.env inline. ISSUE-FORMAT rule 3 reserves file names, code words, and raw config for the folded evidence block; the visible journey is pure user language ("open the compose file as the page instructs", "the exact text is not there"). Fresh eyes re-reading for FORMAT (not just facts) is what keeps a factually-bulletproof finding human-readable. The finding facts were 100% TRUE on independent re-verification; the only edits were altitude/precision.
- 2026-08-13 · scoped run (deploy platforms, cron rotation, autonomous, no human): 3 findings across a 10-page topic that had been PARTIALLY audited once before (issue #48 on 2026-08-07, still all `raised`). The whole run turned on scoping against that prior partial audit: #48 hit 7 of the 10 deploy pages via the recordings-persistence PR (#2693), and since NO deploy PR merged after #2693 the platform is unchanged on those 7 → they are off-limits (re-raising an unanswered still-open finding is noise the verifier kills). The yield lived exactly in the gaps #48 left: the two pages it never touched (coolify, gcp) each carry the SAME recordings-persistence gap #48 raised for their siblings, just never filed. Lesson: when a topic was previously audited by a NARROW trigger (one feature PR fanning into some of its pages), the next full-topic pass should enumerate which pages that trigger MISSED and audit those first — the same defect class usually sits there unraised.
- 2026-08-13 · deploy run, highest-yield finding was an intra-topic CONSISTENCY split, not a fresh falsehood: 7 of 10 deploy pages said passive "the first account becomes the admin" while 3 siblings (heroku/easypanel/fly) + self-hosted.mdx already carried the corrected setup-screen actor wording (the exact fix accepted in #49 finding 2). When the same user step is described two ways inside one topic, the already-corrected sibling IS the spec — the finding writes itself and the fix is copy-the-shipped-wording. Also a re-raise-boundary lesson: extending an ACCEPTED decision (#49, self-hosted.mdx) to a DIFFERENT page set (the deploy pages) is legitimate and NOT a rejected-finding re-raise — the verifier confirmed the reasoning; the ban is only on re-raising REJECTED findings or duplicating a still-open one on the SAME page.
- 2026-08-13 · deploy run, the platform's OWN deploy tutorial (deploy/gcp/TUTORIAL.md) documented the recordings bucket (existence, mount, teardown) fully, while the published docs page (gcp.mdx) omitted all of it. Lesson: for deploy topics, diff the docs page against the in-repo TUTORIAL.md/README for that target, not only against the IaC template — a well-written in-repo tutorial is both the truth source AND proof the omission is a docs-page gap (someone already wrote the user-facing words, they just didn't reach the site). The gap is highest-confidence when the repo tutorial and the template agree and only the site page is silent.
- 2026-08-13 · deploy run, the ISSUE-FORMAT "no code in the visible block" gate is now a RECURRING verifier block on infra/deploy findings (self-hosting #56, then this run): mount paths (/data/request-recordings), Terraform output names (recording_bucket), CLI commands (deploystack uninstall), and URL fragments (/setup) all read as code and all belong in the folded evidence, not the visible journey. Pre-empt it in drafting: the visible "What actually happens" for a deploy finding should survive being read aloud to a non-engineer — "the deploy prints the bucket's name at the end", "a second storage volume", "a setup screen" — every literal identifier lives only in <details>. Facts were 100% TRUE on re-verification both times; the block was pure altitude.
- 2026-08-14 · scoped run (error catalog M-codes, cron rotation, autonomous, no human): 3 findings, and 2 of the 3 turned on a delivery-mechanism fact the docs' status column flattened away. Manifest has TWO ways an error reaches the caller: a friendly HTTP-200 assistant-message stub (buildFriendlyResponse, always status:200, for M100/M101/M102/M200/M302 to ALL clients) vs a thrown status with a chat-vs-tool split (M001-M005/M204/M300/M303/M500: chat→200 stub, tool→real status) vs 429-passthrough-for-everyone (M201-M203). Lesson: for an error-catalog page, "Returns HTTP N" is never verifiable from the code that RAISES the error (which carries an internal status like CREDENTIAL_FAILURE_HTTP_STATUS=401 that the client never sees) — you must trace to the code that WRITES the response (buildFriendlyResponse / proxy-exception.filter / handleProxyError). M200's doc claim "HTTP 429" was the sharpest kind of wrong: not a chat/tool nuance but categorically false (200 for everyone, never 429), because the raise-site reason 'limit_exceeded' looks 429-shaped but the emit-site is the 200 stub.
- 2026-08-14 · error-catalog run, the highest-value finding was a NON-ACTIONABLE remedy, not a false fact: M201/M202/M203 told self-hosted users to "raise RATE_MAX_REQUESTS in proxy-rate-limiter.ts", and the value IS 200 as documented — the defect is that it is a hardcoded compile-time constant with no env/config/runtime override, so the fix instruction points at a knob that cannot be turned in a shipped Docker image. Lesson: audit the FIX steps of a troubleshooting page as hard as the description — "here is how to change X" is a falsifiable claim (does an override path exist?) and fails independently of whether the stated value/status is right. Verify by grepping the constant for process.env/ConfigService AND ruling out adjacent-but-separate mechanisms: the verifier here nearly let a THROTTLE_LIMIT/THROTTLE_TTL env pair look like the override until it found @SkipThrottle() on the proxy controller (that global ThrottlerModule never touches the proxy rate limiter). A plausible env var elsewhere in the codebase is not the override until you prove it reaches THIS code path.
- 2026-08-14 · error-catalog run, check-coherence.mjs's NOT_ENV_VARS exemption list doubled as a pre-seeded finding: the three rate constants were listed there with the comment "code constant; docs wrongly imply configurability — backlogged for the error-catalog audit", mirroring backlog.md line 7. Lesson: the machine-invariant script's exemption comments are a second backlog — when a token is exempted with a "docs wrongly imply X" rationale rather than a clean "not an env var" one, that rationale is an unfiled finding waiting for its topic's audit. Worth grepping the script's exemption comments at the start of any scoped run whose pages the exemptions touch.
- 2026-08-14 · error-catalog run, the visible-block "no code" gate blocked AGAIN (4th topic running: gateway-adjacent, self-hosting #56, deploy #57, now this) — this time a source-file name in a finding TITLE ("...by editing proxy-rate-limiter.ts") and the fix-wording listing the three raw constant names in the visible area. The recurring nature says: draft finding TITLES and OPTION lines in user language from the start ("editing a source file", "remove the constant names, listed in the evidence") and keep every literal symbol/path in <details> — for error-catalog pages the allowed visible tokens are exactly the user-facing surface (M-codes, the 🦚 prefix text, HTTP statuses, and the tech-writer's replacement doc wording), nothing from the platform's internal source. Facts were 100% TRUE on re-verification both blocks; edits were pure altitude, zero factual change.
- 2026-08-15 · full run (cron, autonomous, no human): the scoping rule `number > lastAuditedPr` SILENTLY DROPPED two merged PRs. #2700 and #2702 carry numbers BELOW the last audited PR (#2712) but were merged after it (2026-08-13/14, long-lived branches merged out of order), so a pure number filter would have skipped them forever — and one of them (#2702) shipped an entire new provider. Lesson: the watermark is a MERGE TIME, not a PR number. Always cross-check `gh pr list --json number,mergedAt` and keep anything merged after the last audited PR's mergedAt, whatever its number. This run added `lastAuditedMergedAt` to scripts/state.json for that purpose; ~/docs-harness/run.sh still compares numbers only (line 25), so its watch mode has the same blind spot and is worth fixing with Seb.
- 2026-08-15 · full run, the biggest finding had NOTHING to do with the audited PRs: tracing "how does a user connect the new Meta provider" through the frontend exposed that provider connection left the Routing page in PR #2207 (2026-06-11, two months earlier) and SEVEN docs pages still send users there. Lesson: a full audit is a state comparison, not a diff reading. Use the window's PRs as an ENTRY POINT into a user journey, then verify the whole journey against current main — the drift that hurts users most is usually older than the window, which is exactly why no diff-driven run ever caught it. Two prior scoped runs (#55 providers, #58 error catalog) read these same pages and missed it because they checked the pages' CLAIMS against feature code, never walked the navigation path itself.
- 2026-08-15 · full run, the verifier killed a falsehood the CORRECTION round introduced. Round 1 flagged that step 2 of subscription-based-providers.mdx ("pick the Subscription tab") was a no-op; the fix I drafted said "the panel opens on Subscription, providers with both credential types show both tabs" — but round 2 found the tabs were removed entirely (ProviderSelectContent.tsx:304-305), so the draft would have written a NEW false claim into the docs. Lesson: suggested-wording drafts need the same both-sides evidence as findings. When a correction round says "X is now redundant", verify whether X still EXISTS before writing wording that assumes it does; "redundant" and "removed" produce completely different sentences. Corollary: run the verifier again after applying corrections — round 2 and round 3 each caught a defect that round 1 could not have seen.
- 2026-08-15 · full run, two anti-hallucination catches came from grepping for READS, not declarations. The draft cited `initialTab={copy().authType}` as proof the tab is preselected: the prop is passed and feeds a signal (ProviderSelectContent.tsx:48) that nothing ever reads — the real mechanism is a deep link (setDeepLink → selectedAuthType → isSubMode). Same shape on the Routing page: showProviderModal/providerDeepLink/customProviderPrefill are still passed to RoutingModals and appear only in its Props interface. Lesson: a prop or a signal being SET proves nothing about behavior. Before citing one as evidence, grep for its read site; dead props survive refactors for months and read exactly like live wiring.
- 2026-08-15 · full run, the IMAGE carve-out in ISSUE-FORMAT is narrower than it looks: it drops the options and the rationale paragraph, NOT the `Page:` line. Round 3 blocked an IMAGE finding whose entire visible area was its title, leaving Seb unable to see which page the shot lands on without opening the evidence. Every finding, IMAGE included, keeps the visible Page line plus one sentence saying where the image goes.
- 2026-08-16 · scoped run (reference: api/headers/env-vars/glossary, cron rotation, autonomous, no human): 10 findings, the largest yield of any run so far, on the topic that had NEVER been audited (lastAuditedAt null). The reason is structural: reference pages accumulate silently. Feature pages get re-read whenever their feature ships a PR, but a status table, a header value list or a glossary line is only ever checked by an audit that targets it — the 424 falsehood had been wrong for 4.5 months (PR #1407, 2026-03-30) and had been SPOTTED THREE TIMES (backlog lines from the #2703 and #2705 full runs) without anyone being allowed to raise it, because the owning topic never came up in rotation. Lesson: a never-audited topic in topics.json is a debt marker, not a gap in coverage; when the rotation finally reaches one, expect the backlog it owns to be the floor of the finding count, not the ceiling.
- 2026-08-16 · reference run, the sharpest NEW finding was a knob that exists, is documented, is spelled correctly, and does nothing: api.mdx tells self-hosted users to raise the gateway rate limit with two env vars that the proxy controller is excluded from by one decorator (@SkipThrottle()). This is the same defect the #58 error-catalog run found from the other side (M201-M203 telling users to edit a source constant), and the verifier had ALREADY nearly missed it there. Lesson: when a run establishes that a subsystem has no configuration surface, immediately grep the whole docs corpus for pages CLAIMING to configure it — the falsehood is almost never confined to the page where you found it. Here it produced finding 1 plus a parked self-hosted.mdx line, from one code fact.
- 2026-08-16 · reference run, the verifier caught two factual errors in VISIBLE text that platform-engineer, docs-auditor and tech-writer had all passed: finding 9's title claimed failed requests carry no routing headers (false — provider-error and exhausted responses do; only PRE-ROUTING rejections do not), and finding 3 claimed the only 429s come from Manifest's caps (false — an upstream provider 429 is forwarded verbatim). Both were OVER-GENERALIZATIONS of a true evidence bullet: the folded evidence said the right thing and the visible sentence widened it. Lesson: the drafting step that turns evidence into a one-line title is where absolutes get invented ("at all", "the only", "never", "always"). Audit every universal quantifier in a visible block against its own evidence bullet before filing — and note that finding 9's wrong version CONTRADICTED finding 4 step 5 inside the same issue, so cross-finding consistency is a cheap detector for exactly this failure.
- 2026-08-16 · reference run, a conditional backlog line was correctly CLEARED rather than raised, and the reasoning is worth reusing: the 2026-08-12 line said the env reference must gain host-mapping rows IF the #56 self-hosted fix introduced those vars into prose. #56 is still unmerged, and a grep of the *.mdx corpus found zero mentions — so nothing is used-but-undefined and the never-halfway invariant HOLDS. Retiring the line loses no tripwire because check-coherence.mjs machine-enforces that exact invariant. Lesson: a backlog line with an IF is a claim about a future state; consume it by evaluating the condition against today's corpus, not by raising it defensively. The page still had a real defect (the bind-interface row's advice is a no-op on Docker) — raised on its own evidence, with the recommended option pre-satisfying the invariant for when #56 lands.
- 2026-08-16 · reference run, page-echo discipline scaled to five findings in one issue (echoes of #53 f2, #53 f3/#58 f1, #56 f1, #58 f3, #53 f4/#49 f3) and the verifier upheld every one. The working rule, now well-tested: a false claim already raised for page A is a legitimate NEW finding on page B, provided the finding names the prior issue explicitly and the platform has not changed on that point. What makes it safe is that the fix targets are genuinely different files. The inverse trap also showed up and was avoided: #48 findings 8 and 10 sit on an IN-SCOPE page and are still open, so they were re-verified, recorded in the cleared block as unchanged, and NOT re-raised.
- 2026-08-16 · reference run, no live reproduction was possible: the VM has no harness key, so every claim is code-cited. ISSUE-FORMAT prefers reproduction over citation for behavior a cheap API request can show (the 424 status, the error envelope shape, the missing-anthropic-version acceptance are all one-curl checks). Worth setting up a harness key on the VM with Seb — three of this run's findings would have shipped with a verbatim request/response pair instead of a file:line, which is strictly stronger evidence for a status-code claim.
- 2026-08-17 · scoped run (request logs), the highest-value finding came from a page the rotation had never audited even though its TOPIC had: reference/telemetry.mdx was in the request-logs page list all along, but the 2026-08-06 run of this topic and the 2026-08-16 reference run each assumed the other owned it (the reference run excluded it explicitly and correctly). Three of seven findings landed there, including the privacy-page falsehood. Lesson: when a page appears in one topic's list but reads like it belongs to another, it is the likeliest page in the corpus to be stale. Grep topics.json for pages listed under a topic whose name does not match the page name, and audit those first.
- 2026-08-17 · request-logs run, the redaction trap: a page said inline image data "is stripped out", and the code DOES contain a redactor that strips inline images, called on every request. The claim was still false, because the redacted value is bound to a second variable used only for routing classification while the untouched body is what gets forwarded and stored. The three-line grep (does the redactor exist? is it called? on what?) returns "true, true, true" and the finding dies. Lesson: for any claim of the form "X is removed before storage", do not stop at the call site of the remover — follow the RETURN VALUE to the storage write. A sanitizer whose output feeds a different consumer than the one the doc describes is the most convincing false negative this harness has hit.
- 2026-08-17 · request-logs run, the verifier again caught an over-generalization in a tech-writer draft, this time in suggested DOCS wording rather than a finding title: the draft said a blocked attempt is logged as "No provider", which the evidence supported only for requests that named no model (the row keeps the requested model; the UI falls back to "No provider" when it is null). Same failure shape as the 2026-08-16 reference run. The pattern is now confirmed twice: the loss happens whenever a nullable field's DISPLAY FALLBACK is mistaken for its value. Lesson: when evidence shows `x ?? 'fallback'` in a renderer, the visible text must say what x usually is, not what the fallback prints.
- 2026-08-17 · request-logs run, a "no user-visible signal" finding is only as strong as the grep that failed to find one, so state the grep in the evidence. Finding 3 claims a self-hoster can have logs on everywhere and store nothing; it survived the verifier only because the evidence named the exact search (three identifier spellings across both backend and frontend, one hit, ANDed into the capture gate) and the verifier could re-run it. Lesson: a negative claim needs its search recorded as evidence, not just its conclusion. "No endpoint exposes this" without the query is unverifiable and the adversary is right to kill it.
- 2026-08-17 · full-mode run over a 1-PR window (#2720, removal of a promotional banner + auto-opening modal from the Overview page): CLEAN, valid silence. A marketing-surface DELETION is the cheapest possible audit when the docs never documented the surface: the whole question reduces to "did any page, image, or nav entry describe the thing that is now gone?", and a single corpus grep plus an image inspection answers it. The one non-obvious attack angle is the IMAGE one — a removed UI element strands screenshots, not sentences, and no grep can see that. The verifier opened all 9 files in images/ (not just the 6 referenced ones) and established that NONE shows the page the element lived on, which is what actually closed the silence. Lesson: for any PR that deletes visible UI, the finding, if it exists, is almost certainly an IMAGE finding; go to the screenshots first and only then to the prose. Second angle worth keeping: a deletion PR that also strips CSS can stale unrelated screenshots, so extract the removed selectors and confirm they are scoped to the deleted components' own class families.
- 2026-08-17 · #2720 run, the promotional-offer removal produced no docs finding but TWO platform-side backlog lines, one of them user-facing: the retired offer and the in-app help screen shared a booking slug, and retiring the offer appears to have deleted the shared event type, so the help screen's "Schedule a Call" button now 404s while the docs navbar CTA (a different slug) still resolves. Lesson: when a PR retires a campaign, curl every URL literal the campaign shared with a surviving screen — a marketing teardown deletes external resources the product still links to, and no amount of code reading surfaces a dead third-party link. Verifying the docs' own outbound CTA at the same time cost one extra request and is what proved the docs side clean.
- 2026-08-17 · #2720 run, the audit turned its lens on the harness itself and found an AUDIENCE violation in this repo's own memory: CLAUDE.md and several decisions.md lines carry internal codenames and an internal hostname in clear text, which AUDIENCE.md:61-67 bans (descriptors only, even for names public elsewhere). It was deliberately NOT fixed by the cron run, and the reason generalizes: the names are already in this repo's PUBLIC git history, so redacting the working copy is cosmetic, not remediation — an effective fix is a decision about history, which is Seb's to make. Lesson: a leak in an append-only memory file is not the same class of problem as a leak in a doc page; the doc page fix is a commit, the memory fix is a history decision. Park it, name the constraint, do not silently rewrite the record.
- 2026-08-17 · full-mode run over a 1-PR window (#2727, remounting the self-hosted email-provider form): the first run where the PLATFORM RESOLVED a still-open finding instead of a docs PR doing it. #54 finding 1 said "this screen does not exist"; the screen came back, so the finding's premise is gone and its recommended fix (strip the Limits-page instructions, keep only the env vars) would now write a NEW falsehood into the docs. Lesson with teeth beyond this run: `raised` findings are claims about a moving platform, and the batch of pending verdicts in decisions.md is not safe to apply blind. Before applying ANY accepted-but-unmerged fix, re-verify its premise against current main — the older the verdict, the likelier the platform moved under it. This is the mirror image of the standing "a fix that aligns the platform to the docs is not a finding" rule (2026-08-12): here the alignment silently invalidated a queued docs change.
- 2026-08-17 · #2727 run, a remount is NOT a no-op for the docs, and that is where the whole yield came from. While a screen is dead code, everything the doc says ABOUT that screen is unfalsifiable — only the existence claim can be judged, and the 2026-08-10 audit correctly judged exactly that and stopped. The moment the screen returns, the rest of the paragraph becomes testable for the first time, and here it failed on three separate counts (no Save button, Mailgun-only domain field, mandatory test email; wrong precedence; wrong coverage). Lesson: when a PR restores a previously-unreachable UI, do not treat the old finding's resolution as the end of the audit — re-audit every sentence that describes the restored screen, because none of them has ever been checked against a live surface. A resolution is an ENTRY POINT, same as a PR is.
- 2026-08-17 · #2727 run, the verifier killed a false statement in the visible journey that read as obviously true: "if the test fails you get the provider's own error message". Each provider adapter logs the upstream body server-side and returns a bare `false`, so the service collapses every refusal into the generic string "Provider returned failure" — the user gets no detail at all, which is a materially worse experience than the draft described and changes what the fix should say. Lesson: for any claim about what an error message CONTAINS, follow the value from the failing call to the string the user reads. A boolean return between the two is a total information loss, and it is invisible if you stop at the call site. Same shape as the 2026-08-17 redaction trap (follow the return value, not the call).
- 2026-08-17 · #2727 run, scope ruling worth reusing: finding 3 targets reference/environment-variables.mdx, a page owned by the reference ROTATION topic, in a FULL run about a Limits-page PR. The verifier ruled it in scope on this test: did the audited PR flip that sentence's truth value? It did — "alerts just don't email" without the env vars was TRUE while the form was dead code and became FALSE the moment a saved provider could email them. That is what separates it from the reference-page echoes the #2703/#2705 runs correctly parked in backlog: those were pre-existing defects the audited PRs never touched. Lesson: in full mode the scope boundary is the PR's causal reach, not page ownership; a page outside the window's topic is in scope exactly when the window's code made one of its sentences newly false.
- 2026-08-17 · #2727 run, the two naming rules finally collided and the split is now settled: ISSUE-FORMAT element 1 bans code references in the visible finding SENTENCE, while AUDIENCE's public-surface rule REQUIRES naming already-published surface in clear text. They do not conflict, they govern different surfaces. The verifier stripped the env-var glob from the finding title (descriptor "the email environment variables" costs no readability, since the journey says it too) and kept it verbatim in the suggested wording, which must mirror the doc line it replaces. Precedent it followed: filed issue #68 finding 1 says "the two rate-limit settings" in visible text and names the variables in the evidence. Rule: title and journey use descriptors; shippable doc wording and evidence use the literal names.
- 2026-08-17 · request-logs run, the verifier's non-blocking advice to keep two unverifiable claims OUT of the public issue was taken, and it is now the settled handling: reference/telemetry.mdx asserts Cloud sends no separate report and that the ingest hashes sender IPs, and the platform repo can prove neither (no mode gate is not the same as no report; the ingest lives elsewhere). Publishing "we cannot verify this privacy assurance" on a public repo raises doubt it cannot resolve. Parked in decisions.md as open questions for Seb, matching the 2026-08-07 Railway precedent. Lesson: unverifiable ≠ wrong, and on privacy claims specifically, the public issue is the wrong venue for the uncertainty.
- 2026-08-18 · scoped run (getting started, cron rotation, autonomous, no human): running TWO docs-auditors in parallel on complementary lenses paid for itself. Lens A walked the page claim by claim ("is this sentence still true"), lens B walked it gap-first ("what does a first-timer need that is not here"). They converged independently on the same cloud-journey defect, which is the strongest possible signal it is real, and each found something the other missed entirely: only lens A caught that per-request model choice and fallbacks are mutually exclusive (a truth-value question), only lens B caught that the quota's reset boundary exists on no page in the corpus (a coverage question). One auditor with both instructions would have subordinated one lens to the other. Worth repeating on any page dense enough to carry both kinds of defect.
- 2026-08-18 · getting-started run, the highest-value finding was invisible to a diff-driven audit. No PR in the window touched the introduction's opening paragraph; the sentence "You choose which model handles each request. Add fallbacks…" has presumably been false since tier routing shipped. It surfaced only because the auditor tested the CLAIM against the code rather than testing the CODE CHANGES against the page. On a rotation topic the window is a hint about what to read first, never a boundary on what may be found.
- 2026-08-18 · getting-started run, the verifier FAILed three findings out of four, every one on a visible-text detail that three prior agents had passed, and the pattern is now unmistakable across runs: the errors are always in the confident concrete specifics, never in the analysis. (a) The journey said an explicit model with a retired name "fails outright" — false on cloud, because Autofix repairs a 404 model name before any fallback, and autofix.mdx:31 already says so, so filing it would have put two docs pages in contradiction. (b) The journey and the suggested doc wording both said the harness key "is shown" at step one; it is minted there but rendered only at step five (Welcome.tsx:1320-1322), so the draft would have written a falsehood into the page. (c) The IMAGE finding asked Seb to shoot a frame that cannot exist: "Setup progress 1/5" with the create panel open and unsubmitted, when the counter counts COMPLETED steps and reads 0/5. Rule extracted: for any UI string, count or label a finding puts in visible text, open the render site and read the value, not the variable.
- 2026-08-18 · getting-started run, an absolute in a finding title needs its exception hunted before filing, not after. "An explicit model carries no fallback chain" is true on the default path and the verifier confirmed no re-attachment exists in explicitRouting, effectiveFallbackRoutes or tryFallbackChain — except through a matching custom-tier header, which overrides the explicit model and re-attaches that tier's fallbacks. That exception is already open as issue #68 finding 8 on the routing page, so the fix was to scope it in the evidence and point at the existing finding rather than restate it or weaken the title.
- 2026-08-18 · getting-started run, the scope boundary held under real pressure and the mechanism was the options list. The genuine defect spans three pages: the introduction promises a cloud start, llm-gateway.mdx carries the only cloud snippets but assumes you already have a key, and self-hosted.mdx owns the corpus's only end-to-end walkthrough, against localhost. Only the introduction is in scope. Rather than reaching onto the other pages, the finding stayed on the entry page and offered "add a section here", "fix the sentence only" and "create a Quickstart page" as three different outcomes, with the cross-page halves parked in backlog.md for their owners. A finding whose subject is the in-scope page's FAILURE TO ROUTE THE READER is in scope even when the destination is not.
- 2026-08-18 (full run, PR #2730) · A THIN WINDOW IS NOT A THIN RUN. One merged PR touching an operator README and a CI workflow would have justified silence under a literal reading of the window. Instead the platform-engineer was told to establish the full truth of the SURROUNDING behaviour the PR touches (how the host bind address, the port, the public URL and the upgrade paths actually interact), and that report is what produced six findings, three of them predating the PR. Lesson for the cron: when the window is one docs-shaped PR, spend the budget widening the TRUTH stage on that PR's user-facing surface, not on widening the page list. The page list stayed at two pages; the depth came from the code.
- 2026-08-18 · THE REGISTRY IMAGE IS A CHECKABLE SOURCE OF TRUTH, AND IT SETTLED THE RUN'S BIGGEST FINDING. Finding 1 (the documented `docker run` install publishes a port with no listener) was derived from the Dockerfile, then confirmed by pulling the PUBLISHED image's config blob from the registry API and reading its Env / ExposedPorts / Cmd. Auditor, verifier and orchestrator each ran it independently and got the same answer. For any claim about what a shipped image does, fetch the config blob: it costs one anonymous token request and it is the artifact the user actually runs, which the repo's Dockerfile only predicts.
- 2026-08-18 · THE VERIFIER EARNED ITS SEAT TWICE, AND THE SECOND TIME IT WAS ABOUT MEMORY, NOT THE ISSUE. Round 1 killed a false half-finding (the HSTS boot warning was claimed as a consequence of the LAN change; its gate is the container bind address, which the image pins, so it already fires on every plain-HTTP install), corrected a browser symptom by REPRODUCING it locally (connection accepted then reset, not refused), caught a title that contradicted another finding in the same issue, and fixed five line citations. Round 2 passed the whole issue and FAILed one backlog line for presenting a paraphrase as a verbatim quote across nine pages. Two lessons: (a) a killed half-finding must be recorded WITH its refutation, or the next rotation re-derives it, so finding 6's evidence block carries the reason in the public issue and decisions.md carries it here; (b) memory files get verified too, because they are what the next audit greps.
- 2026-08-18 · TWO AUDITORS ON COMPLEMENTARY LENSES CONVERGED FROM OPPOSITE DIRECTIONS. The claim-by-claim auditor reached the version-pinning defect by checking a sentence against the compose file; the gaps-and-cases auditor reached the same defect by walking the never-halfway invariant from the environment-variables reference. Independent convergence on one finding is the strongest signal the run produced, and it cost nothing extra: the two lenses otherwise overlapped on only one other point. Keep the pairing for full runs.
- 2026-08-18 · WRITE THE BACKLOG LINE BEFORE FILING, NOT AFTER. Finding 4's option 3 referenced a parked item that had not been written yet; the verifier caught the dangling reference in what would have been a public issue. When a candidate is dropped for scope during drafting, park it in the same breath. Corollary that worked: backlog lines may reference the issue number BEFORE it exists as long as the number is corrected after filing if GitHub assigns a different one (issues and PRs share a numbering space in this repo). This run guessed #72 and got #72; the check still has to be run.
- 2026-08-18 · CATEGORY IS A JUDGEMENT, AND THE VERIFIER IS THE RIGHT PLACE TO SETTLE IT. Finding 2 (an upgrade command that cannot run on the install path the page recommends) was drafted MISSING-CASE and recategorised WRONG on the verifier's flag: the printed instruction FAILS, which is a false instruction, not an uncovered situation. Rule of thumb going forward: if following the doc literally produces an error, it is WRONG; if following it works but the user's situation was never addressed, it is MISSING-CASE.
