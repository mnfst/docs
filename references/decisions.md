# Decisions — memory of raised findings and Seb's verdicts

One line per finding: `- <date> · <category> · <doc page> · <one-line claim> · status: raised|accepted|rejected|fixed · <issue/PR url>`
The issue-verifier reads this to never re-raise a rejected finding.
- 2026-08-06 · WRONG · message-recording.mdx · Settings section is "Logs" not "Message recording" · status: fixed (PR #46 merged) · https://github.com/mnfst/docs/issues/45 (finding 1)
- 2026-08-06 · WRONG · message-recording.mdx + reference/telemetry.mdx · "older harnesses keep their setting" · status: fixed (PR #46 merged) · https://github.com/mnfst/docs/issues/45 (finding 2)
- 2026-08-06 · OBSOLETE · message-recording.mdx + observability.mdx · navigation is now Requests → attempts · status: fixed (PR #46 merged) · https://github.com/mnfst/docs/issues/45 (finding 3)
- 2026-08-06 · MISSING-CASE · self-hosted.mdx + message-recording.mdx · pre-July installs need an upgrade step · status: fixed (PR #46 merged) · https://github.com/mnfst/docs/issues/45 (finding 4)
- 2026-08-06 · IMAGE · message-recording.mdx · screenshot of Settings → Logs · status: fixed (image shipped 2026-08-07) · https://github.com/mnfst/docs/issues/45 (finding 5)
- 2026-08-06 · IMAGE · message-recording.mdx · screenshot of the request window · status: fixed (image shipped 2026-08-07) · https://github.com/mnfst/docs/issues/45 (finding 6)
- 2026-08-06 · findings 1-4 applied in https://github.com/mnfst/docs/pull/46 (awaiting review); findings 5-6 await Seb's screenshots.
- 2026-08-07 · WRONG/MISSING-CASE · deploy/heroku.mdx · "one required value" but app.json now marks 4 REQUEST_RECORDING_S3_* required · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #60 merged 2026-08-17 (issue #48 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/48 (finding 1)
- 2026-08-07 · MISSING-CASE · deploy/digitalocean.mdx · template sets STORAGE=s3 but page never asks for a Space + S3 creds · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #60 merged 2026-08-17 (issue #48 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/48 (finding 2)
- 2026-08-07 · MISSING-CASE · deploy/koyeb.mdx · stale deploy link deploys without S3 → logs lost · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #60 merged 2026-08-17 (issue #48 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/48 (finding 3)
- 2026-08-07 · WRONG · deploy/render.mdx · recordings now persist on a mounted 1GB disk (paid Starter, no sleep); doc says ephemeral/needs S3 · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #60 merged 2026-08-17 (issue #48 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/48 (finding 4)
- 2026-08-07 · WRONG · deploy/fly.mdx · deploy.sh auto-provisions Tigris S3; doc says attach a volume · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #60 merged 2026-08-17 (issue #48 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/48 (finding 5)
- 2026-08-07 · WRONG · request-logs.mdx · blanket "Render/Fly/Heroku/Koyeb ephemeral, wire S3 yourself" no longer true · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #60 merged 2026-08-17 (issue #48 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/48 (finding 6)
- 2026-08-07 · MISSING-CASE · deploy/easypanel.mdx · manual-deploy env block omits the recordings volume the template mounts · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #60 merged 2026-08-17 (issue #48 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/48 (finding 7)
- 2026-08-07 · WRONG · reference/environment-variables.mdx · DB_POOL_MAX/AUTH_DB_POOL_MAX defaults are 10/5, doc says 30/10 · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #60 merged 2026-08-17 (issue #48 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/48 (finding 8)
- 2026-08-07 · MISSING-CASE · deploy/aws.mdx · retained S3 recording bucket provisioned; teardown/creation undocumented · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #60 merged 2026-08-17 (issue #48 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/48 (finding 9)
- 2026-08-07 · INTERNAL-LEAK · reference/environment-variables.mdx · lines 32 & 36 name an internal gateway-tester tool + its internal hostname (the *_CORS_ORIGINS var carries the codename) · status: rejected (2026-08-15 — premise false: Wingman is a PUBLIC product, repo mnfst/wingman is public/MIT with hosted app advertised in its README, so naming it in the docs is not an internal leak; Seb instead asked to LINK the public repo from the WINGMAN_CORS_ORIGINS row, shipped in the same PR) · https://github.com/mnfst/docs/issues/48 (finding 10)
- 2026-08-07 · OPEN QUESTION · deploy/railway.mdx + request-logs.mdx · Railway recording storage · RESOLVED 2026-08-15: live check of the public template page (railway.com/deploy/wild-wild) shows Template Content = Postgres + manifestdotbuild/manifest + a `messages-recordings` Bucket — the bucket lives in the Railway-side template, not railway.toml, which is why the repo grep was empty. Both pages corrected in the #48 PR (Fly-style wording).
- 2026-08-07 · OBSOLETE · introduction.mdx · card still says "Message recording" (+5 leftover mentions elsewhere) · status: accepted (option 1) → PR #50 · https://github.com/mnfst/docs/issues/49 (finding 1)
- 2026-08-07 · WRONG · self-hosted.mdx · "first account becomes the admin" vs the setup screen · status: accepted (option 1, user-as-actor wording) → PR #50 · https://github.com/mnfst/docs/issues/49 (finding 2)
- 2026-08-07 · WRONG · self-hosted.mdx · "unknown model falls back to routing" vs M302 · status: accepted (option 1, live-reproduced) → PR #50 · https://github.com/mnfst/docs/issues/49 (finding 3)
- 2026-08-07 · WRONG · self-hosted.mdx · fresh-install table gives M100 the definition of M101 · status: accepted (option 1, live-reproduced) → PR #50 · https://github.com/mnfst/docs/issues/49 (finding 4)
- 2026-08-07 · INTERNAL-LEAK · deploy/aws,render,easypanel,koyeb · the instance-mode env var (named in the deploy guides) absent from the reference · status: accepted (final: documented in the reference with its user-facing surface only — the coherence rule forbids the halfway state where guides use a variable the reference doesn't define) → PR #50 · https://github.com/mnfst/docs/issues/49 (finding 5)
- 2026-08-07 · MISSING-CASE · introduction.mdx · free plan quota (10k/month) stated nowhere outside the M204 error page · status: accepted (option 1) → PR #50 · https://github.com/mnfst/docs/issues/49 (finding 6)
- 2026-08-08 · SCOPED RUN (autofix, cron, autonomous, no human) · pages autofix.mdx + errors.mdx · window: full feature surface (PRs #2379→#2694) · CLEAN — 0 findings, no issue filed. Auditor + fresh-eyes verifier both re-verified every falsifiable claim against code: repairable statuses 400,404,422; synthetic-M302 heal (#2569) gone → forward-to-provider 404 (#2639) matches autofix.mdx:30; before-fallback/one-retry; streaming before-first-byte; toggle defaults ON-cloud/OFF-selfhosted with the nullable-reset migration (pre-existing agents inherit, not force-flipped) matching the user-visible "keeps its state"; consent self-hosted-only on any enable path; guardrails 3-failures/30s/one-success; env table (5 vars, defaults, 256 KB drop, AUTOFIX_URL is a constant not a var); dashboard cards. No internal-name leak (Phoenix/Peacock/Doctor/autofix.manifest.build absent). All 4 images present. lastAuditedAt stamped 2026-08-08; lastAuditedPr untouched (scoped run).
- 2026-08-09 · SCOPED RUN (llm gateway: routing, tiers, fallbacks · cron rotation, autonomous, no human) · page llm-gateway.mdx · window: last 30 days + full current routing/gateway code path · 5 findings filed as issue #53; lastAuditedPr untouched (scoped run), topic stamped 2026-08-09. Categories: WRONG ×3, MISSING-CASE ×1, IMAGE ×1.
- 2026-08-09 · WRONG · llm-gateway.mdx:87 · "/v1/messages always routes through tiers" false — a concrete model ID is a direct route on all three endpoints (proxy.service.ts:899-916) · status: accepted (fix applied, batch 2026-08-15) → fixed, shipped in commit 6b65f3a and live on the page (verified at ca319b8 by the 2026-08-19 rotation) · https://github.com/mnfst/docs/issues/53 (finding 1)
- 2026-08-09 · WRONG · llm-gateway.mdx:97,138,6 · invented "424 exception" to fallback + "exhaustion returns 424" false — trigger is literally >=400 no exception, exhaustion preserves the PRIMARY model's real status and signals via X-Manifest-Fallback-Exhausted + body code fallback_exhausted (fallback-status-codes.ts:1-3, proxy.service.ts:1258-1265, proxy-response-handler.ts:403-410) · status: accepted (option 1, batch 2026-08-15) → fixed, shipped in commit 6b65f3a and live on the page (verified at ca319b8 by the 2026-08-19 rotation) · https://github.com/mnfst/docs/issues/53 (finding 2)
- 2026-08-09 · WRONG · llm-gateway.mdx:142 · "M200 hard-limit block returns HTTP 429" false — delivered as an HTTP 200 friendly assistant stub (proxy-friendly-response.ts:115-119); 429 is M201/202/203, 402 is M204 · status: accepted (fix applied, batch 2026-08-15) → fixed, shipped in commit 6b65f3a and live on the page (verified at ca319b8 by the 2026-08-19 rotation) · https://github.com/mnfst/docs/issues/53 (finding 3)
- 2026-08-09 · MISSING-CASE (low severity, honest leave-as-is option) · llm-gateway.mdx:89 · "any unlisted model ID → M302" is a false universal — an uncatalogued-but-connected model is forwarded to the provider (#2639, proxy.service.ts:981-1049); harm is missed capability, not failure. NOT a re-raise of decisions.md self-hosted.mdx M302 line (different claim, different page) · status: accepted (option 2 soften, batch 2026-08-15) → fixed, shipped in commit 6b65f3a and live on the page (verified at ca319b8 by the 2026-08-19 rotation) · https://github.com/mnfst/docs/issues/53 (finding 4)
- 2026-08-09 · IMAGE · llm-gateway.mdx:114-124 · Fallback Configuration steps describe the Routing page drag-to-reorder flow in prose only; screenshot of a tier + ordered fallback list missing · status: accepted (screenshot pending — needs a dashboard capture, deferred) · https://github.com/mnfst/docs/issues/53 (finding 5)
- 2026-08-10 · SCOPED RUN (observability: requests page, analytics · cron rotation, autonomous, no human) · page observability.mdx · window: observability feature surface, ~45 days (PRs incl. #2374/#2456/#2485/#2502/#2508/#2535/#2609/#2642/#2666) · 4 findings filed as issue #54; lastAuditedPr untouched (scoped run), topic stamped 2026-08-10. Categories: WRONG ×2, MISSING-CASE ×1, IMAGE ×1. Full loop ran (platform-engineer truth → docs-auditor cross-check → issue-verifier fresh-eyes). Auditor cleared two near-findings: local/custom cost shows as $0 in aggregates (spirit-correct, mechanism is NULL not literal 0 — user-visible truth holds, leave-as-is), and the four-card dashboard map (fair conceptual grouping, "error rates" served by the shipped Success rate). No cross-page backlog items surfaced (env-vars reference already carries the EMAIL_* rows, coherence holds).
- 2026-08-10 · WRONG · observability.mdx:98 · self-hosted "Configure a provider on the Limits page: Resend/Mailgun/SendGrid … then save" is false — the in-app email-provider form is unmounted dead code (EmailProviderSetup has zero JSX usages; Limits.tsx:213 renders only read-only CloudEmailInfo); the EMAIL_* env vars (send-email.ts:30-59) are the ONLY self-hosted path · status: accepted (option 1, batch 2026-08-15) → SUPERSEDED by platform PR mnfst/manifest#2727, NOT applied (see the 2026-08-17 supersession line below); the page now documents the restored form via docs PR #76 · https://github.com/mnfst/docs/issues/54 (finding 1)
- 2026-08-10 · WRONG · observability.mdx:67,79 · limit-rule "Action | Email you, block further requests, or both" implies selectable block-only; UI always emails and block is an optional toggle (LimitRuleModal.tsx:25 actionValue notify|both, no block-only emitted) · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #63 merged 2026-08-17 (issue #54 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/54 (finding 2)
- 2026-08-10 · MISSING-CASE (low severity, honest leave-as-is option) · observability.mdx:32-34 · Free cloud plan is clamped to 7d of dashboard history; 30/90/365d ranges are Pro (Overview.tsx:50 PRO_RANGES, :193 clamp, plan-range-lock.ts:27-28) and the page is silent · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #63 merged 2026-08-17 (issue #54 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/54 (finding 3)
- 2026-08-10 · IMAGE · observability.mdx:29-44 · "The dashboard" four-card section has no screenshot; one Overview-tab shot (KPI tiles + spend-over-time chart) under the card grid · status: accepted (screenshot pending — needs a dashboard capture, deferred) · https://github.com/mnfst/docs/issues/54 (finding 4)
- 2026-08-08 · FULL RUN (cron, autonomous, no human) · window: PRs #2698→#2701 (only #2701 merged; #2698-2700 never merged) · CLEAN — 0 findings, no issue filed. #2701 "route Bedrock GPT-5 responses correctly" is a pure internal proxy buildPath fix: Bedrock GPT-5-family models now forward to /openai/v1/responses instead of /v1/responses (provider-endpoints.ts:83-96,208), other Bedrock OpenAI models stay on /v1/responses. No user-facing surface — no setting, model-catalog entry, setup step, or error string; it removes a formerly-broken request-failure mode transparently. Three agents concurred: platform-engineer (internal-only), docs-auditor (only providers/api-key-providers.mdx mentions Bedrock, no obsolete claim, no leak, reference/api.mdx's /v1/responses is the PUBLIC client endpoint not the internal Bedrock upstream), issue-verifier as fresh-eyes adversary (SILENCE CONFIRMED). No leak strings in docs (mantle/api.aws/sol/terra/luna codenames: 0 hits). Coherence GREEN. lastAuditedPr advanced 2697→2701.
- 2026-08-10 · FULL RUN (cron, autonomous, no human) · window: merged PRs since #2703 = only #2705 · CLEAN — 0 in-scope findings, no issue filed. #2705 "test: extend gateway API contracts" is test-only (one file: contracts/gateway/tests/routes.test.mjs, +48/−10): refactors the test helper to a generic request(method,body), ADDS contract tests for GET /v1/models and POST /v1/messages (Anthropic), and strengthens assertAuthenticationRequired to also assert error.message is a non-empty string. Changes ZERO product code. Full loop ran (platform-engineer truth → docs-auditor cross-check → issue-verifier fresh-eyes adversary; all three concurred SILENCE). Surface it pins = the four public gateway routes (GET /v1/models, POST /v1/chat/completions, POST /v1/responses, POST /v1/messages on @Controller('v1'), proxy.controller.ts:147/205/213/221) + the unauthenticated 401 auth envelope {error:{message:<non-empty>,type:'auth_error',code:'manifest_auth'}} (proxy-exception.filter.ts:143-147; templates error-codes.ts:8-30). All documented CORRECTLY: reference/api.mdx:30-37 (four routes, method/path/format), :73 (data[0]=auto), reference/headers.mdx:17 (anthropic-version), errors.mdx:21 + errors/M001-M005.mdx ("HTTP 401 to non-chat clients" — correctly captures the streaming-gets-friendly-200 split, proxy-exception.filter.ts:36-44). Cleared near-finding (recorded so it is not re-litigated): errors.mdx:21 / errors/M001.mdx frame the auth error as firing "on /v1/chat/completions" while the guard is controller-wide (401 universal on all four routes) — NOT a finding: the guard behaves identically across routes (no divergent case), the statement names a representative example not an exclusive scope, and the fix (send Bearer key) is endpoint-independent, so no user is misled. No leak strings (PostHog/cubic/Phoenix/internal hostnames) on any in-scope page. Two known false concepts on reference/api.mdx ("424 on fallback exhaustion" :6/204/208 + headers.mdx:47; limit_exceeded/429 envelope :194-195) stayed OUT OF SCOPE (reference topic, lastAuditedAt null) — already parked in backlog.md as echoes of #53 findings 2/3, not re-raised. Coherence GREEN (48 pages, 56 vars). lastAuditedPr advanced 2703→2705.
- 2026-08-11 · SCOPED RUN (providers: api keys, subscriptions, local, custom · cron rotation, autonomous, no human) · pages providers/{api-key-providers,subscription-based-providers,local-models,custom-providers}.mdx · window: full provider surface, ~60 days of merged PRs through #2705 + current registry/routing code · 6 findings filed as issue #55; lastAuditedPr untouched (scoped run), topic stamped 2026-08-11. Categories: MISSING-CASE ×1, WRONG ×3, IMAGE ×2. Full loop ran (platform-engineer truth → docs-auditor cross-check → issue-verifier fresh-eyes adversary; verifier BLOCKED once on an IMAGE-format violation — finding 5 bundled two shots + carried a visible rationale — fixed to one sentence + rationale-in-details, all 6 shipped). Auditor cleared near-findings (recorded so not re-litigated): local/custom cost="0" is the already-decided spirit-correct display (NULL→0 in aggregates, 2026-08-10 obs decision, NOT re-raised); Claude subscription anthropic-beta header the doc shows a partial-but-true substring of the real comma-list (non-actionable plumbing); OpenCode Go x-api-key variant is a real supported variant; Copilot device URL github.com/login/device is the correct human-facing page; 33-provider catalog completeness verified (no mis-categorization; Command Code correctly sub-only despite user_ prefix, Ollama Cloud correctly sub-only not local); connection ceiling 5→50 (#2611) not stated in any page so nothing to contradict. No internal-name leak on any page (Phoenix/Mantle/bedrock-mantle/credits.manifest.build/codex_cli_rs/claude-cli/Copilot-Integration-Id/peacock/Wingman: 0 hits). Coherence GREEN (48 pages, 56 vars). No new backlog items.
- 2026-08-11 · MISSING-CASE · local-models.mdx (whole page) · local runtimes (Ollama/LM Studio/llama.cpp) are self-hosted-only — blocked on Cloud at connect/discovery/proxy with CLOUD_LOCAL_PROVIDER_MESSAGE + M303 (provider-availability.ts:4-5,15-17; provider.controller.ts:116-119; model.controller.ts:110-111; provider-client.ts:260-261; error-codes.ts:75; PR #2535); page presents them as universally available, Cloud workaround (custom provider on public URL/tunnel) undocumented · status: accepted (option 1 callout, batch 2026-08-15) → FIXED, docs PR #64 merged 2026-08-18 (issue #55 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/55 (finding 1)
- 2026-08-11 · WRONG · subscription-based-providers.mdx:79-82 · GLM Coding Plan tab names only the China host open.bigmodel.cn; global default routes to api.z.ai/api/coding/paas/v4 (zai-region.ts:2-3,8 DEFAULT=global) — MiniMax tab already shows both, GLM tab shows only cn · status: accepted (fix applied, batch 2026-08-15) → FIXED, docs PR #64 merged 2026-08-18 (issue #55 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/55 (finding 2)
- 2026-08-11 · WRONG · custom-providers.mdx:54 · SSRF note invents "self-hosted instance on the same network"; real gate is deployment mode (validatePublicUrl allowPrivate:isSelfHosted(), custom-provider.service.ts:214,297,429; url-validation.ts) — self-hosted allows private IPs + http://, Cloud requires public https, cloud-metadata blocked in both · status: accepted (fix applied, batch 2026-08-15) → FIXED, docs PR #64 merged 2026-08-18 (issue #55 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/55 (finding 3)
- 2026-08-11 · WRONG (low severity, honest leave-as-is option 2) · api-key-providers.mdx:90 · Bedrock + NVIDIA NIM listed as "no fixed prefix" but connect-form placeholders are ABSK (bedrock-region.ts AWS_BEARER_TOKEN_PREFIX) and nvapi- (providers.ts:319); prefixes are UI paste-check aid, not server-enforced (finding does not claim enforcement) · status: REVERSED 2026-08-18 → rejected, option 2 (leave as-is) · derivation: Seb live-tested the NVIDIA and Bedrock connect forms (an sk- key is accepted, no prefix error — matches keyPrefix:'' in the provider registry, placeholder only) and ruled the prefix table is a reader aid, not a spec: adding the two rows plus an enforcement nuance is noise. The page keeps its original sentence (Cerebras, NVIDIA NIM, AWS Bedrock, Kilo, OpenCode Zen: no fixed prefix). Do not re-raise unless the platform starts enforcing these prefixes · https://github.com/mnfst/docs/issues/55 (finding 4)
- 2026-08-11 · IMAGE · api-key-providers.mdx:53-65 · "Add a provider" flow describes the Routing tile grid (shared entry point for all four provider pages) in prose only; screenshot of the tile grid missing · status: accepted (screenshot pending — needs a dashboard capture, deferred) · https://github.com/mnfst/docs/issues/55 (finding 5)
- 2026-08-11 · IMAGE · subscription-based-providers.mdx:41-45 · "Pick the Subscription tab" step describes the dual-auth API Key vs Subscription two-tab panel in prose only; screenshot missing · status: accepted (screenshot pending — needs a dashboard capture, deferred) · https://github.com/mnfst/docs/issues/55 (finding 6)
- 2026-08-12 · SCOPED RUN (self-hosting with docker: install, upgrade, backup · cron rotation, autonomous, no human) · page self-hosted.mdx · window: full self-hosting/Docker surface (docker/install.sh, docker-compose.yml, .env.example, Dockerfile, image-signing CI, telemetry) + PRs #2565/#2591/#2693/#1649/#1777/#1785 · 1 finding filed as issue #56; lastAuditedPr untouched (scoped run), topic stamped 2026-08-12. Category: OBSOLETE ×1. Full loop ran (platform-engineer truth → docs-auditor cross-check → issue-verifier fresh-eyes adversary; verifier PASS-WITH-EDITS: purged code-words/raw-template from the visible "What actually happens" block into evidence, fixed a dangling cross-reference; every factual claim re-verified TRUE). Auditor cleared near-findings (recorded so not re-litigated): custom-port section (PORT=8080 alone still drives host bind + container + BETTER_AUTH_URL; the HOST_PORT layer in the ports template gives the identical PORT-only result, docker-compose.yml:50,67 — not misleading); M-code table M003/M005/M101/M100 all still match error-codes.ts:16-37 (the #49-finding-4 M100/M101 swap fix holds); env-var Core table six vars correct (BETTER_AUTH_URL default shown as http://localhost:${PORT} vs code http://localhost:${HOST_PORT:-${PORT:-2099}} — identical on the documented PORT-only path); the "before v6.18.0 → run install.sh --upgrade to add manifest_request_recordings" note is ACCURATE (volume first shipped in released tag 6.18.0, verified against tags); install-resume/backup/pg_dump-U-manifest-manifest all correct. No internal-name leak on the page (Phoenix/Peacock/Wingman/credits.manifest.build/WINGMAN_CORS_ORIGINS/MANIFEST_PUBLIC_STATS: 0 hits, though several live in the compose/.env.example). Coherence GREEN (48 pages, 56 vars). One backlog line appended (reference env-vars needs HOST_BIND_ADDRESS/HOST_PORT rows if the LAN fix lands).
- 2026-08-12 · OBSOLETE · self-hosted.mdx:208 (+ intro Note :17, BIND_ADDRESS Note :326) · "Exposing on the LAN" tells users to edit a compose ports line "127.0.0.1:2099:2099" that does not exist (real line docker-compose.yml:50 is env-templated "${HOST_BIND_ADDRESS:-127.0.0.1}:${HOST_PORT:-${PORT:-2099}}:${PORT:-2099}"); any hand-edit is wiped by install.sh --upgrade (install.sh:233-259 atomically replaces docker-compose.yml, never touches .env); real upgrade-safe knob is HOST_BIND_ADDRESS=0.0.0.0 in .env (docker-compose.yml:30-31, .env.example:28), shipped in released tag 6.18.0 · status: accepted (option 1, batch 2026-08-15; env-vars reference row added in the same PR per never-halfway) → FIXED, docs PR #65 merged 2026-08-18 (issue #56 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/56 (finding 1)
- 2026-08-12 · FULL RUN (cron, autonomous, no human) · window: merged PRs since #2705 = #2711 + #2712 · CLEAN — 0 findings, no issue filed. Both PRs are transparent backend proxy-path bug fixes (files: proxy-response-handler.ts / provider-client.ts + their __tests__ + a changeset; zero doc-facing surface). #2711 "return real content for non-streaming Bedrock GPT-5.x responses": handleNonStreamResponse's forward.isChatGpt branch now sniffs SSE vs JSON (content-type/event:/data:) and routes JSON Responses objects through providerClient.convertChatGptResponse instead of assuming SSE (proxy-response-handler.ts:797-816); non-streaming /v1/chat/completions to openai.gpt-5.6-luna/-sol/-terra, gpt-5.5, gpt-5.4 previously returned HTTP 200 content:null/zero-usage, now returns real content+usage+reasoning+tool_calls; no new setting/env/model/error/header — restores expected output on an existing endpoint. #2712 "keep subscription requests streaming upstream": provider-client.ts:648-657 non-native path now forces stream:true unconditionally for endpointKey 'openai-subscription' (native path already did via forceStream); the subscription backend serves SSE only and Manifest buffers it back to JSON for stream:false callers, so a stream:false caller previously got a provider 400 + one spurious fallback Provider Attempt and now succeeds from the selected model with no spurious attempt. Full loop ran (platform-engineer truth → docs-auditor cross-check → issue-verifier fresh-eyes adversary; all three concurred SILENCE). Both fixes make reality MATCH existing docs (reference/api.mdx:182 stream:true→SSE, reference/headers.mdx:32 X-Manifest-Response-Mode buffered) — a fix that aligns the platform to the docs is not a finding. INTERNAL-LEAK sweep of *.mdx for bedrock-mantle/mantle/api.aws/codex_cli_rs/"/codex/responses": 0 genuinely-internal hits; the two "codex" hits (reference/api.mdx:33 public OpenAI model family; providers/subscription-based-providers.mdx:58 OpenAI's own public backend URL chatgpt.com/backend-api/codex/responses) are public OpenAI surface, already cleared in the 2026-08-11 providers audit (decisions.md line 43), not re-raised (PRs did not change that surface). sol/terra/luna + gpt-5.x are public user-selectable model IDs (configs.ts knownModels), not codenames. No CASE-MATRIX dimension added (both are stateless request-path fixes, no fresh/upgrade/existing-agent/subscription split). No new backlog items (the 2026-08-12 self-hosting HOST_BIND_ADDRESS/HOST_PORT backlog line is untouched, out of this window). Coherence GREEN (48 pages, 56 vars). lastAuditedPr advanced 2705→2712.
- 2026-08-13 · SCOPED RUN (deploy platforms · cron rotation, autonomous, no human) · pages deploy/{aws,coolify,digitalocean,easypanel,fly,gcp,heroku,koyeb,railway,render}.mdx · window: full deploy-template surface (app.json, render.yaml, railway.toml, .do/deploy.template.yaml, deploy/{fly,aws,gcp,coolify,easypanel,koyeb,digitalocean}/* ); last deploy change is PR #2693 (2026-08-07) — nothing newer touches the deploy surface (verified `git log a87520dfd..HEAD` on deploy files = empty) · 3 findings filed as issue #57; lastAuditedPr untouched (scoped run), topic stamped 2026-08-13. Categories: WRONG ×1, MISSING-CASE ×2. Full loop ran (platform-engineer truth → docs-auditor cross-check → issue-verifier fresh-eyes adversary; verifier BLOCKED once on the ISSUE-FORMAT "no code in the visible block" gate — all 3 "What actually happens" blocks + finding-1 title carried code tokens (`/setup`, `/data/request-recordings`, `recording_bucket`, `deploystack uninstall`); fixed by moving every token to the folded evidence and phrasing the visible journey in user language, same purge enforced on #56; every factual claim re-verified TRUE on both sides). The still-open #48 deploy findings (heroku/render/digitalocean/aws/fly/easypanel/koyeb recordings persistence) were NOT re-raised — platform unchanged since #2693 — and the Railway recordings/S3 storage OPEN QUESTION was kept OUT of the public issue (private, decisions.md 2026-08-07). Auditor cleared near-findings (recorded so not re-litigated): health path is `/api/v1/health` on every page and every template (health.controller.ts @Controller('api/v1')+@Get('health'), gcp uses `terraform output -raw health_check_url` → same) — CLEAN; fly.mdx:52 admin wording already correct (no passive "becomes the admin" claim); heroku.mdx:54/easypanel.mdx:76 already carry the corrected setup-screen actor wording (excluded from finding 1). No internal-name leak on any deploy page (the internal error-page CMS codename + its push-secret var, the public-stats cloud-marketing flag: 0 hits on deploy/*.mdx). Coherence GREEN (48 pages, 56 vars). No new backlog items.
- 2026-08-13 · WRONG (low severity, consistency, honest leave-as-is option 2) · deploy/{gcp:71,coolify:55,aws:79,digitalocean:55,render:54,koyeb:66,railway:47}.mdx · seven pages assert passive "The first account becomes the admin"; a fresh instance redirects to a /setup wizard where the USER creates the account via a form ("Create admin account" button), no automatic role promotion and no admin-role column exists (setup.service.ts:84-90,109-160; Setup.tsx:70,127) — same wording corrected on self-hosted.mdx in #49 finding 2 (accepted → PR #50) and already fixed on sibling deploy pages heroku:54/easypanel:76; recommend aligning the seven to the shipped setup-screen actor wording · NOT a re-raise (different page set; #49 was self-hosted.mdx only) · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #66 merged 2026-08-18 (issue #57 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/57 (finding 1)
- 2026-08-13 · MISSING-CASE · deploy/coolify.mdx:67,72 · "What Gets Provisioned" + backup note name only the PostgreSQL volume, but the Compose template mounts a SECOND persistent volume for request logs (manifest-recordings-data:/data/request-recordings, REQUEST_RECORDING_STORAGE=filesystem — deploy/coolify/docker-compose.yml:16-17,19,49-50); a user backing up "the PostgreSQL volume" as instructed loses request logs · coolify was NOT among the #48 deploy findings · status: accepted (fix applied, batch 2026-08-15) → FIXED, docs PR #66 merged 2026-08-18 (issue #57 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/57 (finding 2)
- 2026-08-13 · MISSING-CASE · deploy/gcp.mdx (whole page; Production notes :81) · the page never mentions the private GCS bucket the Terraform provisions for request logs (google_storage_bucket "recordings" ${local.name_prefix}-recordings force_destroy=false, mounted /data/request-recordings, output recording_bucket — main.tf:123-126,204-210,249-250; outputs.tf:21-23), so neither the log-storage existence nor the retained/deletion-protected bucket at teardown (TUTORIAL.md:57 export/delete contents before `deploystack uninstall`) is documented; same class as the aws.mdx retained-bucket case (#48 finding 9) but on the never-audited GCP page · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #66 merged 2026-08-18 (issue #57 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/57 (finding 3)
- 2026-08-14 · SCOPED RUN (error catalog (M-codes) · cron rotation, autonomous, no human) · pages errors.mdx + errors/{M001-M005,M100-M102,M200-M204,M300,M302,M303,M500}.mdx · window: full error-catalog surface (error-codes.ts registry, proxy delivery paths, rate limiter) at main HEAD 44cb47b3 · 3 findings filed as issue #58; lastAuditedPr untouched (scoped run), topic stamped 2026-08-14. Categories: WRONG ×2, MISSING-CASE ×1. Full loop ran (platform-engineer truth → docs-auditor cross-check → tech-writer drafts → issue-verifier fresh-eyes adversary; verifier re-verified every doc quote + code claim TRUE, then BLOCKED once on the ISSUE-FORMAT "no code in the visible block" gate — finding-2 title carried a source-file name + the fix-wording listed the three raw constant names in the visible area; fixed by retitling to "editing a source file" and making the keyword-removal instruction generic (names live in the folded evidence). Verifier independently strengthened finding 2 by ruling out the separate global ThrottlerModule: THROTTLE_LIMIT/THROTTLE_TTL env vars feed app.module.ts:78-81 but the proxy controller carries @SkipThrottle() on @Controller('v1') (proxy.controller.ts:123), so those env vars do NOT touch M201/M202/M203 — the rate caps remain genuinely unconfigurable). Backlog line 7 (2026-08-07, M201/M202/M203 edit-source-file, owner: this topic) CONSUMED by finding 2. Auditor cleared near-findings (recorded so not re-litigated): errors.mdx M204 overview "surfaces as HTTP 402" omits that the 402 is SDK/tool-only (chat→200 stub) and that M204 is Cloud-only (assertWithinRequestLimit early-returns unless isBillingEnabled() = !isSelfHosted + Stripe keys, plan.service.ts:435,450-458) — NOT a finding because the authoritative M204.mdx page already carries both facts (":4/:20 Manifest Cloud Free workspace", ":17 SDK and tool callers receive an HTTP 402"), acceptable overview simplification; M301 retirement note (errors.mdx:63-67) accurate vs the 17-code registry (M301 absent). INTERNAL-LEAK sweep of all 18 in-scope pages CLEAN (the autofix/self-heal codename, the hourly-scraper codename, the 4xx-report flag, routing_reason analytics strings: 0 hits; the 🦚 emoji + manifest.build/docs/errors are user-facing/public). Coherence GREEN (48 pages, 56 vars).
- 2026-08-14 · WRONG · errors.mdx:43 + errors/M200.mdx:4,7 · docs say "M200-M203 surface as HTTP 429"/"Returns HTTP 429" but M200 is NEVER a 429 — a tripped agent-level usage limit is delivered as a friendly HTTP 200 assistant-message stub to ALL clients (buildFriendlyResponse always status:200, proxy-friendly-response.ts:70-98,115-127; emitted proxy.service.ts:260 via enforceLimits :1352-1373); only M201/M202/M203 are real 429s (proxy-rate-limiter.ts:47,72,85). NOT a re-raise of #53 finding 3 (that was llm-gateway.mdx:142; different page, platform unchanged) · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #61 merged 2026-08-17 (issues #52/#58 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/58 (finding 1)
- 2026-08-14 · WRONG (primary; secondary internal-implementation-leak, same fix) · errors/M201.mdx:27, M202.mdx:25, M203.mdx:25 (+ frontmatter descriptions/keywords) · step 3 tells self-hosted users to "raise RATE_MAX_REQUESTS/IP_RATE_MAX_REQUESTS/CONCURRENCY_MAX in proxy-rate-limiter.ts" — these are hardcoded compile-time module constants (proxy-rate-limiter.ts:5,6,8=200/500/10), the class reads no ConfigService/process.env, the names appear nowhere else in packages/backend/src, docker/.env.example has no rate keys, and the separate ThrottlerModule is @SkipThrottle()'d off the proxy; there is NO env/config/runtime override, so the documented fix is non-actionable and leaks an internal source path/symbol. Consumes backlog line 7 · status: accepted (option 1, batch 2026-08-15) → FIXED, docs PR #61 merged 2026-08-17 (issues #52/#58 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/58 (finding 2)
- 2026-08-14 · MISSING-CASE (low severity, consistency, honest leave-as-is option 3) · errors.mdx:21 vs :55,:71 · the chat-vs-tool status split is stated only for the auth block ("HTTP 401 to non-chat clients") but M300 (":55 HTTP 400"), M500 (":71 HTTP 500") are given as flat statuses and M303 is lumped with M302 as "friendly proxy message"; truth: chat/streaming clients always get a friendly HTTP 200 stub, only tool/SDK clients get the real status — M300 chat200/tool400, M303 chat200/tool400 (provider-client.ts:261 BAD_REQUEST), M500 chat200/tool500, M302 is 200 for all (proxy.service.ts:1438); recommend a single umbrella sentence + splitting M302 from M303 · status: accepted (option 1 umbrella sentence, batch 2026-08-15) → FIXED, docs PR #61 merged 2026-08-17 (issues #52/#58 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/58 (finding 3)
- 2026-08-10 · FULL RUN (cron, autonomous, no human) · window: merged PRs since #2701 = only #2703 · CLEAN — 0 in-scope findings, no issue filed. #2703 "test: protect gateway API contracts" is CI/test-only (contracts/gateway/* + docker-smoke.yml): adds HTTP contract tests codifying pre-existing gateway promises (POST /v1/chat/completions and /v1/responses available; unauthenticated non-streaming call → 401 with error.type auth_error), changes ZERO product behavior. The surface it protects is documented CORRECTLY (llm-gateway.mdx:87 direct IDs on chat/completions+responses; errors.mdx:21 "401 to non-chat clients"; M001–M005 → 401 auth_error). platform-engineer state-of-truth confirmed the auth 401 envelope {message,type:'auth_error',code:'manifest_auth'} reaches ONLY non-chat clients — a stream:true/SSE caller gets a friendly HTTP 200 instead (proxy-exception.filter.ts:38-44,140-147); the contract test posts plain JSON so it lands on the 401 path. Two real discrepancies on reference/api.mdx (424-on-exhaustion; envelope example type:"limit_exceeded",code:429) are OUT OF SCOPE (reference topic, lastAuditedAt null) and are page-echoes of falsehoods already raised for llm-gateway.mdx in #53 (findings 2 and 3) → parked in backlog.md, not re-raised. Coherence GREEN. lastAuditedPr advanced 2701→2703.
- 2026-08-15 · backlog lines for reference/api.mdx (424 echo, limit_exceeded/429 envelope) + reference/headers.mdx:47 CONSUMED by the #53 batch fix — same falsehoods, same decision, fixed in the same PR to honor never-halfway.
- 2026-08-15 · WRONG · errors.mdx + errors/M200.mdx · M200 documented as HTTP 429, platform returns HTTP 200 friendly assistant message (live-reproduced) + metric list wrongly included message-count (metrics are cost|tokens only) · status: accepted (Seb chose docs-fix over platform change; option B declined) → fixed with #58 findings · https://github.com/mnfst/docs/issues/52
- 2026-08-15 · WRONG · providers/api-key-providers.mdx + subscription-based-providers.mdx + local-models.mdx + custom-providers.mdx + errors/M100,M101,M102 · connect steps send the user to the Routing page; connection moved to the Providers sidebar section (Usage-based / Subscriptions / Local) in platform PR #2207 · status: accepted (option 1, batch 2026-08-18, live-verified on a local instance: sidebar Providers section, Usage-based cards + Connect, Subscriptions direct form, tile-free Routing page) → FIXED, docs PR #73 merged 2026-08-18 (issue #67 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/67 (finding 1)
- 2026-08-15 · MISSING-CASE · providers/subscription-based-providers.mdx + api-key-providers.mdx · model lists are cached per connection and only change on connect or an explicit "Refresh models"; the Kimi Coding Plan renamed kimi-k3 to k3 and added k3-256k + kimi-for-coding-highspeed, so existing connections serve a dead id · status: accepted (option 1, batch 2026-08-18) → FIXED, docs PR #73 merged 2026-08-18 (ce48b7f); text live at providers/api-key-providers.mdx:73-77 and the Kimi Note at subscription-based-providers.mdx:35-40. Bookkeeping corrected 2026-08-19: this line read "PR pending" for a merged fix, and the 2026-08-19 draft published that staleness as fact until round 3 of verification caught it · https://github.com/mnfst/docs/issues/67 (finding 2)
- 2026-08-15 · OBSOLETE · providers/api-key-providers.mdx · supported-providers table and key-prefix table are missing Meta (new API-key provider, prefix LLM_, min 20 chars, platform PR #2702/#2717) · status: accepted (option 1, batch 2026-08-18; Meta card seen live on Usage-based; prefix row justified: LLM_ IS enforced by the form, unlike the reversed Bedrock/NVIDIA rows) → FIXED, docs PR #73 merged 2026-08-18 (issue #67 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/67 (finding 3)
- 2026-08-15 · IMAGE · providers/api-key-providers.mdx · screenshot of Providers → Usage-based under the "Add a provider" steps · status: accepted (FULFILLED — screenshot captured on a live self-hosted instance and shipped as images/providers-usage-based.png in the same PR; supersedes issue #55 findings 5-6 as recorded) → FIXED, docs PR #73 merged 2026-08-18 (issue #67 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/67 (finding 4)
- 2026-08-15 · issue #55 findings 5 and 6 (IMAGE, both still `raised`) are SUPERSEDED by issue #67 finding 4: both ask for shots of screens the platform removed (the Routing-page provider tile grid, RoutingModals.tsx:326; the two-tab API Key / Subscription panel, ProviderSelectContent.tsx:304-305). No reversal derivation owed: neither was ever accepted or rejected, so no recorded position flips.
- 2026-08-16 · SCOPED RUN (reference (api, headers, environment variables, glossary) · cron rotation, autonomous, no human) · pages reference/{api,headers,environment-variables,glossary}.mdx · window: full reference surface at platform main 633455b98 + the ~60-day PR set on that surface (#2540 capabilities param, #2566 cost param, #2678, #2565, #2485, #2668, #2569, #2623/#2628 credits-gateway env rename, #2697 pool default, #2539, #2578, and the load-bearing historical #1407 that removed the 424 sentinel on 2026-03-30) · 10 findings filed as issue #68; lastAuditedPr untouched (scoped run), topic stamped 2026-08-16 (first-ever audit of this topic, lastAuditedAt was null). Categories: WRONG ×7, MISSING-CASE ×3. reference/telemetry.mdx deliberately EXCLUDED (owned by the request-logs topic in topics.json). Full loop ran (platform-engineer truth → docs-auditor cross-check → tech-writer drafts → issue-verifier fresh-eyes adversary). Verifier returned PASS-WITH-EDITS and caught TWO factual errors in visible text that all three earlier stages had missed: (a) finding 9's title claimed "failed requests carry no routing headers at all" — false, provider-error and exhausted responses DO carry them (proxy-response-handler.ts:309,404); only pre-routing rejections (auth/quota/gateway-429/internal) carry none, and the wrong version contradicted finding 4 step 5 inside the same issue; (b) finding 3 claimed "the only 429s come from the gateway caps" — false, an upstream provider 429 is forwarded with its real status and full envelope (proxy-response-handler.ts:308-310). Both fixed before filing, plus 7 line-number corrections and the missing "## Findings" skeleton heading. Also killed invented vocabulary in visible text ("the management API" → "the dashboard's own API calls") and tightened finding 8 option 2 back inside scope. Four owned backlog lines consumed (see backlog.md). Coherence GREEN (48 pages, 56 vars). No live reproduction: no harness key exists on the VM, so evidence is code-cited only.
- 2026-08-16 · WRONG · reference/api.mdx:212,214 + reference/environment-variables.mdx:38-39 · the documented gateway rate-limit override does nothing: the proxy controller carries @SkipThrottle() (proxy.controller.ts:119,123) so the throttle env pair (app.module.ts:78-81) governs only Nest-routed dashboard controllers; real gateway caps are hardcoded constants 200/min per tenant (M201), 500/min per IP (M202), 10 in flight (M203) with no env/ConfigService read in proxy-rate-limiter.ts:4-9; the page's "100 req/60s per harness, override with the two vars" is wrong in both directions, and Cloud plans move only the MONTHLY quota (plan-limits.ts:9,11-16). NOT a re-raise of #58 finding 2 (that is errors/M201-M203.mdx saying the caps are unconfigurable; this is the opposite-side defect on the pages that advertise the knob) · status: accepted (option 1, batch 2026-08-18; today's truth documented — RATE_MAX_REQUESTS env overrides exist in still-open platform PR #2724, docs follow once released) → FIXED, docs PR #74 merged 2026-08-18 (issue #68 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/68 (finding 1)
- 2026-08-16 · WRONG · reference/api.mdx:6,204,208 + reference/headers.mdx:47 + reference/glossary.mdx:53 · "424 on fallback exhaustion" on three in-scope pages; no production path emits 424 since PR #1407 (2026-03-30), exhaustion preserves the PRIMARY provider's status (proxy.service.ts:1258,1265) and signals via the exhausted header (proxy-response-handler.ts:403-405); api.mdx:208's "the only status that does not trigger a fallback" describes a rule that does not exist (fallback-status-codes.ts:1-3 is >=400, no exclusions). Fix caveat recorded: the body code is providerCode ?? 'fallback_exhausted' (:410), so it is NOT always fallback_exhausted. Page-echo of #53 finding 2 (llm-gateway.mdx), different pages, named. Consumes backlog line of 2026-08-10 · status: accepted (batch 2026-08-18; api.mdx + headers.mdx halves were already fixed by merged PR #62, glossary.mdx:53 fixed now) → FIXED, docs PR #74 merged 2026-08-18 (issue #68 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/68 (finding 2)
- 2026-08-16 · WRONG · reference/api.mdx:191-197,205 · the only error example on the API page shows type "limit_exceeded" + numeric code 429; code is string|null (errorField() nulls non-strings, proxy-error-sanitizer.ts:110) with the numeric status in a SEPARATE field, type comes from a closed OpenAI set, limit_exceeded is a routing reason not a type, and M200 is an HTTP 200 assistant stub (proxy-friendly-response.ts:89,118). Page-echo of #53 finding 3 + #58 finding 1, different page, named. Consumes backlog line of 2026-08-10 · status: accepted (batch 2026-08-18; mostly pre-fixed by merged PR #62, the provider-forwarded-429 clause added now) → FIXED, docs PR #74 merged 2026-08-18 (issue #68 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/68 (finding 3)
- 2026-08-16 · WRONG · reference/headers.mdx:27,30 · X-Manifest-Tier is documented as possibly carrying "a custom tier's name"; a header-tier match reports tier 'standard' + reason 'header-match' and the name is DB-only (resolve.service.ts:325-335, buildMetaHeaders has no name branch); Manifest-blocked 200s report tier 'simple' / model 'manifest' / provider 'manifest' (proxy-friendly-response.ts:59-63), values the doc says cannot occur. Fix constraint recorded: do NOT introduce the complexity-routing tier vocabulary, it is being retired in-product (RoutingDeprecationNotice.tsx:4) · status: accepted (option 1, batch 2026-08-18; LIVE-VERIFIED: a Manifest-answered request returns X-Manifest-Tier: simple, model/provider manifest, reason no_provider/model_not_available) → FIXED, docs PR #74 merged 2026-08-18 (issue #68 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/68 (finding 4)
- 2026-08-16 · WRONG · reference/api.mdx:53 · "the body is forwarded verbatim … all standard OpenAI fields pass through" false: 8 OpenAI-only fields are stripped outside the openai/openrouter passthrough set (provider-client-converters.ts:98-107,113,372), max_tokens is renamed both directions, DeepSeek clamps to 8192, cache markers are injected and the payload is converted whole per target (provider-client.ts:572,611,647,724,729), and harness param defaults are merged into every routed attempt (proxy.service.ts:304-317,328-338). The page naming `stream` as passing through makes the sibling stream_options drop maximally surprising · status: accepted (option 1 compact, batch 2026-08-18) → FIXED, docs PR #74 merged 2026-08-18 (issue #68 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/68 (finding 5)
- 2026-08-16 · WRONG · reference/environment-variables.mdx:32 · the bind-interface row tells Docker users to open the listener for LAN (a no-op: the shipped image already sets it, docker/Dockerfile:78) and to restrict a container by hand-editing the compose ports line (wiped by install.sh --upgrade, which mv's over docker-compose.yml at :239-251 and never touches .env); real reachability is the compose publish line (docker-compose.yml:50) driven by the two commented host-mapping vars (.env.example:28,31). Page-echo of #56 finding 1 (self-hosted.mdx:208), different page, named. RESOLVED stage-1 uncertainty: the container binds 0.0.0.0 · status: accepted (already fixed by merged PR #65 — BIND_ADDRESS row re-pointed and HOST_BIND_ADDRESS row added; nothing left to do) → FIXED, docs PR #74 merged 2026-08-18 (issue #68 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/68 (finding 6)
- 2026-08-16 · WRONG · reference/headers.mdx:17 (+ api.mdx:61 example) · the Anthropic version header is marked "Required for /v1/messages. Forwarded to the upstream" — both halves false: zero inbound readers/validators in packages/backend/src (all 7 hits are outbound injection of the hardcoded 2023-06-01), and the gateway contract test posts to /v1/messages without it and gets 401 auth, not 400 (routes.test.mjs:85-95). Consumes backlog line of 2026-08-10 · status: accepted (option 1, batch 2026-08-18) → FIXED, docs PR #74 merged 2026-08-18 (issue #68 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/68 (finding 7)
- 2026-08-16 · MISSING-CASE · reference/glossary.mdx:45 + reference/headers.mdx:21 · a matching custom-tier header BEATS an explicit model ID in the body: resolveExplicitModel calls resolveHeaderTier first and returns on match before the catalog lookup (proxy.service.ts:903-905,976-978, rationale :957-959); the common SDK case (mandatory model field + tier header) silently runs on the tier's model and bills it. Rest of the direct-routing definition verified correct (fallback_routes null :1063, scoring bypassed :1057-1072) · status: accepted (option 1, batch 2026-08-18; the parked llm-gateway.mdx:52 sentence fixed in the same PR with the same wording) → FIXED, docs PR #74 merged 2026-08-18 (issue #68 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/68 (finding 8)
- 2026-08-16 · MISSING-CASE · reference/api.mdx:200-206,218 + reference/headers.mdx:23 · the status table and the "every request" header heading assume one client kind: streaming/chat clients get HTTP 200 with the message in the assistant turn instead of 401/402 (proxy-exception.filter.ts:38-44,131-149), M100/M101/M102/M200/M302 are 200 for EVERYONE and appear in no row, and pre-routing rejections (auth/quota/429/internal) carry no X-Manifest-* headers. Page-echo of #58 finding 3 (errors.mdx), different page, lower severity. Verifier-corrected: provider-error and exhausted responses DO carry the headers (proxy-response-handler.ts:309,404) · status: accepted (option 1, batch 2026-08-18; headers heading retitled to routed requests) → FIXED, docs PR #74 merged 2026-08-18 (issue #68 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/68 (finding 9)
- 2026-08-16 · MISSING-CASE (low severity, honest leave-as-is option 3) · reference/api.mdx:80 · "any model ID not in this harness-specific list returns M302" is a false universal — a provider-qualified/inferable ID still routes through connected credentials before discovery learns it (proxy.service.ts:961-964,990-999), M302 only on the null path (:1438). Third page carrying the concept after llm-gateway.mdx:89 (#53 finding 4) and self-hosted.mdx (#49 finding 3, accepted); named as such. Harm is missed capability, not failure · status: accepted (option 1, batch 2026-08-18) → FIXED, docs PR #74 merged 2026-08-18 (issue #68 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/68 (finding 10)
- 2026-08-16 · cleared near-findings from the reference audit (recorded so they are not re-derived): the model-list capabilities/cost query params are CORRECT on every falsifiable point (proxy.controller.ts:104-116,150-168,190,194); X-Manifest-Confidence/-Specificity set but unlisted is acceptable curation; the pool-default and gateway-tester-leak rows stay #48 findings 8/10 (re-verified unchanged, NOT re-raised); the stream-idle-timeout var is absent from the page but also absent from the Docker env example, so nothing is used-but-undefined; the autofix-timeout default 10000 is right; the recordings-retention default 365 is right FOR THIS PAGE'S self-hosted audience even though the code default is plan-derived; PORT/NODE_ENV/BETTER_AUTH_URL/recordings-path documented as the bundled compose's EFFECTIVE values, upholding the 2026-08-12 verdict; api.mdx:26 pointing at the Harnesses page for a key is imprecise (key lives at the harness's Settings tab, Settings.tsx:247-311) but lands nobody on a dead screen; the glossary describing only Default+Custom tiers is correct under "less beats confusing" since complexity routing is being retired; the session-key row is true as written (the momentum/cache cliff would need a concept no page defines); the content-type "Required" row is true in practice (body parsing is content-type driven, main.ts:206-207); api.mdx:184 streaming/fallback is correct; glossary "Up to 5 models" is true (routing.dto.ts:201-202,212-213). INTERNAL-LEAK sweep of the four pages for every codename in the stage-1 internal-tools table: 0 hits beyond the already-open #48 finding 10 rows.
- 2026-08-15 · cleared near-finding (no issue): platform PR #2700 made a stale fallback entry deletable (a tier/fallback row pointing at a de-listed model or disconnected provider used to fail with "Cannot resolve fallback model ..." on every save). llm-gateway.mdx:110-124 never claimed otherwise, so the platform moved TOWARD the docs. Recorded so a future run does not re-derive it.
- 2026-08-17 · WRONG · reference/telemetry.mdx:14 · "Inline image data is stripped out" reads as covering stored logs; the image redaction only feeds the throwaway copy used to pick a model (proxy.controller.ts:382, proxy.service.ts:249,266) and a stored body is kept verbatim, uncapped, unscrubbed (attempt-recording-capture.ts:19-54; 2 MiB spec) · status: accepted (option 1, batch 2026-08-18) → FIXED, docs PR #75 merged 2026-08-18 (issue #69 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/69 (finding 1)
- 2026-08-17 · MISSING-CASE · request-logs.mdx:57-67 · the retention table promises Pro 365 days, but the plan query counts a tenant as Pro only while the subscription is active or in trial (request-recording-retention.service.ts:27-59); canceled/payment-failed drops to the 7-day bucket and the 02:00 sweep applies it retroactively over all history · status: accepted (option 1, batch 2026-08-18) → FIXED, docs PR #75 merged 2026-08-18 (issue #69 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/69 (finding 2)
- 2026-08-17 · MISSING-CASE · request-logs.mdx "Self-hosted storage" · a half-configured or mistyped storage setting yields no backend, silently: boot warn only, the per-harness switch still reads on and still confirms, the log tabs are simply absent, and no endpoint or frontend read exposes storage availability (request-recording-storage.service.ts:46-107,216-229; attempt-recording.service.ts:17-19; single read at proxy.controller.ts:273) · status: accepted (option 1, batch 2026-08-18) → FIXED, docs PR #75 merged 2026-08-18 (issue #69 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/69 (finding 3)
- 2026-08-17 · WRONG · observability.mdx:25 + request-logs.mdx:28 · "zero attempts" for a Manifest-blocked request is false: the single blocked-row writer persists one attempt row for M100/M101/M200/M201-M203/M204/M300/M500/M004 (proxy-message-recorder.ts:725-737,803), so the Attempts list shows ONE entry under the requested model (or No provider) with no log tabs; only the four auth codes write nothing, and those never reach the dashboard · status: accepted (option 1, batch 2026-08-18) → FIXED, docs PR #75 merged 2026-08-18 (issue #69 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/69 (finding 4)
- 2026-08-17 · MISSING-CASE · reference/telemetry.mdx:42-45,51 · the field table does not match the payload a private collector receives: the agent-platform grouping emits composite <category>:other keys (payload-builder.service.ts:106-133), the provider/tier/auth groupings each carry an unnamed "unknown" bucket plus an "other" collapse (:195-214), and the message counter is one per provider attempt plus blocked requests, not per request (:145-155) · status: accepted (option 1, batch 2026-08-18) → FIXED, docs PR #75 merged 2026-08-18 (issue #69 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/69 (finding 5)
- 2026-08-17 · OBSOLETE · reference/telemetry.mdx:21-22 · the Note still tells users to turn "message recording" off on the Settings page; the shipped control is the Logs section's Enable logs switch, and :16 of the same page already says Settings → Logs. Verified NOT a re-raise: the #49/PR #50 rename sweep (58ca5c2) does not touch this file, and the only two commits that ever touched it changed :16 alone · status: accepted (option 1, batch 2026-08-18) → FIXED, docs PR #75 merged 2026-08-18 (issue #69 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/69 (finding 6)
- 2026-08-17 · IMAGE · request-logs.mdx:38 · the Messages tab, the screen the feature exists for, has no screenshot; the existing drawer image shows the Details tab. Both existing images re-opened and confirmed CURRENT, no replacement needed · status: accepted (FULFILLED — Messages-tab screenshot captured on a live seeded instance, shipped as images/request-logs-messages.png in the same PR) → FIXED, docs PR #75 merged 2026-08-18 (issue #69 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/69 (finding 7)
- 2026-08-17 · SCOPED RUN (request logs · cron rotation, autonomous, no human) · pages request-logs.mdx, observability.mdx, reference/telemetry.mdx · window: the full logs + data/telemetry surface at platform main 633455b98 (v6.18.0) plus the PRs that built it (#2317 body-storage removal shipped in v6.12.0; #2565 per-attempt logs + storage backends, #2627 default-on for new harnesses, #2678 API surface on the request, all v6.18.0) · 7 findings filed as issue #69; lastAuditedPr untouched (scoped run), topic stamped 2026-08-17. Categories: WRONG ×2, MISSING-CASE ×3, OBSOLETE ×1, IMAGE ×1. reference/telemetry.mdx audited for the first time (deliberately excluded from the 2026-08-16 reference run as belonging to this topic) and produced 3 of the 7. Full loop ran (platform-engineer truth → docs-auditor cross-check → tech-writer drafts → issue-verifier fresh-eyes adversary). Verifier returned PASS-WITH-EDITS, killed nothing, and caught TWO factual errors in visible text: (a) the header line claimed all four PRs shipped in v6.18.0 — the body-storage removal is v6.12.0; (b) finding 4's suggested wording said a blocked attempt is logged as "No provider", false when the caller named a model (the row keeps the requested model, proxy-message-recorder.ts:775). Also stripped raw subscription-status literals from finding 2's visible journey and wording, fixed 6 line numbers, corrected an option count, added two missing evidence citations, and folded the trailing cleared-items paragraph into a details block. Coherence GREEN (48 pages, 56 vars). No live reproduction: still no harness key on the VM.
- 2026-08-17 · cleared near-findings from the request-logs run (recorded so they are not re-derived): the upgrade default (every pre-existing harness has logs OFF after v6.18.0 — the re-add migration wrote false into all rows, 1801300000000:7-9, and the default-flip migration does not backfill, 1801500000000:6-8) is REAL but stays unraised, because the accepted #45-finding-2 wording gives the path to check and AUDIENCE editorial principle 1 cites this exact feature's removal/re-add history as the thing not to narrate; the ephemeral-filesystem list (request-logs.mdx:122) re-verified unchanged and NOT re-raised (#48 finding 6 still open); the retention-override var outside a toggle block self-scopes in its own sentence, not a ban-3 leak; the recordings volume name is correct against the pinned compose name (the platform's own operator README disagrees, see backlog); non-streamed responses storing the upstream rather than the client-facing body is already framed as provider-facing at :24; cooldown-skipped fallback attempts having no log tabs is covered by the existing tabs rule; requests rejected for a bad key never appearing on the Requests page is fine (explicit 401 to the caller, and the one case a user hits unknowingly, an expired key, IS recorded); the "expired" state being invisible after a sweep is folded into finding 2 rather than raised alone; the 15-field count, the opt-out example value, and the install-id description are all correct. INTERNAL-LEAK sweep of the three pages for every codename in the stage-1 internal-tools table (both service codenames, the internal push secret + endpoint + header, the healing host and its three internal vars, the credits gateway host, the public-stats flag, six dev seeder credentials, six E2E knobs, the two edge headers, the internal worktree path, the compose marker, the advisory lock key): 0 hits on all three pages, and 0 hits in the filed issue body.
- 2026-08-17 · FULL RUN (cron, autonomous, no human) · window: merged PRs since #2717, cross-checked by MERGE TIME not number per the 2026-08-15 lesson (`mergedAt > 2026-08-14T07:29:59Z` returns exactly one PR) = only #2720 · CLEAN — 0 findings, no issue filed. #2720 "Remove the $25 Gemini credit user-discovery offer" is a pure deletion (10 files, +5/−668, zero product code added; the +5 is a changeset): it removes the Overview page's promotional card ("🎁 Talk to us and get $25 of Gemini credit" + a booking link) and the dialog that auto-opened after the harness list loaded, plus their tests, their image asset and their CSS. The offer was ungated (both deploy modes, any harness count) and its modal gate was a post-checkout redirect query flag, NOT a plan check — so the PR body's "every non-Pro user" is inaccurate, Pro users saw it too. No code path ever granted $25 of anything; fulfilment was an external booking page. Full loop ran (platform-engineer truth → docs-auditor cross-check → issue-verifier fresh-eyes adversary); all three concurred SILENCE. The docs never described the offer: a corpus grep over *.mdx/*.json/*.js/*.md for the offer's copy, the promo component names, the credit amount and the booking host returns zero hits describing it, and the two prior grep hits are false positives (the navbar CTA is a `navbar.primary` button on a DIFFERENT booking slug carrying no credit promise, live-checked HTTP 200; `providers/custom-providers.mdx:43` "## Model discovery" is the `GET /v1/models` probe). Under the AUDIENCE coherence invariant this sits cleanly on the "don't talk about it at all" side — no halfway state, nothing stranded. IMAGE angle attacked and closed: the verifier opened all 9 files in `images/` (6 referenced, 3 orphaned) and NONE shows the Overview page, so no screenshot could contain the removed element. CSS-collateral angle closed: every removed selector is scoped to the two deleted components' own class families; surviving modals use a different one. CASE-MATRIX collapses on every dimension (removal uniform across cloud/self-hosted, fresh/upgrade, agent count, activation; orphaned browser-local dismissal keys have no user-visible effect). Adjacent mechanisms confirmed UNTOUCHED and their doc sentences still true: the managed free-Gemini tile (`providers/api-key-providers.mdx:42`) and the autofix sidebar card's session-scoped dismissal (`autofix.mdx:89`). No prior decision reversed or reopened — decisions.md carries no line about this offer, so no derivation is owed. INTERNAL-LEAK sweep of the whole corpus for every codename in the stage-1 internal-tools table (the autofix patch-promotion codename + its DB column prefix, the credits gateway host and its four env vars, the internal error-pages controller path + its discovery service + its dev seeder, the PR-review bot): 0 hits in any *.mdx, in docs.json, in scripts/, in style.css. Coherence GREEN (48 pages, 56 vars). Three backlog lines parked (two platform-side, one harness-maintenance). lastAuditedPr advanced 2717→2720; topics.json untouched (full run, not a rotation).
- 2026-08-17 · FULL RUN (cron, autonomous, no human) · window: merged PRs since #2720, cross-checked by MERGE TIME per the 2026-08-15 lesson (`mergedAt > 2026-08-17T08:16:26Z` returns exactly one PR) = only #2727 · 5 findings filed as issue #70; lastAuditedPr advanced 2720→2727; topics.json untouched (full run, not a rotation). Categories: WRONG ×2, MISSING-CASE ×2, IMAGE ×1. #2727 "restore the email provider form for self-hosted installs" is frontend-only (new EmailProviderSection.tsx, Limits.tsx +76/−5, plus tests; zero backend files, no migration, no changeset): it remounts a form that had been unmounted dead code, behind `isSelfHosted() === true` with the read-only cloud panel as fallback. THE PLATFORM MOVED BACK TOWARD THE DOCS — this is the first run where a still-open finding was resolved by the platform rather than by a docs PR (see the supersession line below). Full loop ran (platform-engineer truth → docs-auditor cross-check → tech-writer drafts → issue-verifier fresh-eyes adversary). Verifier returned PASS-WITH-EDITS, killed nothing, and caught TWO factual errors in text that would have shipped: (a) finding 1's visible journey said a failed test shows "the provider's own error message" — false, every provider swallows the upstream body server-side and returns false, so the user sees the generic "Provider returned failure" (email-provider-config.service.ts:256; resend/mailgun/sendgrid.provider.ts); (b) finding 3's option-2 suggested wording said "Alerts only email when a provider is saved on a harness's Limits page", which is false on the very page documenting the env-var alert path (corrected to "Alerts still email if..."). Also killed an evidence overstatement ("there is no configured-by-environment state in the UI" — the rules table's "No provider" tag does read the env, Limits.tsx:41-42,59-60), stripped `EMAIL_*` from finding 2's TITLE while keeping it in the shippable wording (ISSUE-FORMAT element 1 bans code refs in the visible sentence; AUDIENCE's public-surface rule mandates naming it in the doc prose — the #68-finding-1 precedent), removed a relative date from the IMAGE title, rewrote finding 3's journey (steps 1-2 restated the doc instead of describing behavior), fixed 5 line citations, and softened the preamble's claim that the harness "closes" #54 finding 1 (the harness states a premise is gone; only Seb closes). Auditor cleared near-findings (recorded so not re-litigated), all four upheld by the verifier: the self-hosted auto-detection defaulting to CLOUD for a non-container install with no instance-mode var (so the form is unreachable) — real, but every documented install path is containerised or sets the var, and the same doc sentence offers the env path, so nobody is stranded → backlog; the newly-reachable amber "No provider" tag (hasProvider stopped being hardcoded true) — reinforces what observability.mdx:100 already says and blocks nothing (LimitRuleModal declares the prop and never reads it); observability.mdx:42's Limits card promising "email provider setup" outside the deploy toggle (cloud gets a read-only panel) — weakest clearance, defect predates the PR which moved the platform toward the sentence → backlog; fresh-vs-upgrade and plan dimensions COLLAPSE (frontend-only, no migration, no backfill, no plan gate anywhere in packages/backend/src/notifications — the state is only "row exists or not"). INTERNAL-LEAK sweep of the in-scope pages and of the filed issue body: 0 hits (the unused internal release-notification email template with the medical-sounding codename, the operational trigger-check endpoint, the container host aliases, the encryption-key var, the instance-mode var's Kubernetes companion). The EMAIL_* vars appear only inside a self-hosted toggle block and the self-hosted reference section, so ban 3 holds. Coherence GREEN (48 pages, 56 vars). No live reproduction: still no harness key on the VM.
- 2026-08-17 · SUPERSESSION (not a reversal, no derivation owed) · issue #54 finding 1 (2026-08-10, WRONG, observability.mdx:98, status `raised`) is RESOLVED BY THE PLATFORM via PR #2727. Its claim was "the screen does not exist" (the form was unmounted dead code, zero JSX usages); the form is mounted again, so the premise is gone and its proposed fix — strip the Limits-page instructions and leave only the env vars — MUST NOT BE APPLIED, since applying it today would write a new falsehood into the docs. No reversal protocol is owed: the finding was never accepted or rejected, so no recorded position flips (same reasoning as the 2026-08-15 line about #55 findings 5/6). Issue #70 findings 1, 2 and 4 land on the SAME doc line but assert the OPPOSITE-SIDE claims (the screen exists and the paragraph describes it wrongly), which is why closing f1 explicitly in the issue preamble was the precondition for raising them. #54 findings 2, 3 and 4 are untouched by this PR and were not re-raised.
- 2026-08-17 · WRONG · observability.mdx:98 · "Enter the API key, domain, and notification address, then save" does not match the restored form: there is no Save button (the footer is "Send test email" + "Test & Connect"/"Test & Save", EmailProviderModal.tsx:409-425), the sending-domain field renders only for Mailgun (:62,372-393; server-side required for Mailgun only, email-provider-validation.ts:35-37), the notification address is prefilled and optional (:55,110-114,201-202), and handleSave sends a REAL test email first and persists nothing if it fails (:178-185) · status: accepted (option 1, batch 2026-08-18; tiles live-verified, dialog buttons code-cited in the restored component) → FIXED, docs PR #76 merged 2026-08-18 (issue #70 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/70 (finding 1)
- 2026-08-17 · WRONG · observability.mdx:98 · "You can set the same thing with the EMAIL_* environment variables" is false in both directions: the saved dashboard provider WINS for threshold alerts (notification-cron.service.ts:110,139 + notification-email.service.ts:33-49, env path only at :52; same in limit-check.service.ts:121,151), and it covers ONLY alerts — verification, password reset (auth.instance.ts:128,146) and billing mail (billing-email-sender.ts:57,74) are the env's exclusive territory and never read email_provider_configs. The setup block is also blind to the env (Limits.tsx:254-260), so an env-configured install sees empty tiles · status: accepted (option 1, batch 2026-08-18; tiles live-verified, dialog buttons code-cited in the restored component) → FIXED, docs PR #76 merged 2026-08-18 (issue #70 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/70 (finding 2)
- 2026-08-17 · MISSING-CASE · reference/environment-variables.mdx:97,107 · "alerts just don't email" without these vars was TRUE while the form was dead code and is FALSE since #2727 — a provider saved in the dashboard emails alerts with no variable set; the auth half of the note stays true. Page-echo of finding 2 on a second page, shipped together under the never-halfway invariant. IN SCOPE despite belonging to the reference rotation topic: the verifier's ruling is that the test is whether the audited PR flipped the sentence's truth value (it did), not page ownership — which is what distinguishes it from the reference-page echoes parked in backlog by the #2703/#2705 runs · status: accepted (option 1, batch 2026-08-18; tiles live-verified, dialog buttons code-cited in the restored component) → FIXED, docs PR #76 merged 2026-08-18 (issue #70 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/70 (finding 3)
- 2026-08-17 · MISSING-CASE · observability.mdx:98 (read with :73) · the email provider is configured from a per-harness Limits page but is stored ONE ROW PER TENANT (email-provider-config.entity.ts:5 unique index; created_by_user_id is "Audit-only … Never used for scoping" at :13; every read/write tenant-keyed, email-provider-config.service.ts:52,162,164,167; the frontend resource takes no harness argument, Limits.tsx:43), so it is workspace-wide and removing it from any one harness stops alerts for all of them; the removal dialog warns alerts stop without saying for whom (LimitModals.tsx:143-180) · status: accepted (option 1, batch 2026-08-18; tiles live-verified, dialog buttons code-cited in the restored component) → FIXED, docs PR #76 merged 2026-08-18 (issue #70 batch); text re-verified live in the working tree 2026-08-19 · https://github.com/mnfst/docs/issues/70 (finding 4)
- 2026-08-17 · IMAGE · observability.mdx:96-102 · the restored self-hosted email provider block is described in prose only; screenshot of a harness's Limits tab, unconfigured, showing the "Configure email provider" heading and the three service tiles above the rules table. All 9 files in images/ re-opened: none shows the Limits page, so nothing existing is stale. Not a duplicate of #54 finding 4 (Overview-tab shot under the four-card grid) · status: raised · https://github.com/mnfst/docs/issues/70 (finding 5)
- 2026-08-17 · OPEN QUESTIONS (not findings, deliberately kept OUT of the public issue on the verifier's advice, same handling as the 2026-08-07 Railway line) · reference/telemetry.mdx · (a) :30-33 says Manifest Cloud has no separate telemetry report; the platform repo proves only that there is NO deployment-mode gate in the sender (module imported unconditionally, no mode check in any telemetry file), so whether Cloud sets the opt-out var is undeterminable from the code — publishing that doubt would question a privacy assurance on a public repo without being able to resolve it. Ask Seb. (b) :59 says the ingest SHA-256s the sender's IP and discards the original; the sender posts only a content-type header and the JSON body, and no ingest code exists in this repo, so the claim is unverifiable here rather than wrong. Ask Seb before either sentence is touched.
- 2026-08-17 · PLATFORM CHANGE absorbed · observability.mdx (PR #63) · the self-hosted email-provider form is BACK: platform PR mnfst/manifest#2727 remounted the Resend/Mailgun/SendGrid picker on the Limits page for self-hosted (it had been unmounted by #1539's local-mode removal and garbage-collected by #2207 — accidental loss, confirmed by the PR trail). The #54 finding 1 text (env-vars-only) was true from 2026-04-13 until this merge; PR #63's email section now documents the form as the primary path with the EMAIL_* vars as the instance-wide alternative, dashboard config taking precedence (per docker/.env.example). NOT a reversal of the #54 verdict: the platform moved, the docs follow.
- 2026-08-18 · SCOPED RUN (getting started: introduction, first request · cron rotation, autonomous, no human) · page introduction.mdx · window: merged PRs since 2026-07-19 against main fd5a089ae · 4 findings filed as issue #71; lastAuditedPr untouched (scoped run), topic stamped 2026-08-18. Categories: WRONG ×1, MISSING-CASE ×2, IMAGE ×1. Two auditors ran in parallel on complementary lenses (claim-by-claim truth check / gaps-and-cases) and converged on the same cloud-journey defect; the verifier FAILed the first draft on three visible-text errors, all corrected before filing.
- 2026-08-18 · WRONG · introduction.mdx:12,21 · "You choose which model handles each request" paired with "add fallbacks" is false: the two are mutually exclusive. An explicit model takes the explicit branch (proxy.service.ts:903) whose explicitRouting() returns fallback_routes: null (:1058-1072, null at :1066); no re-attachment path exists (effectiveFallbackRoutes :1109-1122, tryFallbackChain :1169-1170). Only `auto`, or no model field at all, reaches tier routing and therefore fallbacks. llm-gateway.mdx:35,75 already state the truth, so the entry page contradicts the feature page · status: raised · https://github.com/mnfst/docs/issues/71 (finding 1)
- 2026-08-18 · MISSING-CASE · introduction.mdx:40,49 · "Sign up, connect a provider, and you're routing" omits the two things the product enforces: creating a harness, which is what mints the mnfst_ key (Welcome.tsx:299-315, agents.controller.ts:104-113; sole onboardAgent caller, and the Playground agent gets no key), and giving the default tier a route (Welcome.tsx:1287-1297 disables Continue at routedProviderCount() === 0). The word "harness" is used three times on the page with no definition and no glossary link. Compounding: no page in the corpus walks a CLOUD user to a first request; the only end-to-end walkthrough is self-hosted.mdx:123 against localhost:2099 · status: raised · https://github.com/mnfst/docs/issues/71 (finding 2)
- 2026-08-18 · MISSING-CASE · introduction.mdx:51 · the page states the free plan's 10,000 routed requests but never says when the count resets, and errors/M204.mdx:26 ("wait for the monthly request quota to reset") does not either; a corpus-wide grep for the reset moment is empty. Truth: UTC calendar month, cleared at 00:00 UTC on the 1st (request-quota-window.ts:11-13, plan.service.ts:199-207), with the dashboard sidebar already showing the live count (Sidebar.tsx:382-425). Not a re-raise of issue #49 finding 6, which was the quota NUMBER and shipped in PR #50; this is the boundary · status: raised · https://github.com/mnfst/docs/issues/71 (finding 3)
- 2026-08-18 · IMAGE · introduction.mdx:38-49 · the page describes cloud signup in prose only and carries no image; screenshot of the guided setup at /welcome on step one, sidebar reading "Setup progress 0/5" (the counter counts COMPLETED steps, Welcome.tsx:751, so it is 0/5 while the Create-harness panel is open and unsubmitted) with the five step labels beside the open panel. All 9 files in images/ re-opened: none shows onboarding, so nothing existing is stale · status: raised · https://github.com/mnfst/docs/issues/71 (finding 4)
- 2026-08-18 · CLEARED, not raised (recorded so it is not re-derived next rotation) · introduction.mdx · (a) the four provider categories at :10,18 vs the two wizard tabs a cloud user meets: custom endpoints connect from inside Usage-based, not from a tab, and all four are genuinely connectable in the deployment each applies to, with the cloud/local divergence corrected on the same page at :51. (b) The Autofix and Request-logs cards vs their diverging defaults (ON-cloud/OFF-selfhosted, autofix.service.ts:143; new-vs-existing harnesses, migration 1801500000000): the cards state a capability, not an activation behavior, and the owning pages carry the toggle. (c) The absence of the deploy-mode toggle on "Cloud or self-hosted?": that section exists to let an undecided reader COMPARE, and a sticky mode toggle would hide half the comparison; it becomes required only if finding 2 option 1 lands. (d) "see the cost of every call" at :12 — soft (flat-fee subscription calls resolve to 0, unknown models to null, cost-calculator.ts:32-61) but settled twice as spirit-correct (2026-08-10, 2026-08-11); re-raising would be oscillation. (e) ":49 everything else is a setting in the dashboard" — the plan quota is not dashboard-configurable, but the very next sentence carries the plan limit, and the caveats bite self-hosted, which this cloud sentence does not address. Left alone under "less beats confusing".
- 2026-08-18 · OPEN QUESTIONS (published as questions in issue #71, deliberately not findings) · introduction.mdx · (a) does cloud provision the managed free model tile for new accounts? The auto-provision path needs the credits master key set and the allowlist to clear (managed-free-providers.ts:49-77); production env is not in either repo. The answer decides whether the introduction may say a new cloud account has something to route to before connecting anything, and it also affects finding 2's wording, since a provisioned tile already clears the provider gate. (b) Is email verification enforced on cloud signup (auth.instance.ts:19-22)? If yes, a "check your email" screen sits between signup and the guided setup and belongs in finding 2's journey. Both are undeterminable from the code, same handling as the 2026-08-07 Railway line and the 2026-08-17 telemetry line: ask Seb.
- 2026-08-18 · FULL RUN (cron, autonomous, no human) · window: merged PRs since #2727 = only #2730 · 6 findings filed as issue #72; lastAuditedPr advanced 2727→2730; topics.json untouched (full run, not a rotation). Categories: WRONG ×3, MISSING-CASE ×3. #2730 "docs(docker): fix the LAN instructions and auto-sync the Hub description" touches two files (the operator README's LAN step, and a new CI workflow republishing that README to the image registry page) and changes ZERO product code. THE PR'S OWN SURFACE IS CLEAN: the docs shipped the same fix yesterday in #65, and the LAN section + the two host-mapping reference rows re-verify TRUE against the compose publish expression. The six findings come from sweeping the SAME PAGE's install/port/upgrade claims, which the thin window made room for; findings 1-3 predate #2730. Full loop ran (platform-engineer truth → TWO docs-auditors in parallel on complementary lenses, claim-by-claim truth check / gaps-cases-audience → tech-writer drafts → issue-verifier fresh-eyes adversary, TWO rounds). Verifier FAILed round 1 on four factual errors in visible text and one wrong code citation, FAILed round 2 on a memory-file quote (not the issue), then FILE. Both auditors converged on the version-pinning defect from opposite directions. Coherence GREEN (48 pages, 57 vars) before and after.
- 2026-08-18 · WRONG · self-hosted.mdx:88,100,113,191 · the four `docker run` blocks publish `-p 2099:2099` and pass no `-e PORT`, but the image sets no PORT (Dockerfile:77-78 sets only NODE_ENV and the container bind address) and the backend falls back to 3001 (main.ts:212), so the published port has no listener: the container is healthy (its own healthcheck reads PORT||3001, Dockerfile:105) and the dashboard never answers. 2099 comes only from docker-compose.yml:61. Verified twice against the PUBLISHED image, not just the Dockerfile: the latest amd64 config blob's Env is PATH/SSL_CERT_FILE/NODE_ENV/bind-address only, ExposedPorts 2099. Second half: the same commands carry no interface in the mapping, so they publish on every interface while :17 describes the compose install as loopback-only, and the setup routes are public with no origin check (setup.controller.ts:10-38), so an unclaimed instance can be claimed by anyone who reaches it. NOT a reversal of the 2026-08-12/2026-08-16 clearances of the `PORT | 2099` reference row: those were reasoned about the compose path, which still holds; this is the other path · status: raised · https://github.com/mnfst/docs/issues/72 (finding 1)
- 2026-08-18 · WRONG · self-hosted.mdx:252-256 · the note tells users to run `bash install.sh --upgrade`, but the install the page recommends at :27 is a process substitution that writes nothing to disk, and the installer never copies itself into the install directory (it writes only the compose file at :271 and the env file at :279,299-334; no `cp "$0"` anywhere). Downloading it is still not enough for a `--dir` install, which the page advertises at :38: the flag resolves against $HOME/manifest (install.sh:46) and dies "No installation found at ..." (:114-115). Consequence: the recordings volume is never added and log bodies keep dying with the container, the one thing the note exists to prevent. NOT a re-raise of the 2026-08-12 clearance of this note (decisions.md:50), which verified its version boundary and volume name, both still correct, and never tested whether the command runs · status: raised · https://github.com/mnfst/docs/issues/72 (finding 2)
- 2026-08-18 · WRONG · self-hosted.mdx:249 (read with :214 and :232-233) · the page tells users to pin the version by editing the compose file, which holds no tag to edit (the image line is templated from an env var, docker-compose.yml:48) and which the installer upgrade replaces after backing it up (install.sh:240-251), contradicting :214 on the same page; the supported knob (the image-tag env var, .env.example:37-38, default latest) appears in ZERO .mdx files. Compounding: the tag examples are 5.46.0/5.46/5 while the current release is 6.18.0, which the page itself cites at :252, so a reader who copies the example pins to a dead major. Confirmed the app never reads that env var: the dashboard version badge is a build-time define from package.json (vite.config.ts:61), and compose interpolation is its only consumer · status: raised · https://github.com/mnfst/docs/issues/72 (finding 3)
- 2026-08-18 · MISSING-CASE · self-hosted.mdx:196,217 · both mentions of the invalid-origin login error are prospective, and no page (zero corpus hits outside those two lines, including the 17 error pages) describes the state the user is IN once it happens. Journey: the setup routes are public with no origin check (setup.controller.ts:10-38), so from a second machine the setup screen loads and really creates the admin (setup.service.ts:145, server-side signUpEmail, never sees a browser Origin), the browser sign-in that follows is rejected and the code silently redirects to the login page (Setup.tsx:44-52) where the same credentials fail, and setup refuses to run again because a user now exists (setup.service.ts:120-138). Trusted origins in production are the public-URL var plus the CORS var, unset in the bundled compose (auth.instance.ts:46-68; compose hardcodes production at :80) · status: raised · https://github.com/mnfst/docs/issues/72 (finding 4)
- 2026-08-18 · MISSING-CASE · self-hosted.mdx:214 (read with :244-247) · installs exposed on the LAN by the OLD instruction (hand-editing the compose ports line, which both the docs and the installer banner taught until 2026-08-18) go dark at the next `install.sh --upgrade`: that path downloads a fresh compose file and replaces it after copying it to a timestamped backup (install.sh:236-256, :248 date -u), warning only on the FIRST managed upgrade (:192,195). The page has no migration line, and its Upgrading section documents only the image-pull path, which leaves a hand edit intact, so the two documented upgrades differ on exactly this point while the page says the same thing about both. NOT a re-raise of #56 (the LAN section naming a ports line that does not exist, accepted, shipped in #65): this is the migration of installs already exposed the old way · status: raised · https://github.com/mnfst/docs/issues/72 (finding 5)
- 2026-08-18 · MISSING-CASE · self-hosted.mdx:217 · the LAN step tells users to change the public-URL var and states no consequence: that value is the auth layer's baseURL (auth.instance.ts:105), which builds the callback URLs for the three social sign-ins (:114,154-166), so a configured Google/GitHub/Discord login breaks until the redirect URI is updated in the provider console (Google and GitHub answer redirect_uri_mismatch, Discord "Invalid OAuth2 redirect_uri"). The reference page already carries the callback formula at :123; the step points at neither. Provider SUBSCRIPTION OAuth is unaffected (per-provider constant redirect, redirect-pkce-oauth.base.ts:131-132,164). The page already knows this consequence class at :199, but only for the legacy-port case · status: raised · https://github.com/mnfst/docs/issues/72 (finding 6)
- 2026-08-18 · KILLED BY THE VERIFIER, recorded so it is not re-derived · self-hosted.mdx:217 · the second half of what became finding 6 claimed the LAN change STARTS the HSTS boot warning. False: the warning's gate is the container bind address (main.ts:228-239), which the image pins to all-interfaces (Dockerfile:78) with no compose override, so all four conditions already hold on every plain-HTTP Docker install with the default public URL. The warning fires before the LAN change and after it. The suppression var's rows (reference/environment-variables.mdx:37, self-hosted.mdx:320) are correct and stay
- 2026-08-18 · cleared near-findings from the PR #2730 run (recorded so they are not re-derived): self-hosted.mdx:214's "hand edits do not survive an upgrade" is OVERSTATED (true for the installer path, false for the image-pull path the same page documents) but stays unraised on its own, because it errs toward the safe knob and misleads nobody into a broken state; its real cost is the contradiction with :249, which finding 3 resolves. The blank-dashboard-over-a-LAN-IP workaround still carried by the platform's operator README is DEAD (the mixed-content upgrade was disabled in main.ts:66-71 and shipped in 5.47.2, two majors back) and narrating it would be exactly the history-narration AUDIENCE principle 1 forbids. The host-side-only port var has no reference row and needs none: it appears in zero .mdx files, so the never-halfway invariant holds and the 2026-08-12 backlog line anticipating it should NOT be re-parked. The public-URL default shown as localhost:${PORT} vs the code's nested host-port fallback is identical on the documented PORT-only path (upheld from 2026-08-12). The backup section's volume names and mount points are CORRECT against the pinned compose names, and notably the platform's own operator README is the one that is wrong there. The system-API-key reference row reads like a do-not-touch while the compose file describes a user-settable knob: wording only, nobody blocked, outside this run's variable set. INTERNAL-LEAK sweep of the two in-scope pages, the ten deploy pages and the filed issue body against the stage-1 internal list (the healing service codename and its three vars and its DB column, the gateway-tester codename, the dev sign-in credential pair, the two installer CI override vars, the migration recovery flag, the embedding flag, the registry CI secret names): 0 hits. The issue's platform-side section names the registry sync FAILURE, never a credential, and the PR's own public body names the secret anyway
- 2026-08-18 · SCOPE DECISION (upheld by the verifier) · a seventh candidate was found and deliberately NOT raised: nine deploy pages tell users to move to a custom domain and frame it as an OAuth concern only, never saying the public-URL var must follow the domain or every login on it fails, with the setup screen unusable as a way back in once an admin exists. Real, but nine pages sharing one variable name is the deploy topic's own audit, not a window whose single PR touched an operator README and a CI workflow. Parked in backlog.md with the nine line numbers, the four verbatim-vs-reworded split, and the koyeb exception (its public URL is templated to the platform domain, so it has no such note and is not an omission) · owner: deploy platforms topic
- 2026-08-18 · MISSING-CASE · reference/api.mdx:33,37 · the endpoints table's Responses row plus the format-translation sentence under it are silent on the one thing that breaks a Responses client: translation carries messages, tools and tool results, not Responses-only conversation state. A `/v1/responses` turn is served natively only when the resolved endpoint's format is chatgpt (provider-client.ts:273); every other target is rebuilt as a chat body whose allow-list (responses-adapter.ts:115-133) has no previous_response_id, the route validates nothing inbound (proxy.controller.ts:213-219), and Manifest reads the field nowhere (one repo-wide hit, responses-adapter.ts:391, an output hardcoded to null). The reply still looks continuable (status completed, fresh synthetic id at :379/:638, store false at :393), so a threading conversation silently restarts from zero with no error, header or log line. PREDATES the window's PR: #2732 only made the surrounding class quieter, turning a provider 400/422 into a silent drop; the lossy translation shipped with the endpoint itself (first release 5.55.1). Distinct from the still-open #68 finding 5 on the same page (that one is field stripping on /v1/chat/completions at :53), so both should ship in one PR · status: raised · https://github.com/mnfst/docs/issues/79 (finding 1)
- 2026-08-18 · cleared near-findings from the PR #2732 run (recorded so they are not re-derived): the window's PR is a bugfix with NO doc claim to correct, and that verdict stands. A self-hosted version note ("upgrade past the release that fixes this") is NOT shippable: the fix is unreleased, no tag contains the merge commit, the next number is inference, and after the cut it would be pure bug-history narration, which AUDIENCE principle 1 forbids by name. llm-gateway.mdx:87 ("a direct model ID works on all three endpoints") is CLEAN: direct routing is identical on the three endpoints and the translation loss is not a routing property. The Responses row's "Use it for" column is CORRECT, verified against the platform's own responses-only model pattern one family at a time, Codex exception included. The "you must use a Responses client for those models" reading of that row was CLEARED, not raised: the platform transparently swaps those models onto the provider's Responses endpoint even when the client posted to /v1/chat/completions, the column header is a recommendation not a requirement, and llm-gateway.mdx:87 already answers it. Reasoning items were deliberately excluded from the finding: dropping them is the only correct mapping to a chat target, no doc sentence claims otherwise, and the word has zero hits in the whole corpus. CASE-MATRIX sweep: deployment, install state, agents and activation all collapse (translation is stateless, no env var, no setting, no migration, no per-agent read); the only dimension that does not collapse is providers, which is exactly what the finding raises. INTERNAL-LEAK sweep of the two in-scope pages and of the filed issue body against the stage-1 internal list (the healing service codename, its four env vars including the healing endpoint's internal subdomain, and the automated PR-reviewer bot named in the PR description): 0 hits. Noted for future runs: stage 1 listed Autofix itself as internal, which is WRONG, AUDIENCE.md names it as product vocabulary and /autofix is a published page
- 2026-08-18 · parked items consumed with the #68 PR: llm-gateway.mdx:52 gains the header-beats-model-ID sentence (same wording as #68 finding 8), self-hosted.mdx optional-env list drops 'rate limits' (same root as #68 finding 1).
- 2026-08-19 · SCOPED RUN (autofix · cron rotation, autonomous, no human) · pages autofix.mdx + errors.mdx · window: full feature surface at main 0d244d0c (the Autofix code itself unchanged since ecb3c8d34, 2026-08-06) · 3 findings filed as issue #80; lastAuditedPr untouched (scoped run), topic stamped 2026-08-19. Categories: WRONG ×1, IMAGE ×1, MISSING-CASE ×1. Full loop ran (platform-engineer truth → docs-auditor cross-check → tech-writer drafts → issue-verifier fresh-eyes adversary). The verifier FAILed the first draft on three visible-text defects and one memory contradiction, all fixed before filing: finding 1's title over-generalised ("two thirds of the cards don't link" when two of three documented cards do link), finding 1 option 3 carried a false trade-off (the recovered-by-Autofix definition sits ABOVE the table at :141, not below it), finding 2 broke the IMAGE shape (no Page line, and four image FILE NAMES in the visible block — the same gate that blocked #56/#57/#58), and finding 3's journey steps 1 and 5 had no code reference in the folded evidence. Verifier also corrected three cited line numbers by one or two (:110→:111, :132→:131, :143→:145) and strengthened finding 3 by ruling out an over-claim: two other forward results also lack the retry closure (proxy-fallback.service.ts:392-395, :412-419) but neither carries a wire body, so neither reaches the gate and the "one provider" framing survives.
- 2026-08-19 · WRONG · autofix.mdx:153,157 · the Overview cards table names "Request success rate", a label that is not on the screen (it renders "Success rate", AutofixKpiCards.tsx:111), and claims "Each card links through to the matching filter on the request log" when the success-rate card is a plain div with no linkProps · status: raised · https://github.com/mnfst/docs/issues/80 (finding 1)
- 2026-08-19 · IMAGE · autofix.mdx:69-71,91-93,95-97,143-145 · all four screenshots still render the retired hyphenated spelling of the product name, dropped platform-side by ecb3c8d34 on 2026-08-06; captions and alt text are already correct, only the pixels are stale · status: raised · https://github.com/mnfst/docs/issues/80 (finding 2)
- 2026-08-19 · MISSING-CASE · autofix.mdx:20-30 · "When it runs" presents the trigger as status-only, but the heal gate also requires a resend closure (proxy.service.ts:457-458) that the Kiro transport never returns (provider-client.ts:286-311), so Autofix is unreachable for a documented provider · status: raised · https://github.com/mnfst/docs/issues/80 (finding 3)
- 2026-08-19 · CORRECTION to the 2026-08-08 record, NOT a reversal (no verdict flips, Seb never saw the point, so no `revised` derivation is owed under the reversal protocol): that run's line reads "dashboard cards" among the items it verified, and issue #80 finding 1 raises those same cards. The clearance was incomplete rather than wrong — it checked what the cards MEASURE, which is still correct, and not their rendered labels or their link behaviour. Neither side moved in between: the doc table dates from ee5b58b (2026-07-29, renamed by 2b3cdaa) and AutofixKpiCards.tsx from c1e7717ed (2026-07-21), touched since only by the spelling commit. Recorded so the next rotation does not read the two lines as contradicting each other.
- 2026-08-19 · cleared near-findings from the autofix run (recorded so they are not re-derived): errors.mdx's total silence about Autofix is CORRECT, not a gap — it catalogs Manifest M-codes, Autofix acts on provider 4xx which carry no M-code, and it changes no M-code's status, delivery or wording; the only adjacency is M302 and :62 is a definition, not the false universal already raised elsewhere. PR #2712 (the only commit touching provider-client.ts since the last autofix audit) forces stream:true on the Responses body for the subscription endpoint (:647-657) and leaves the retry closure untouched — no Autofix effect. PR #2732 touches no Autofix file; its only effect is volume, removing a class of /v1/responses 400s that Autofix would have been asked to repair. The failed-patch error substitution (autofix.service.ts:491-511: when a patch lands and the retry fails, the caller sees the RETRY's error) contradicts no sentence — autofix.mdx:134 scopes its preservation promise to healer failure/timeout, which is exactly where the code preserves the original (:403-406, :419-431, :452-468) — and the request drawer names the substitution in plain words. The notification bell (NotificationBell.tsx:17,153-175) is a second nudge to the same Settings toggle the page documents at :85; no behaviour differs across any matrix dimension, so AUDIENCE principle 1 argues against narrating it. The kill switch's exact-string parse (autofix.service.ts:142: !== 'false', untrimmed, case-sensitive) is mechanism narration, and :172 already prints the exact working value. The card-count gap (3 documented, 5 shipped) is defensible curation on an Autofix page and was folded into finding 1's options rather than raised. No new IMAGE finding for the Overview row: issue #54 finding 4 already asks for that screenshot (accepted, capture pending). The Playground never routing through Autofix corrects no expectation ("playground" has zero hits in the whole *.mdx corpus). The Anthropic subscription extra-usage 400 skip stays settled from 2026-08-08. INTERNAL-LEAK sweep of both in-scope pages and of the filed issue body against the stage-1 internal list (the healing-service codename and its DB column prefix, the scraper codename, the release codename, the internal ingestion module, the three internal wire headers, the internal error-pages write API, the dev-only mock client and its catalog marker, the three removed or never-existing env vars): 0 hits — the one regex match is autofix.mdx:6's frontmatter keyword "self-healing", a generic descriptor and not the codename. All five Autofix env vars sit inside the self-hosted block (:159-180) and each has its row in reference/environment-variables.mdx:90-94, so AUDIENCE rule 3 and the never-halfway invariant both hold. Coherence GREEN (48 pages, 57 vars).
- 2026-08-19 · FULL RUN (cron, autonomous, no human) · window: merged PRs since #2732 = only #2733 "fix: enrich Ollama Cloud models from models.dev" (models.dev provider map + a narrow Ollama release-tag strip; backend only) · 5 findings filed as issue #81; lastAuditedPr advanced 2732→2733, topics.json untouched (full mode with a new PR, no rotation). Categories: WRONG ×2, MISSING-CASE ×3. SCOPE RULING WORTH REUSING: PR #2734 shows as MERGED on GitHub but its base branch is fix/ollama-cloud-models-dev-capabilities, not main, and its merge commit db0fccdfa is not an ancestor of origin/main — it was excluded from the window and the issue says so publicly. The audited PR itself falsified NO docs sentence (no page mentions Ollama Cloud capabilities, modalities or counts); all 5 findings are pre-existing defects on the surface it lands on, which is the #2730/#2732 precedent applied again. Full loop ran (platform-engineer truth → TWO docs-auditors on complementary lenses → tech-writer drafts → issue-verifier, THREE adversarial rounds, two of them FAIL). Coherence GREEN (48 pages, 57 vars). One candidate parked instead of filed (the Refresh-models IMAGE: #2733 gives the user no new reason to press that button, so it is outside the window's causal reach) plus 5 out-of-scope items → backlog.
- 2026-08-19 · MISSING-CASE · providers/api-key-providers.mdx:73-77 (+ subscription-based-providers.mdx:56) · discovery silently DELETES models the public catalog marks tool_call:false (model-discovery.service.ts:330, persisted :345, no env var and no setting), so the served list can be shorter than the provider's own; the page attributes every disappearance to the provider (:75). Live today for OpenAI gpt-3.5-turbo, 39 NVIDIA and 9 Groq entries. In this window because #2733 made the filter reachable for Ollama Cloud for the first time (it removes none of them today: all 20 catalog entries are tool_call:true) · status: raised · https://github.com/mnfst/docs/issues/81 (finding 1)
- 2026-08-19 · WRONG · reference/api.mdx:148,153-154 · the ?cost=true example prices a ChatGPT subscription id at 0.25/2; that connection's parser hardcodes 0/0 (provider-model-fetcher.service.ts:734-735) and enrichment short-circuits on 0>=0 (model-discovery.service.ts:653-659), and 0.25/2 are gpt-5-mini's prices, not gpt-5.4-mini's (live catalog: 0.75/4.5) · status: raised · https://github.com/mnfst/docs/issues/81 (finding 2)
- 2026-08-19 · WRONG · reference/api.mdx:110,122 · the ?capabilities=true example carries the endpoint-support field on an OpenAI model; the single producer in the codebase is the GitHub Copilot parser (provider-model-fetcher.service.ts:748, ids prefixed copilot/ at :744), and the request-time resolver cannot add it (model-capabilities.ts:140-151) · status: raised · https://github.com/mnfst/docs/issues/81 (finding 3)
- 2026-08-19 · MISSING-CASE · providers/local-models.mdx:12 + subscription-based-providers.mdx:33 · "Ollama" names two providers with opposite deployment answers (shared/providers.ts:379-390 localOnly true vs :391-402 localOnly false; the Cloud gate keys off localOnly alone, provider-availability.ts:15-17), and the only page that discusses Ollama sends a Manifest Cloud reader to build a tunnel without saying Ollama Cloud needs none; the subscription row is bare and no page says where its key comes from. The 2026-08-11 providers run checked CATEGORISATION (correctly: sub-only, not local) and not the name collision · status: raised · https://github.com/mnfst/docs/issues/81 (finding 4)
- 2026-08-19 · MISSING-CASE · reference/api.mdx:124 · capability metadata comes from a hardcoded third-party catalog URL (models-dev-sync.service.ts:114, boot + daily cron :224) with NO env var, no opt-out, no redirect and no bundled snapshot (three in-memory maps, :200/:202/:204); failure is log-only (:668,:673, bail at :228) with no user-facing signal, so a self-hosted install behind restricted egress reports far fewer capability fields forever. Contrast documented in code: the sibling parameter catalog IS bundled and offline-safe (provider-param-spec.service.ts:80-82) with two switches (:108,:123). Option 2 would trigger the never-halfway invariant on those two variables · status: raised · https://github.com/mnfst/docs/issues/81 (finding 5)
- 2026-08-19 · CORRECTION, not a reversal, of the 2026-08-16 cleared near-finding at decisions.md:80 ("the model-list capabilities/cost query params are CORRECT on every falsifiable point"). Derivation, per the reversal protocol: (1) the invariants are ISSUE-FORMAT's double-evidence rule and AUDIENCE's coherence invariant, which make a documented response BODY a claim about what the platform emits; (2) the clearance's own citation list is proxy.controller.ts:104-116,150-168,190,194 — the projection only — and the projection IS faithful; issue #81 findings 2 and 3 are about the PROVENANCE of the values the projection receives (the fetcher parsers, enrichModel), in files that pass never opened; (3) no expressed position of Seb's is contradicted, because a cleared near-finding is an auditor's note and Seb never ruled on it. Therefore no `revised` line is owed and the clearance is narrowed, not flipped. Lesson embedded: a clearance covers exactly what its citations cover.
- 2026-08-19 · KILLED CLAIM, recorded so no future run re-derives it: "a `-subscription` model id always reports cost 0/0" is FALSE, and so is every enumeration of the connections for which it IS true. The suffix marks auth type alone (openai-model-id.ts:20,23). Zero comes from specific fetch paths: the ChatGPT/Copilot/Ollama/Qwen-Token-Plan/OpenCode-Go parsers hardcode 0/0, AND — the trap that broke two successive enumerations — any subscription connection whose live fetch is skipped or empty falls to the curated list, which hardcodes 0/0 for every model it adds (model-fallback.ts:299-300, :333-343). Anthropic ALWAYS takes that path (model-discovery.service.ts:214-215), as do the Kimi Coding Plan and Sign in with Google (fetchers return [] at :1010-1013 and :1022-1026) and Kiro (routing/proxy/kiro-adapter.ts:65-66). ClinePass does NOT (nulls at :1351-1352) and enriches from the catalog, as does a live-discovered zai/glm-4.6-subscription (0.6/2.2 today). Conclusion for any future docs wording: never write a closed list of "the connections that report zero"; say a subscription connection CAN report zero.
- 2026-08-19 · cleared near-findings from the #2733 run (recorded so they are not re-derived): reference/api.mdx:124 "Manifest omits the entire capabilities object when it has no known metadata" is TRUE and reachable (needs all four fields empty, openai-model-capabilities.ts:26-34, which custom-provider models satisfy, model-discovery.service.ts:597-608); api.mdx:176's cost prose matches openAiModelCost field for field; api-key-providers.mdx:46 "Ollama" is a line-wrapped "Ollama Cloud" inside the subscription-only Note and is correct; local-models.mdx:91-95 host.docker.internal claim holds on every documented install path (compose default docker-compose.yml:70 + runtime alias resolution); api-key-providers.mdx:75 "Nothing re-checks it on a schedule" is TRUE of the connection's model list (no @Cron for discovery; the daily catalog refresh does not change which ids a connection serves); the newly-reachable tool_call filter fires for ZERO Ollama Cloud models today; quality_score and the capability flags drive NOTHING user-facing (single consumer computeQualityScore; the only frontend reader of quality_score is imported by a test file; auto_assigned_route is written null at every insertion site, tier.service.ts:66,166,223 — "tier auto-assignment" does not exist as live code, so the PR body over-claims); features array ORDER differs from the doc example but array order is not a contract; subscription-based-providers.mdx:33 "Subscription token" is not false though the product UI says "API key" on that row; errors/M303.mdx scopes itself to localhost-routing tiles, so no sentence is false about Ollama Cloud. INTERNAL-LEAK sweep over every *.mdx, docs.json and scripts/*.js for the stage-1 codenames: 0 hits.
- 2026-08-19 · SCOPED RUN (llm gateway: routing, tiers, fallbacks · cron rotation, autonomous, no human) · page llm-gateway.mdx · window: the full current gateway code path at main 6b045749b + the merged platform PRs of the last 30 days touching routing/proxy/tiers/fallback/limits (#2639, #2600, #2573, #2676, #2689, #2685, #2712, #2611, #2535) · 4 findings filed as issue #82; lastAuditedPr untouched (scoped run), topic stamped 2026-08-19. Categories: WRONG ×1, MISSING-CASE ×3. Verifier took 3 rounds: round 1 FAIL (a false coherence constraint, invented vocabulary, an unbacked journey step, an announced-but-unwritten backlog filing, a garden-path title, doc quotes in visible prose, a preamble claiming an action that had not happened), round 2 FAIL (an impact line whose own citation showed the harmless case, two titles whose "it" attached to the wrong actor), round 3 PASS.
- 2026-08-19 · SCOPE DECISION (full mode produced no window) · the only PR merged since #2733 is #2734, whose base branch is fix/ollama-cloud-models-dev-capabilities, not main; its merge commit is not an ancestor of origin/main, so nothing in it is live. lastAuditedPr stays 2733, per the same call recorded for the #2733 run: #2734 gets audited when it reaches main. No main-landed PR therefore existed and the nightly rotation ran instead, on the oldest topic (llm gateway, last audited 2026-08-09).
- 2026-08-19 · WRONG · llm-gateway.mdx:21,75 · both sentences promise that naming a model skips routing entirely, but a matching custom-tier header is compared FIRST and wins (proxy.service.ts:904 → :976-979, returns at :978 before the catalog lookup at :981, comment :957-959), and because explicitRouting (:1058-1073, fallback_routes null at :1066) is then never reached, the tier's fallbacks apply too (gate :502, chain :507) and the response reports standard/header-match, not direct. The page states the rule correctly at :52 (shipped for #68 finding 8); this is the pair of sentences that fix did not touch · status: raised · https://github.com/mnfst/docs/issues/82 (finding 1)
- 2026-08-19 · MISSING-CASE · llm-gateway.mdx:93,97 · the fallback section promises "a response instead of an error" for any status >= 400 and never states the streaming boundary: before the first chunk a silent stream becomes a synthetic 502 that re-enters the chain (proxy.service.ts:592-596,612-634), after it the error is written in band as an SSE event and the response ends with no further model tried (proxy.controller.ts:489,740-748,804-839). reference/api.mdx:184 already says the second half, so the feature page contradicts the reference · status: raised · https://github.com/mnfst/docs/issues/82 (finding 2)
- 2026-08-19 · MISSING-CASE · llm-gateway.mdx:39,48 · both "every request lands on your default tier" sentences are false universals: a request whose last user message contains the heartbeat keyword is routed by proxy.service.ts:918,926-927 to the first complexity slot, bypassing resolveLazy's custom-tier check (:110-113) and default tier (:116-118) entirely; on a harness configured the modern way that slot is empty and unreachable from the dashboard, so the answer is M101 "You're connected, but no providers are set up yet" (resolve.service.ts:207-217,467 → proxy.service.ts:275-287,1425-1428 → error-codes.ts:34-37). Recommended option is the platform fix, also parked on the backlog · status: raised · https://github.com/mnfst/docs/issues/82 (finding 3)
- 2026-08-19 · MISSING-CASE · llm-gateway.mdx:75,104 · a 429 puts the route into a per-process cooldown (proxy-fallback.service.ts:521-533, key :569-578, TTL from Retry-After capped at 5 min :96/:552-567, else 15 s :95) and a live entry returns a synthetic 429 with no provider call (:392-395,488-519); the primary forward goes through the same method with agentId and authType (proxy.service.ts:419,429,435), so a direct model call comes back with a status the provider never sent, and the page mentions 429 only as a fallback trigger · status: raised · https://github.com/mnfst/docs/issues/82 (finding 4)
- 2026-08-19 · VOCABULARY DECISION (upheld by the verifier) · the product's word for the keep-alive path is "heartbeat" (the Requests table icon carries title="Heartbeat", message-table-cells.tsx:125-131) and no .mdx page uses it (zero corpus hits). The tech-writer's invented "keep-alive ping" was replaced by the product's own word. Rule generalized: when a path has a user-visible label anywhere in the product, that label is the docs word; inventing one is a finding against the draft, not a style choice.
- 2026-08-19 · KILLED CONSTRAINT, recorded so no future run re-derives it: the claim that naming the stream warm-up env var in llm-gateway.mdx would create the halfway state is FALSE. reference/environment-variables.mdx:59 already carries STREAM_WARMUP_MS (default 15000) and docker/.env.example ships it commented, so the never-halfway invariant is already satisfied and a self-hosted sentence may name it. The drafting agent asserted the absence without grepping the reference page.
- 2026-08-19 · cleared near-findings from the llm-gateway rotation (recorded so they are not re-derived): (a) :127 "with a single provider, fallback can only switch between that provider's models" is TRUE — the per-provider auth-type exclusion applies to the legacy inference path only, structured routes carry their own provider/auth (proxy-fallback.service.ts:217-222,236-241); (b) :148 "immediately if you raise the threshold" is TRUE, the 60 s rules cache is invalidated on every rule mutation (notifications.controller.ts:97,110,120); (c) :15 "a bad or missing key stops here with a 401" is an overview simplification that streaming/chat clients do not see (proxy-exception.filter.ts:38-44) but the authoritative page states the split (errors.mdx:21), same call as the M204 note at :151-154 and the same reasoning that cleared the 2026-08-14 M204 simplification; (d) the Configuration steps at :114-124 ("select a tier") do not match a select-then-configure interaction because the clean-agent Routing view renders the cards directly (Routing.tsx:605-670, FallbackList.tsx:548-590), but no user is blocked, so not raised; (e) no second IMAGE finding on this page while #53 finding 5 is still awaiting capture; (f) the absence of the retired complexity/task-specific vocabulary from this page stays correct, per decisions.md:73 and :80; (g) :89's softened M302 sentence, :132's 180 s timeout and synthetic 504, :138's exhaustion headers, :142-148's hard limits, :87's X-Manifest-Tier: direct, :54's reserved header keys, :71's "as many tiers as you need" (no count cap in header-tier.service.ts) and the M200 message example at :145 all re-verified TRUE at HEAD; (h) INTERNAL-LEAK sweep of the page against the platform report's internal-name list: zero hits, and PROVIDER_TIMEOUT_MS at :134 self-scopes in its own self-hosted sentence and has its row at reference/environment-variables.mdx:58.
- 2026-08-19 · BOOKKEEPING CORRECTION, not a reversal (no verdict flips): the four accepted findings of issue #53 were still recorded as "PR pending" although commit 6b65f3a shipped them; their lines now read fixed. Verified against the live page at ca319b8 during this rotation.
- 2026-08-19 · OPEN QUESTIONS (published as questions in issue #82, deliberately not findings) · llm-gateway.mdx · (a) does the per-attempt timeout bound the body of a streaming response or only the time to headers? The streaming timer is cleared in the finally once fetch settles (provider-client.ts:761-767,796-798), which suggests headers only, and if so the advice at :132-134 is buffered-only reasoning; (b) does the API accept more than 5 fallbacks on a custom tier? No size validator on models (header-tier.controller.ts:82-84, only routes capped at :88) and no DB constraint, while the default tier validates both (routing.dto.ts:202,213). The doc sentence at :112 matches the dashboard either way, so the answer only decides whether a caveat is owed.
- 2026-08-19 · SCOPED RUN (observability: requests page, analytics · cron rotation, autonomous, no human) · page observability.mdx · window: the whole observability surface at platform main 6b045749b, plus the PRs that built it (#2644 #2648 #2653 #2660 #2662 #2663 #2666 #2675 #2676 #2678 #2683 #2685 #2689 #2691 #2697 #2712 #2717 #2727 #2729 #2733) · 8 findings filed as issue #83; lastAuditedPr untouched (scoped run), topic stamped 2026-08-19. Categories: WRONG ×4, MISSING-CASE ×4. ROTATION TRIGGER: no PR merged into main since #2733. Two PR numbers above #2733 exist and were BOTH excluded because they were merged into feature branches, not main: #2734 (base fix/ollama-cloud-models-dev-capabilities, merge commit db0fccdfa, not an ancestor of origin/main) and #2677 (base fix/provider-key-label-pin; the same title reached main as #2678). Full loop ran (platform-engineer truth → TWO docs-auditor lenses in parallel, claim-by-claim and gaps-and-cases → tech-writer drafts → issue-verifier fresh-eyes adversary). Verifier returned PASS-WITH-EDITS, killed nothing, and caught TWO FALSEHOODS IN SHIPPABLE DOC PROSE: (a) finding 6's draft said the endpoints "take one query parameter" and that "any other parameter name gets a 400", both false because RangeQueryDto also whitelists agent_name (range-query.dto.ts:9-11); (b) finding 3's evidence claimed the pricing cache key is the string written to agent_messages.model, false because canonicalizeAgentMessageKeys rewrites it (custom-provider.service.ts:183-186) — only the lookup-before-canonicalisation order is load-bearing, and it survives. Also stripped an AUDIENCE ban-2 leak from the visible caveat (a sentence about which machines carry a harness key = credentials handling, on a public issue), corrected 10 line citations (LimitRuleTable.tsx:39 was the loading skeleton, :109 an SVG path inside the "No provider" tag), cut finding 5's journey from 7 steps to 6 and removed a doc line number from its visible text, fixed two pronoun-ambiguous titles, added citations for three journey steps that had none, and normalised seven "Recommended" marks to the parenthesis form. Auditor cleared near-findings (recorded so not re-litigated), both lenses concurring on the first three: the M-code claim at :54 — the structured error_code field and its clickable docs link ARE unreachable (MessageDetails.tsx:386-396 renders only when onRowSelect is absent; MessageLog.tsx:992, Overview.tsx:547, GlobalOverview.tsx:797 all pass it), but formatManifestError persists "[... Manifest M###] ... See https://manifest.build/docs/errors/M###" verbatim into error_message (error-codes.ts:94-104, proxy-message-recorder.ts:770-771) and the drawer prints it, so the user-visible truth holds and the doc promises a legible distinction, not a field (COROLLARY: no future wording may upgrade the promise to a clickable link or a labelled "Error code" field); local/custom cost=0 display (settled 2026-08-10 and 2026-08-11, NOT re-raised — finding 3 touches only the PRICED custom model, a path no prior clearance's citations reach); ":88 the rule takes effect immediately" (literally true for blocking, notifications.controller.ts:92-99, and :92 already states the alerting lag); ":36 open any row for the request body" (universal is false when logs are off, but :56 states the precondition two paragraphs later); ":98 check it's current in your profile settings" (checking is possible; that it cannot be CHANGED went to the backlog); the filter bar, five KPI cards, four chart tabs, connection detail page and trigger-history table are undocumented by curation, not omission (AUDIENCE principle 1); fresh-vs-upgrade, existing-harnesses, no-provider and subscription dimensions all COLLAPSE on this surface (no per-install default, no migration, no backfill anywhere in the analytics or notifications surface). No IMAGE finding: two image requests already stand on this page awaiting a capture (#54 finding 4, #70 finding 5), following the llm-gateway precedent. INTERNAL-LEAK sweep of the page AND of the filed issue body: 0 hits (the healing service codename, the error-page CMS codename, the dev-only gateway tester, the doctor-release template, the telemetry ingest host, the seeder credentials, the internal push header). AUDIENCE ban 3 holds: the only env-var mention on the page (the EMAIL_* family, :108) sits inside the self-hosted toggle block. Coherence GREEN (48 pages, 57 vars). No live reproduction: no harness key on this machine. 9 out-of-scope observations parked in backlog.md in commit 3804ad2 BEFORE the issue was filed.
- 2026-08-19 · WRONG · observability.mdx:66 · "over a rolling period" is false: the limit window is a CALENDAR one anchored on the server process's local clock (period.util.ts:16-41 — day = local midnight, week = Monday 00:00, month = the 1st, hour = the top of the PREVIOUS clock hour), used by both evaluators (limit-check.service.ts:73, notification-cron.service.ts:104) and by the consumption sum (notification-rules.service.ts:144-145); the count restarts at the next boundary (period.util.ts:47-72). llm-gateway.mdx:148 already describes the same mechanic correctly from the blocking side, so the two pages contradict each other today. DRAFTING CONSTRAINT for whoever applies this: never write "UTC", the boundaries are deliberately local (period.util.ts:9-15) · status: raised · https://github.com/mnfst/docs/issues/83 (finding 1)
- 2026-08-19 · WRONG · observability.mdx:27 · "a failed attempt can still burn tokens" is false: cost_usd and non-zero tokens are written at exactly two sites, both success paths (proxy-message-recorder.ts:1105 in recordFallbackSuccess, :1203 in recordSuccessMessage); the four failure builders (:638, :737, :807, :934) supply none of them, so every failed attempt keeps the buildMessageRow defaults input_tokens: 0 / output_tokens: 0 (:301-302) and a NULL cost, and contributes nothing to the spend chart, the model table or a cost rule's consumption. The sentence is offered as the REASON cost lives on attempts, so it seeds a wrong premise for anyone reconciling the dashboard against a provider invoice · status: raised · https://github.com/mnfst/docs/issues/83 (finding 2)
- 2026-08-19 · WRONG · observability.mdx:59-61 · "Custom providers do the same, because Manifest can't infer pricing for a model it doesn't know" is false whenever the user priced the model: the connect form carries visible per-model columns "Input / 1M tokens" and "Output / 1M tokens" (CustomProviderForm.tsx:410-420), the cache stores an entry whenever at least one is filled and skips only when both are empty (model-pricing-cache.service.ts:293-313, :303), and the recorder looks pricing up on the caller's raw model string BEFORE canonicalisation (proxy-message-recorder.ts:1154-1164 vs :1168), so cost is computed normally. NOT a re-raise of the settled NULL-vs-literal-zero DISPLAY question (2026-08-10, 2026-08-11): those clearances reasoned about an UNPRICED model, a different path; the verifier adjudicated this explicitly and found no oscillation and no derivation owed. providers/custom-providers.mdx:65 carries the same falsehood and is parked in backlog.md for the providers topic; under the never-halfway invariant the two pages should be fixed together · status: raised · https://github.com/mnfst/docs/issues/83 (finding 3)
- 2026-08-19 · MISSING-CASE · observability.mdx:92 (read with :110) · the page promises one email per period and covers the no-sender case, never the failed-send case: both evaluators check hasAlreadySent, then INSERT the dedup log row, and only then attempt the send (notification-cron.service.ts:106 → 115-124 → 126-140; limit-check.service.ts:118 → 126-135; dedup notification-log.service.ts:14-20), so a failed send is never retried inside the period and the cron's own "will retry next cron run" warning (:141-143) describes a retry that cannot happen; the Limits page's Triggered column counts those same rows (notification-rules.service.ts:44-55, LimitRuleTable.tsx:80,128) so a crossing that emailed nobody still increments, and the History panel shows it too (LimitHistoryTable.tsx:28). Silence therefore reads as "no crossing" and the dashboard confirms the wrong conclusion. Platform-side fix also parked in backlog.md · status: raised · https://github.com/mnfst/docs/issues/83 (finding 4)
- 2026-08-19 · MISSING-CASE · observability.mdx:20-27 (read with :36) · the request model gives a Request three shapes and never names Pending or Cancelled: a Request is written pending with ZERO attempts at ingress before routing (proxy.controller.ts:256-268) and a caller disconnect writes a terminal cancelled row keeping only an already-started attempt (proxy-message-recorder.ts:590-608); both render their own pills (message-table-cells.tsx:402-419). The Requests log's Failed filter is `NOT IN` the success list (messages-query.service.ts:268-272) so it RETURNS both, while the Overview's KPI universe excludes them via sqlIsCompletedStatus (query-helpers.ts:67,73-75; request-volume.service.ts:136) and its Failed requests card deep-links straight into that filter (AutofixKpiCards.tsx:168,170) — two irreconcilable numbers on screens the page presents as one dashboard. Opening a zero-attempt row also shows "Manifest rejected this request before contacting a provider" (RequestDrawer.tsx:413-439), which the page's own :25 teaches the reader to read as a hard limit; the platform half of that is parked in backlog.md · status: raised · https://github.com/mnfst/docs/issues/83 (finding 5)
- 2026-08-19 · MISSING-CASE · observability.mdx:116-126 · the section names a use case ("stopping work before a limit blocks you") and documents no parameter, no default and no response field: usage defaults to 24h and costs to 7d (agent-analytics.controller.ts:26,37), range is a closed set 1h|6h|24h|7d|30d|90d|365d (@IsIn, range-query.dto.ts:4-7; range.util.ts:5) rejected with a 400 by the global whitelist pipe (main.ts:113-121), API windows count back from now (agent-analytics.service.ts:41-44) while limit periods start at a calendar boundary (finding 1), and message_count excludes pending, cancelled and every error status while the token sums in the same response include every attempt row (query-helpers.ts:90-96). NOTE for whoever applies this: the draft's JSON sample VALUES are illustrative, not captured (no live call was made); the FIELD NAMES are verbatim from agent-analytics.service.ts:14-32 and agentName is on /usage only. NOTE 2: the endpoints also whitelist agent_name (range-query.dto.ts:9-11) and ignore it, so no wording may say range is the only accepted parameter · status: raised · https://github.com/mnfst/docs/issues/83 (finding 6)
- 2026-08-19 · MISSING-CASE · observability.mdx:48 · the Free-plan note scopes the range lock to the Overview; the same three ranges are locked on the Requests log too (MessageLog.tsx:86-87 PRO_RANGES with the comment "Same Pro gating as the Overview range selector", built :266, badged/disabled :584-598, selection refused :328), and the log additionally offers "All time" which is OUTSIDE the locked set and stays free (MessageLog.tsx:587-598) while the Overview has no All-time option at all (GlobalOverview.tsx:134-140). The phrase "covers up to 7 days of history" also reads as retention, hiding the one view that answers the reader's question; request rows survive log retention (request-recording-retention.service.ts nulls recording_key only). NOT a re-raise of #54 finding 3 (fixed, docs PR #63): that finding put the sentence on the page for the Overview, this one extends it to the second screen · status: raised · https://github.com/mnfst/docs/issues/83 (finding 7)
- 2026-08-19 · WRONG · observability.mdx:41-43 · the Limits card in the CardGroup at :31-44 sits OUTSIDE every data-deploy wrapper and promises "plus email provider setup" to a default-Cloud audience that has no such screen: Limits.tsx:246-261 mounts the provider picker only when isSelfHosted() is true, cloud falls back to the read-only CloudEmailInfo card headed "Email alerts" (CloudEmailInfo.tsx:15-17), and the "Configure email provider" heading with its three tiles exists only on the self-hosted branch (EmailProviderSetup.tsx:19 via EmailProviderSection.tsx:66-75). AUDIENCE's Cloud/Self-hosted rule makes a mode-dependent instruction outside the toggle a WRONG finding in its own right. CONSUMES the 2026-08-17 backlog line on this exact point, which was parked during the full audit of PR #2727 and explicitly deferred to this topic's own audit; the verifier confirmed the re-decision is legitimate and owes no reversal derivation, since a cleared near-finding is not a Seb verdict · status: raised · https://github.com/mnfst/docs/issues/83 (finding 8)
- 2026-08-19 · MISSING-CASE · llm-gateway.mdx:96-108 (the "What triggers a fallback" section) · a tier moves to its fallback list with no provider error and no HTTP status when Manifest itself removes the pinned model from the connection's discovered list (model-discovery.service.ts:329-330; the one-token lookupModel → lookupModelCapabilities swap extends a filter dating from ca5fdd3f0 to the five providers of the new capability-only map, openrouter included); stored tier rows are validated only at write time (tier.service.ts:101-111), isRouteAvailable then returns false because the connection still exposes other models (provider-key.service.ts:429), and the fallthrough is a backend-log-only warn (resolve.service.ts:447-450) ending in the tier's first credentialed fallback or M101 (proxy.service.ts:275-287 → :1425-1429). No response header marks the swap: X-Manifest-Fallback-From originates only at proxy.service.ts:1240, after a failed attempt. NOT a re-raise of issue #81 finding 1 (that finding is on providers/api-key-providers.mdx and documents the removal itself; this one is on llm-gateway.mdx and documents what an already-configured tier does afterwards) nor of any of issue #82's four findings on the same page · status: raised · https://github.com/mnfst/docs/issues/84 (finding 1)
- 2026-08-19 · CORRECTION, not a reversal, of the same-day #2733-run clearance of providers/api-key-providers.mdx:75 ("Nothing re-checks it on a schedule" cleared as TRUE) · that clearance cited backend cron paths only (no discovery cron; the daily capability-catalog refresh does not change which ids a connection serves) and the mechanism that falsifies the sentence is a FRONTEND trigger no citation reached: the model picker re-runs discovery on mount, once per day per harness, whenever a non-custom active provider's models_fetched_at is not today (ModelPickerModal.tsx:126-143, wasFetchedToday :74-83). The clearance is narrowed, not flipped, and no Seb verdict moves, so the reversal protocol owes no `revised` derivation here (same shape as the two earlier cleared-near-finding precedents in this file). Parked in backlog.md for the providers topic and deliberately left out of issue #84's suggested wording, because stating the timing on llm-gateway.mdx would put two pages in conflict · status: recorded
- 2026-08-19 · FULL RUN (cron, autonomous, no human) · window: the two PRs merged into platform main since the #2738 watermark, cross-checked by ANCESTRY not by number per the 2026-08-19 lesson (`git log 7b09c5ebd..origin/main` returns exactly `2c3f412f9` = #2736 and `9f2e05655` = #2741, both confirmed ancestors of origin/main) · note that #2736 sits BELOW the watermark by number and above it by merge time, so a number-only scope would have missed the run's only substantive PR · 2 findings filed as issue #85; lastAuditedPr advanced 2738→2741; topics.json untouched (full run, not a rotation). Categories: OBSOLETE ×1, MISSING-CASE ×1. Full loop ran (platform-engineer truth → two docs-auditor lenses → tech-writer → issue-verifier ×2, FAIL then PASS). The previous run had audited this same window at 20:43 UTC, parked six out-of-scope observations in backlog.md and stopped without filing or advancing state; this run redid the window end to end.
- 2026-08-19 · OBSOLETE · providers/api-key-providers.mdx:14-37 and :83-101 (+ :4, :7) · neither the supported-providers table nor the key-prefix table carries the provider added by platform PR #2736, and no page in the corpus names it (zero grep hits across every .mdx); the connect form enforces its prefix and a 40-character minimum client-side, so the page's own "double-check the prefix below" advice dead-ends on the one provider that produces a prefix error. Governed by the accepted Meta precedent (decisions.md:66, platform PR #2702/#2717, option 1, docs PR #73); the prefix row clears the bar of the REVERSED verdict at decisions.md:47, which rejected rows only for prefixes the form does not enforce · status: raised · https://github.com/mnfst/docs/issues/85 (finding 1)
- 2026-08-19 · MISSING-CASE · providers/api-key-providers.mdx:18,41-43 (read with subscription-based-providers.mdx:18) · the dashboard now renders two Google-branded API-key cards adjacent (Gemini Free, Google, Google Vertex AI, Groq after the subscription-only entries are filtered out), the two take incompatible credentials (empty prefix / 30 chars versus AQ. / 40 chars) and point at different key sources, and no page gives a reader a basis to choose; the one product string that would explain it (the registry subtitle) has a single render site serving local-server providers only, so it never reaches a screen · status: raised · https://github.com/mnfst/docs/issues/85 (finding 2)
- 2026-08-19 · OPEN QUESTIONS (published as questions in issue #85, deliberately not findings) · providers/api-key-providers.mdx · (a) has anyone connected a real express-mode key and routed a request through it end to end? The platform PR's description claims both addressing modes were verified live against a real key, and no stage of this run could reproduce that: no key, no upstream call. The docs are about to advertise the provider, so nothing in the wording asserts that it serves a request. (b) every other name cell in the supported table links to a vendor page, and the obvious vendor URL now 301s to a page carrying a different product brand, which works against the disambiguation finding 2 asks for. Leave the cell unlinked (the draft's choice, with two unlinked precedents on the subscriptions page) or link it anyway?
- 2026-08-19 · cleared near-findings from the #2736/#2741 run (recorded so they are not re-derived): (a) the regional-providers line correctly stays silent about the new provider — it declares no api-key endpoint regions and the connect API answers a region for it with a 400, so no region mention is owed and the backend's project/location mode stays a parked platform item; (b) the subscriptions page and the local-models page correctly owe it no row; (c) the whole CASE-MATRIX grid collapses for this change — not local-only so cloud and self-hosted are identical with no toggle owed, static frontend list so fresh and upgraded installs match with no migration or flag, and the connection fans out to every harness in the workspace exactly as any other provider does; (d) NO IMAGE FINDING for the Usage-based screenshot, which shipped one day earlier: its caption sells page shape rather than an inventory, and the new card renders as a bare letter tile until its icon PR lands, so a re-shoot today freezes a state that changes again (parked for re-check instead); (e) the page's dashboard hedge at :39 is literally true and does not excuse a missing row, since it was already on the page when the identical Meta omission was accepted; (f) the personal booking link that sits in a platform file the PR edited is byte-identical to this docs site's own published navbar CTA, so it is user-facing surface on both sides and outside the AUDIENCE ban.
- 2026-08-18 · issue #71 PROCESSED (batch, Seb's "go" 2026-08-18, recommended options) · finding 1 accepted (option 1: both sentences rewritten around `auto`, the spend-cap and cost facts kept in the lede via the closing sentence); finding 2 accepted (option 1: "Your first request" section behind the deploy-mode toggle, cloud minimum path + curl, self-hosted branch links to /self-hosted#first-request; frontmatter keywords extended); finding 3 accepted (option 1: calendar-month reset at 00:00 UTC on the 1st + sidebar usage meter, in the plan-limits sentence); finding 4 accepted (images/welcome-setup.png shipped) · the screenshot was captured on a LOCAL instance booted in cloud mode (MANIFEST_MODE=cloud, fresh empty DB, SEED_DATA=false) with a freshly registered account, because the self-hosted local instance shows self-hosted copy ("Logs stay in your own database") and non-zero progress from the seeded harness; the capture shows the cloud copy ("Logs are stored in your Manifest workspace") and "Setup progress 0/5", matching what a real cloud signup renders (Welcome.tsx renders identically in both modes at step one except that copy line) · the two OPEN QUESTIONS stand unanswered and the shipped wording does not depend on them, except that "Then connect a provider" overstates the required action IF cloud pre-provisions the managed free tile; flagged to Seb in the PR · status: accepted → PR pending · https://github.com/mnfst/docs/issues/71
